# 更新日志

## 未发布

### 功能

- **标签匹配现在能同时写入封面，并且把「覆盖已有封面」做成独立开关**。
  - **缺的只有中间那一段**：对话框里 `cover: '封面'`（`TagMatchDialog.vue:56`）与 `(已有封面)`（`:68`）早就写好了，但 `metadata-match.ts` 的 `buildPatch()` 只 `consider('title'/'artist'/'album')`，从不产出封面，所以那两行永远不会被触达；封面下载（`IPC.matchCover`）与三条写入路径（MP3 APIC / FLAC picture 块 / 同名 sidecar）也都齐全。**`metadata-match.ts` 一个字没改** —— 封面进 `fields` 会立刻撞红 `tools/e2e-verify.mjs:643-647`（那条拿一个字段全对得上的文件断言「无改动」），所以封面完全在 `apply` 那一侧处理。
  - **两个开关是独立的，这是用户拍板推翻我的地方**：我原来想把封面挂在既有的「覆盖已有标签」上（一套心智模型），被否。**理由是风险不对称**：文本写错明天还能再匹配一次，用户自己内嵌的 1200×1200 封面被搜索结果那张 300×300 顶掉是不可逆的（`.bak` 是整文件备份、不是逐字段，普通用户不会去挖）。于是新增「同时写入封面」（默认开，与歌词对称）+「覆盖已有封面」（默认关，只在文件本来有封面时才出现 —— 摆一个点了没作用的框比不显示更糟）。
  - **不做「新图更小就静默跳过」**：Picard 有那条过滤，但它是批量过上千文件、用户不在跟前的工具；这里是单文件、盯着预览点确认的对话框，判断权已经交出去了。改成**把两边的尺寸与体积并排摆进预览**（`当前 367×347 → 将写入 300×300 · 16.9 KB（比现有封面更小）`），写不写由那个开关决定。⚠️ 一处与文档给的示意不同：**「当前」那一侧只有像素尺寸、没有体积** —— 体积要 `fetch(jjmedia://…)` 才能拿到，而页面 CSP 的 `connect-src` 不含 `jjmedia:`，为这个数要么放宽 CSP 要么新开一条 IPC，都不划算；决定降级与否的是尺寸不是字节。
  - **主进程再挡一次，而不是只信渲染层**：`target.coverPath` 取索引里的事实（对话框拿的是打开那一刻的快照），并且 `matchApply` 会先 `delete effective.cover` —— 这条通道的封面**只有那两个开关一个入口**，补丁里夹带进来的图一律丢掉（`buildPatch()` 不产封面、前端也不发，所以正常路径无感，绕过的路径被堵住）。跳过封面**不会**跳过同一批里的歌词，两个分支各自独立。
  - ⚠️ **`dryRun` 必须挡在下载之前**：`commitAssetWrite` 自己会早退、`exportAssets` 在 dryRun 下不落盘 —— 但**下载是主进程发的网络请求，那两道都挡不住它**。判据抽成 `shouldFetchCover(options, hasCurrentCover)` 而不是留在 `handle()` 里，就是为了这条能写单测。
  - **护栏照成熟实现抄**：只接受 `image/jpeg|png|webp`（`asset-files.ts` 对不认识的 mime 一律给 `.jpg`，门户页/错误页的 `text/html` 就是这么变成一张"封面"的）；尺寸上限沿用既有常数（8 MB / 10s），不引入第四个数字；sidecar 命名保持**同音频名**（`Song.jpg`）而不是 Picard 的 `cover.jpg`，那套命名已被 `asset-export.test.mts:80` 钉住。
  - 新增 `src/main/online/cover-fetch.ts`（`fetchCoverBytes` + `shouldFetchCover`，`getBytes` 可注入，照 `artist-images.ts` 的先例）；`MatchApplyOptions` 进 `shared/library-types.ts`（原来同一个内联对象类型在主进程和 preload 各写一遍，加字段会悄悄漏）。
  - 验收：单测 `src/test/cover-fetch.test.mts` **15 条**（含 text/html 与 GIF 被拒、`image/jpg` 归一、dryRun 两种形状都不发请求）；实跑 `.cache/cover-match-check.mjs` **24 项全绿** —— 三首副本现造（其中一首剪掉 ID3 变成真没封面），全部封面写入走**真实在线候选的 picUrl**，用 music-metadata 读回文件的 picture 逐字节比对：不勾覆盖时封面 sha 不变而同一批歌词写进去了、勾上才换、**连写两张不同候选之后 MP3 里只剩一张且是后那张**（node-id3 对 APIC 是替换不是追加）、FLAC 的 1200×1200 被顶掉后 picture 块只有 1 个、`.bak` 里读回来还是写前那张、sidecar 落到 `有封面.jpg` 而不是 `cover.jpg`、dryRun 之后 sha 与 mtime 一字不变、预览那张图与列表缩略图**是同一份字节**（没有多发出请求）。
- **封面升降动画两半重做，播放列表改成就地面板（3 页历史 + 改序 / 逐条移除 / 清空）**。
  - **动画是「重做」不是「补收起」**：升起那一半从 `v0.1.3` 就在，用户明确说"现在升起的效果我不满意"。读代码定位到三个具体参数而不是机制 —— FLIP 骨架（量首尾盒 → 打反向 transform → 双层 rAF 释放）保留，`opacity = '0.35'` 删掉（它让封面"先淡再长大"，读起来是淡入淡出而不是飞过去，这正是不满意的主要来源），时长从 `--dur-slow`(320ms) 提到进入 **420ms** / 收起 **300ms**（跨 49px → 420px 的距离，320ms 是"闪一下"）。**收起比进入短**按 Material 的 container-transform 惯例：进场的东西减速落位可以慢，离场的东西加速走不该慢。**圆角插值 6px → 14px**（`border-radius` 走 paint 不走 layout）；**弧线不做**，理由写进注释：单段 transform 是直线，400ms 内肉眼分不出来，做弧要把 `is-flying` 状态机翻倍。
  - 收起方向是本次真正的新代码，**顺序是被几何逼出来的**：先飞封面、飞完 overlay 才滑走 —— overlay 一下移，`.np__art` 自己的盒就在动，量不到正确终点。四条关闭路径（收起按钮 / Esc / 标题栏那个开关）全部收敛到 `App.vue` 的 `closeNowPlaying()`，`closingNowPlaying` 挡住连按；`v-if` 会销毁组件，所以 await 期间不能让 `.np` 重渲染。三个已踩过的坑原样保留：零尺寸起点直接不飞、双层 rAF、60ms 等 overlay 落定。**`reduceMotion` 两个方向都判**，否则"减少动态效果"下会飞一半。
  - **面板抽成 `PlaybackPanel.vue`，`<Teleport to="body">` 挂在 App 根**：以前主页是 `/queue` 整页路由、播放页是 `.np-panel` 抽屉，同一个队列两套皮肤、能力还不一样（抽屉没有改序和「当前播放」）。现在一个组件、`ui.playbackPanel` 驱动，主页与播放页点开的是同一个东西 —— 「同步改动主页面和播放页」靠的是没有第二份代码，不是改两遍。`.np-panel` / `.aria-label="关闭播放面板"` 这些类名和钩子**刻意保留**，冒烟按它们选元素。
  - ⚠️ **Teleport 之后 Esc 必须自己接**：App 的全局快捷键挂在 `.shell` 上，面板搬进 `body` 后焦点在面板里时事件根本不经过 `.shell`，"点遮罩能关、Esc 不能关"就是这么来的。面板自带一个 window 级 keydown，只在开着时挂。
  - ⚠️ **面板高度必须是固定值，不能只给 `max-height`**：截图当场抓到 —— sheet 只有 157px 高、列表被压到窗口外面，而"恰好 N 行"的断言**照样绿**（虚拟列表在 DOM 里就是有那些行）。原因是列表是 `flex:1; height:auto`，没有高度约束就塌成 0。现在 `height: min(70vh, 620px)`，探针加了一条"视口有真实高度 + 第一行在窗口内"钉住。
  - **3 页历史 = 当前队列 + 2 份归档**，挂点只有一个：`playQueue()` 是"换一整份列表"的唯一入口（全仓 19 处调用都走它），归档在**替换之前**做。`addToQueue` / `insertNext` / `removeFromQueue` 不算新列表 —— 否则插一首歌就多一页历史。⚠️ **归档条目带的是它自己当年的名字**（`queueLabel`），不是接班那份的：写反过一轮，结果是「专辑 · 三」下面躺着二的那批歌。同一份列表连续播两次只算一次（比 id 序列），播历史里已有的一页会把那页挪到前沿而不是留重复。
  - **写盘只在"播整份列表"这个动作上，不用定时器**：`lastSession` 需要节流是因为播放位置每秒都在变；队列列表一次用户操作才变一次，节流在这里没有东西可省。体量与 `lastSession` 同量级（实测 974 首 ≈ 611 KB），存的是完整曲目 —— 在线曲目的身份在 `meta` 里，只存 id + 名字没法再播回去。重启后仍能翻（`restoreQueueHistory` 会丢掉形状不对的条目）。
  - **翻页只换显示**：`player.queue`、正在播的歌、底栏一个字不动；要替换必须点「播放这份列表」（双击历史里的行同理）。左箭头=更早、右箭头=更新，两端不循环。**滚轮不接**：用户推翻了自己上一条（"滚轮是查看歌曲列表的"），面板只验"wheel 没被 preventDefault 抢走 + 容器能滚 + 页码不变"。
  - **追加的三件事全部复用现成机制**：拖动改序用 `TrackList` 已有的 `reorderable`（队列以前根本没开这个开关，所以这是新能力不是搬运）；改序逻辑从 `QueueView.vue` 搬进 store 成 `moveInQueue(from, to)`（**它是全仓唯一一份，页面一删就没地方住了**，含 `currentIndex` 的三连修正）；逐条移除是行尾新增的常驻 `×`（`TrackList` 加了可选 `removable` prop，没去动那条被约 15 个视图共用的 `.row-more{opacity:0}` 悬停规则）；清空走 `ui.confirm()` —— 旧队列页是**直接 `clearQueue()` 不确认**的，没照抄。
  - **表头计数**左边是当前第几首、右边是屏幕上那份的总数（翻到历史页右边跟着变）；`currentIndex === -1` 时显示 `—` 而不是 `0`。
  - `TrackList` 新增三个**可选** prop：`rowHeight` / `singleLine` / `removable`，默认值下行为与逐字不变（约 15 个视图共用它）。删掉 `/queue` 路由与 `QueueView.vue`；设置里的 EQ 入口不再顺手拉起播放页。
  - 验收 `.cache/panel-s-check.mjs` **34 项全绿**（独立 profile + 副本曲库，动画按 25ms 采样 `.np__art` 的视觉盒而不是读 class；900 首队列验虚拟滚动的撑高与行距；跑完比对用户真实数据与歌曲原件的 sha256/mtime）；新增 6 条 store 单测（改序三个方向 + 历史压栈/去重/上限 + 形状校验）；冒烟新增 3 条面板断言并把页数判据改成"与 store 里的条数一致"——写死「第 1 / 1 页」会红在早期步骤自己播过的列表上。
- **歌词三处收尾：两个在线入口合成一条「在线匹配歌词」、翻译/音译开关进右键菜单、编辑窗口新增「导出歌词」**。
  - **合并的根据是它们底层本来就是同一个函数**：`searchLyricOnline()` 第一行就是 `lyricCandidates(track)`，
    再取第一条 —— 也就是说旧菜单是同一批候选的两种用法（自动采用 vs 摊开让用户挑）。新列表第一项就是当年
    自动采用那条，所以标了「最佳匹配」；列表本来就按 `score` 降序，不用重算。零额外查询。
  - **采用的语义换了，这才是这一条真正的工作量**：以前在列表里点一下就当场写 sidecar（并按设置改标签）；
    现在走新增的 `lyric:stage-candidate`，与自动抓词同一条通道 —— 立即显示 + 进「待写入」，**一个字节都不写**。
    元数据匹配仍然是应用的猜测，"从列表里选中它"是更强的信号，但还不等于同意改用户的文件。
    ⚠️ `tlyric` / `rlyric` 必须跟着进暂存：`pendingAssets.add` 只存正文，而 `primeLyricCache` 存整次解析 ——
    只传正文的话，采用之后翻译和音译会当场消失（探针 Q5a/Q5b 专门钉这一处）。
    用户丢弃过的同一条词仍然不再进队列（`stageFetchedLyric` 既有语义），但那不是"点了没反应"：会给一句提示。
  - **音译开关搬到菜单，一次三处**（播放页歌词右键 / 底栏「更多」 / 悬浮条原生菜单 + 主窗口「桌面歌词」子菜单），
    不用为了勾一个开关跑到设置页。悬浮条只有一个副行槽位，所以**没加第三行**，改成两个开关各自 gate 自己那一路：
    翻译优先、没有翻译时才轮到音译。**一处行为变化要如实说**：以前只要「显示翻译」开着、这首恰好没翻译，
    音译会不看 `lyricRomanization` 直接顶上；现在音译要自己那个开关也开着。`lyricRomanization` 默认是开的，常见情况不变。
  - 播放页模板原来直读 `library.settings.lyricRomanization`，改成读 `showRomanization`，与「显示翻译」对称 ——
    不然菜单勾了界面不动。
  - **「导出歌词」= 只写同名 `.lrc`，音频文件一个字不动**：`to` 写死 `['sidecar']` 而不是沿用设置，
    `patch` 只带 `{ lyrics }`（带上 title/artist 就会被 `hasMetadataFields` 拉回内嵌那一路）。
    它是"采用即暂存"之后需要的出口：先看着，满意了自己导出。编辑器不关闭、不 `emit('saved')`。
    顺带修掉一处**文档性错误**：那颗按钮原来叫「保存 .lrc」、副标题写着"保存后写入同目录 .lrc"，
    实际上保存按「设置 · 写入位置」走、默认连标签一起改 —— 现在按钮叫「保存」，副标题把两颗按钮的差别说清。
    对照证据：同一颗 MP3 副本上，导出后 sha/mtime 一字不变（Q9d），点保存之后两者都变（Q10）——
    没有这条对照组，"导出不碰音频"是一个可以空转的断言。
  - `sanitiseRequest()`（悬浮窗那条唯一输入通道的白名单）**移到了 `src/shared/desktop-lyric.ts`**，
    紧挨着它校验的那个类型 —— 留在主进程里是因为那里 import 了 electron，node 侧加载不了，
    于是这条通道此前一条测试都没有。新增命令若忘了进白名单，会在原生菜单里**安静地失效**，
    而原生菜单探针点不到。现在 4 条单测钉住：认得的命令、越界的 size/action、未知 type、多余键只取 type。
- **在线曲目在列表行上显示音质徽标，歌单打开时按需回填音质**：以前 `meta.qualitys`（这一首有哪些档）只有下载菜单和「可用最高档」判断在用，列表行一个字节都不显示——本地曲目靠 `SQ` / `Hi-Res` 两枚徽标看得出质感，在线曲目全是哑的。现在两者同款同色，共用设置里已有的「显示音质徽标」开关。
  - **顺手修掉一处会说谎的数据**：`src/main/online/search.ts` 过去给每一首网易云搜索结果无条件写 `128k/320k/flac` 三档（当时的注释承认是「反正只用来试档位，先声明全档」）。徽标一上线，这句话就变成「每首 wy 歌都显示无损」。现在按 `/api/song/detail` 的 `lMusic/hMusic/sqMusic/hrMusic` 逐首判，**只算 `size > 0` 的档**；`mMusic`（192k）在本仓库没有对应档位，丢弃。**零额外请求**：`fetchNeteaseCovers` 本来就要为那一页 20 首调一次这个接口，封面和音质在同一个响应体里，于是它改成 `fetchNeteaseDetails`，一次请求取两样。判别用的探针必须用 size 而不是「有没有缺档」——旧的无条件写法对冷门查询也会缺，那个判据本身没有判别力。
  - **歌单回填**：`PlaylistDetailView` 画完列表再在后台补（660 首实测 2.3 秒，白屏不可接受），按 100 个 id 一批发 7 次；已经有 `qualitys` 的曲目的不再问，所以第二次打开同一个歌单是 **0 个请求**，空歌单 0 个请求。回填结果按 id 合并回列表，比较一次 `loadGeneration`——没有这道守卫时，快速切歌单会把 A 歌单的补丁打进 B 歌单的行里。网络失败静默，不弹错、不挡列表。
  - **写盘侧只加了一个 `patchQualitys`**：`map()` 重建（绝不 filter + push）、逐键合并 `meta`（`songmid` / `albumId` / `hash` 是各平台解析要用的）、跳过本地曲目、`touched === 0` 时连 `persist()` 都不发。既有那两条钉「并发写后仍是 17 个歌单」和「封面被删则丢引用」的回归测试必须仍绿，它们是这条通道的安全网。
  - 实测「忆」660 首：`SQ 597 / Hi-Res 48 / HQ 14 / 标准 1`，与需求文档给的量级一致。**验收跑在 `.cache/quality-profile` 的一份副本上**，用户真实的 `playlists.json` 前后 sha256 与 mtime 未变（`eb0cbde401c74f45…`）。
  - **两处已知瑕疵，本次未动**：kw / kg 搜索行的 `size` 传的是字面量 `1`，所以下载子菜单里显示 `0.00 MB`（徽标只看 `type`，它们显示的档位是对的）；播放页那行 `` `${source} · ${player.quality}` `` 显示的是**请求档位**（默认 `flac24bit`）而不是实际拿到的档位，实际档位 `MusicUrlResult.quality` 进了 `ResolvedSource` 之后没有任何 ref 保存它。
- **桌面歌词悬停时，深色底板盖住整片歌词区**：以前那块板只到卡片为止（实测卡片 61…113、歌词 117…152），中间和下面直接压在桌面上；现在从卡片下沿一路铺到窗口底边，与卡片读起来是同一块板的两段。
  - **底板挂在新的 `.verse` 包裹层上，不是挂在 `.lyric__line` 上**：`.lyric__line` 是 shrink-to-fit 的（宽度=当前这句字宽），那正是"拖动把手跟着文字走"的基础；给它加背景会让板子宽度每句跳变，成了卡拉OK高亮条而不是板子。**底板是 `pointer-events:none` 的纯装饰层**——不然上一轮刚收窄的热区会被这块全宽矩形原样放大回去。
  - 卡片圆角随之改成 `12px 12px 0 0`、底板 `0 0 12px 12px`：卡片原来四角全圆，底板一接上去就在接缝两侧露出两个圆角缺口的桌面色。
  - 原来"不铺底板"的理由（透明悬浮条铺满就成了桌面上的灰盒子）**仍然适用于收起状态**，写在样式表顶部了，没删。`body` 三种状态下都保持全透明，冒烟那条断言管的是"空闲那一半"，现在紧跟着多了一条"不悬停时那块区域一个像素都不画"。
  - 验收 `.cache/plate-check.mjs` **9 项全绿**：量的是截图的 **alpha 通道**而不是颜色（底板 ≈189，未画处 = 0），所以结论与桌面壁纸是什么颜色无关。两处判据本身被修过：①扫描列内缩 16px，否则底板自己的 12px 圆角会让最后几行"必然不达标"；②"这张图有没有判别力"改成看**整张图**是否同时存在透明与不透明像素——原来在"应该铺满的那一段"里要求也出现 0，等于底板铺得越好越判"不可信"。翻译行那条：`#sub` 平时是 `hidden`（`main.ts:117`），盒子全 0，所以显出它和量盒子写在同一个 tick 里，跨过 await 就会被 2Hz 的载荷打回去。锁定态不出现底板由冒烟新增的一条钉住。
- **下载管理：条目从 178px 的堆叠卡片压成一行，新增「移除记录」（可移入回收站）与「打开下载文件夹」**。
  - **先认一笔方向性的错**：这一项最初被描述成"卡片太宽"，我照做并缩到 620px，还用量
    （可用 1004px vs 最宽一行 344px）"证实"了它。用户随后更正：**要缩的是高度，宽度恢复原样**。
    算术没错，但它证明的是"空"，不是"错"——红框圈住整片区域只说明"看这里"，不说明"哪里不对"。
    620px 已回退，现在条目铺满内容区。
  - **一行一条**：`歌名 | 歌手·音质·来源 | 大小 | 状态 | 操作`，写法照 `.track-row` 的 grid，
    高度直接吃 `--row-height`（跟着 rowDensity 走），实测 178px → **72px**。以前它是 5 个
    垂直堆叠的块各带 10px 上下 margin，打底 140px，所以调参压不下来。
  - **下载路径那一行删掉了**（它就是"最没信息量的一行"），改由页头的「打开下载文件夹」承担
    （设置 → 下载设置 里那颗是同一条 IPC 的第二个入口）；确认框里仍然显示完整路径，
    因为在那儿"要删的是哪个文件"必须看清。
  - 两处例外是用户拍板的：**进行中的任务**进度压成**行底一条 2px 细线**（实测
    `h:2 / w:1002 / 行高 72`，不额外撑高）；**失败/警告**各占横跨整行的第二行，这一条允许超过 72px。
  - **列宽全是 `minmax(0,…)`**：窗口最小宽 940，扣掉侧栏 220 与 .view 内边距只剩约 664px，
    而右侧"大小/状态/两颗按钮"几乎不可压。写死 min 宽度会在那里直接横向溢出。实测压到
    664px：溢出行数 0、最大 `scrollWidth-clientWidth` = 0px。
  - **确认框里那颗「删除文件并移除」原来没有边框**：`.btn--danger` 这个变体把
    `border-color` 设成 `transparent`，只留红色文字 —— 在「取消 / 仅移除记录」两只有边框的
    按钮旁边，它看起来不像按钮，禁用时整颗被压到 45% 不透明度更像一句说明。现在给它一圈
    淡红描边（`color-mix(in srgb, var(--danger) 55%, transparent)`），既读得出"是按钮、
    现在不能按"，也仍然带破坏性动作的颜色。只改确认框这一颗，行内那颗「移除记录」保持原样。
  - **「打开文件位置」从条目里搬到页头，改名「打开下载文件夹」**，排在「下载设置」左边。
    理由不只是"挪个位置"：它原来只在**已完成**的条目上出现，于是"下完的东西在哪个目录"这件
    全页通用的事，对失败/进行中的任务就没有入口；搬到页头后它与单条任务的状态无关了。
    目录路径仍由主进程算（含"没设置过就回落到系统下载目录 / JJ Music"和"目录没建过先建出来"）。
    **代价说清楚**：单条已完成歌曲不再有"在资源管理器里选中这一个文件"的入口，只能打开整个
    目录自己找 —— 这是用户明确要的替换，不是遗漏。
  - **「打开下载文件夹」走新的 `downloads:open-folder`**，不能复用 `fileReveal`：那条开头就是
    `if (!existsSync(path)) throw`，而 `mkdir` 只在任务真正开跑时才做 —— 本机默认下载目录
    实测就不存在，复用只会给没下载过的用户一个报错。实现是先 `mkdir(recursive)` 再
    `shell.openPath`，并且**判断 openPath 的返回值**（它失败是返回错误字符串而不是抛异常，
    不读返回值就只能报"打开成功"而屏幕上什么都没发生）。目录路径由主进程算，渲染层一个字符串都不拼。
  - **上一轮已修的三处保留**（它们是真 bug，不是口味）：`var(--border)` 这个 token 全仓从未
    定义过 → 那圈描边**根本没渲染**，改 `--border-subtle`；`--bg-elevated` 暗色下与页面底色
    `#1b1d26` 同色、只有 acrylic 才被加上 alpha → 材质切"无"时条目整个看不见，改 `--bg-panel`；
    `.task-actions` 没有 `gap` → 多颗按钮贴在一起。实测材质=无 时描边 `1px solid
    rgba(255,255,255,.06)`、行底 `rgb(36,38,46)` 对页面 `rgba(0,0,0,0)`。
  - **「移除记录」在三个出口里选**：取消 / 仅移除记录 / 删除文件并移除。没复用 `ui.confirm`，
    它只有两个按钮，问不出"要不要连文件一起删"。失败任务（`path` 为空）时第三颗是 `disabled`
    并写明"这个任务还没有下载出文件"，而不是点下去什么都不发生。
  - **abort → 删记录 → 落盘 必须在主进程一个动作里做完**，所以 IPC 只收 `{id, deleteFile}`：
    拆成渲染层先 cancel 再 remove 的话，两次调用之间任务可能刚好跑完并发布文件，于是记录没了、
    文件还在，成一个没人指向的孤儿。**路径只从主进程自己那份 task 记录取**。删除一律走
    `shell.trashItem`，回收站不收时明确报错并且**不移除记录**（不静默失败、不退化成永久删除）。
  - **只删这次下载产生的那份 `.lrc`**：sidecar 是不覆盖语义，旁边完全可能放着用户自己写的同名
    文件。所以 `DownloadTask` 新增 `lyricPath`，只在 `exportAssets` 真的写出时记；移除前还会
    校验它确实形如本条 `path` 的同名 `.lrc`，防止 `downloads.json` 被改过之后指向别处。
  - 应用自己的 `.jj-<id>.part` / 暂存 / `-tagged` 在移除时总清掉（记录没了就没人再来续传）；
    用户的音频文件只在勾选删除时才进回收站。
  - 验收：新增 6 条单测（`trash` 是**记录而不执行**的假动作，所以任何一条跑完磁盘上不会少东西），
    `.cache/download-remove-check.mjs` **26 项全绿 / 2 项 SKIP** —— 含"仅移除记录后文件仍在"、
    "两个文件都出现在回收站里"（用 `Shell.Application` 枚举；光看 `existsSync=false` 分不清
    "进回收站"和"永久删除"）、"删不动时报错且记录仍在"、"目录从没建过时点一下它被建出来"。
    两条 SKIP 是应用层起的那发真下载没等到"下载中"那一帧（`wy`/`tx` 这一轮整家返 500），
    同一件事由冒烟里那发排队五首的下载量到了（上面那组 `h:2 / w:1002 / 行高 72`）。
    全程跑在独立 `--user-data-dir` 与独立下载目录上，没碰用户真实的 `D:\Download\JJ Music`。
  - **冒烟的下载段顺手改结实了**（这一段在 `--downloads` 开关后面，平时跑冒烟根本不执行它）：
    ①`public playlist preview includes songs` 的选择器 `.preview li` **结构上就不可能命中** ——
    预览里那份列表是虚拟化的 `TrackList`，行是 `.track-row`。它旁边那条"导入进侧栏与详情页"
    一直是 PASS 的（用的正是同一份 200 首数据），红的一直是断言自己。
    ②以前取样本是一次裸 `search('tx')`，平台一抖整条冒烟就死在 reject 上、后面几十项全部没跑
    （"红得无声无息"）；现在挨家试 tx/wy/kw/mg、一家一次 `evaluate`（把四次重试塞进同一个页面
    表达式那次，直接撞爆冒烟自己的 evaluate 超时），一次排五首保证进页面时确实还有活在跑，
    拿不到样本时三项明确报 SKIP。③进度线那条要"下载中"那一帧，单首 128k 常在渲染出来之前就
    下完，所以排多首；抓不到就 SKIP。④锁定态那条把 `hover===true` 焊在断言里，CDP 的
    `mouseMoved` 偶尔进不了悬停态，那种情况现在与它上面那条一样明确报 SKIP。
- **全局搜索去掉翻页器，改成滚到底自动加载 + 底部「加载更多」兜底**：`<input>` 下面那行 `上一页 1 / 94 下一页` 没了；结果列表累积追加，滚到底自动要下一页。
  - **按钮留着不是装饰**：纯无限滚动在无障碍上是有已知缺陷的模式（后退回不去、键盘难以触达后续内容、缺"正在加载/还有多少"的反馈），业界对"搜索这种要定位特定条目"的场景建议显式加载。所以自动加载与那颗按钮是同一条 `loadMore()` 的两个触发器，加载中那行带 `role="status"`。
  - **跨页去重按 `track.id`**，规则抽成 `src/renderer/src/utils/search-merge.ts` 好让"整页都是重复"那一支能被单测钉住（线上数据构造不出那个条件）；那一支同时是"停止自动请求"的保险，没有它哨兵会一直可见、反复请求同一页，变成一个安静的无限循环。
  - **删掉了 `shownLocal` 里那条 `page > 1` 就不置顶本地**的老规则：那是翻页时代"每一页都把整批本地结果重新钉一遍"的产物，改成累积列表后它会把已经答过的问题答错（同一首在线歌第 1 页是 01、第 2 页变 15），注释里写明了为什么现在是反的。
  - **这一轮最大的收获是探针抓出来的一个真 bug**：`TrackList` 的触底哨兵挂在 `onMounted` 里创建，而哨兵本身是 `v-if="tracks.length"` —— 搜索页挂载时列表是空的，`onMounted` 看到 `null` 就再也没回来。结果**滚动加载从头到尾一次都没生效过**：滚到 100 条底部，`fetchCount` 纹丝不动。改成对模板 ref 建立 `watch`（元素出现即接、消失即断）后才有第一轮真数据。
  - 验收 `.cache/search-scroll-check.mjs` **10 项全绿**。为了量得准补了三件事：①"到底不再发请求"只能靠**计数**证明（一页失败或全是重复时 `page` 和条数都不动，从外面看和"根本没发"一模一样），所以 `fetchPage` 里加了 `fetchCount`，通过 `VITE_E2E` 钩子暴露；②`allPage` 是五家平台取最大值，实测热门词一律 148~180 页、首页固定 100 条，**在「全部」页签下"滚到 allPage"永远不可能发生** —— 上一轮那条断言实际从没被执行过，改成另挑"单平台页签 + 生僻歌名"（网易云 ×《爷爷的故乡》= 4 页）才真的滚到了底；③主词改成按曲库本地命中数现场挑（拿《茶汤》时本地是 0 条，"顶部本地结果不能消失"那条反向对照就成了 `0 === 0` 的空测）。
  - 三个探针自身的坑，都记在文件里：`key-press.ps1` 用了 `[ushort]`（PS 5.1 里没有这个类型加速器，按键从来没发出去过）；键盘 `INPUT` 结构体必须凑满 `sizeof(INPUT)=40`（union 按最大的 `MOUSEINPUT` 算），写 32 的话 `SendInput` 返回 `sent=0` + 错误 87，看起来和"应用没响应"一模一样；窗口被最小化时 Windows 把它摆到 `(-32000,-32000)`，由 `window.screenX` 算出的坐标全部飞出屏幕，所以每次真实输入前都要按 PID 把它置回前台并校验坐标在屏内。
  - `.cache/verify-round20.mjs` 里依赖 `.pager` 的四处同步更新：那个选择器已经不可能匹配，留着它是一个不会红也不会对的死条件；而"翻到第 2 页之后本地不再置顶"这条断言的**前提整个变了**（列表只有一份、没有"第 2 页那个视图"），期望值反过来改成"加载更多之后本地仍然置顶"。同一趟顺手改正了 H 轮遗留的"搜索框与标题在同一行"。


- **全局搜索页在你输入之前会给你东西**：新增两行——**搜索历史**（最近搜过的词，点一下就搜，右键可单条删除，标签旁边一个小垃圾桶一键清空）与**热门搜索词**（问平台"大家在搜什么"）。两者在新增的 设置 → 搜索 里各有一个开关。
  - 热搜按 lx-music-desktop 那份端点清单接了**四家**：QQ `gethotkey.fcg`、网易云热搜榜 `chart/detail`（id 是 `HOT_SEARCH_SONG#@#`，要百分号编码，不然 `#` 会被当成片段发不出去）、酷我 `hotword.kuwo.cn`、酷狗 `gateway.kugou.com`（它一次给九个榜各 30 条，只取「热搜榜」）。之前只有两家，是因为我当时试的是**搜索适配器已经在用的那几台主机**——酷狗的搜索主机连不上、酷我的 `mobile.kuwo.cn` 返回 `Hit=0`——那是端点选错了，不是这两家没有热搜。咪咕确实没有公开端点，那一家的页签里整行不出现，而不是转圈或显示"暂无"。
  - 一个平台的词最多显示 20 条，「全部」页签把四家**轮流**并起来（去重后上限 48）；来源不再写在胶囊里，鼠标悬停时才通过 `title` 显示。汇总行**不等最慢的那家**：每家最多让 3 秒，QQ 那种偶发挂起（本机实测同一 URL 一次 500、下一次 30 条）就不会把已经答话的三家一起堵成转圈，被放弃的那家结果照样落进缓存，下次切页签就有它。
  - 请求走逐跳 SSRF 守卫，并把允许的主机名钉死在自己那一家；返回里的词只用于显示与填进搜索框，绝不会被抓取。酷狗与酷我的移动端接口要带自己的设备头（`dfid/mid/x-router`、安卓 UA），少一个就直接不认。
  - 历史存在设置里（`searchHistory`，上限 12 条，最近在前、去重），跟 `recentPlayed` 同一类数据。**关掉「记录搜索历史」会把已存的也清掉**，而且这条规则做在主进程：渲染层手里那份设置可能比主进程旧，只在前端判会漏。
- **顶栏的放大镜和全局搜索页现在是同一个功能**：联想逻辑与下拉行抽成 `use-search-suggest` + `SearchSuggest`，两处共用同一套索引（拼音、首字母、歌手、专辑）、同一套上下键与高亮模型。之前顶栏只是个窄输入框，输入 `zjl` 什么也不出，回车才跳——两处长得一样、能力差一截，正是"两个搜索框"的观感来源。下拉挂在胶囊外面的定位层上（胶囊自身的 `overflow:hidden` 是宽度动画需要的，不能动）。
- **发现音乐的统计卡：专辑换成艺术家**。1242 张专辑里大半是"每首歌各一张"，看了得点进去才知道有没有内容；472 位艺术家才是能直接感知的规模。无损数没有丢，挪到第一张卡的副行（`3 个文件夹 · 约 237 小时 · 3336 首无损`），专辑本身仍在侧栏。
- **在线歌现在默认记进内置的「默认列表」，队列走这个列表**：双击搜索出来的**在线**曲，先把这首记进「默认列表」（`id=default`，不新建列表），然后把**整个默认列表当队列**、从刚点的那首开始播。以前在线歌的队列就是当前页的搜索结果，翻一页、搜下一个词就断了，"在线听过的东西有个地方收着"这件事没人承担。
  - **同一页两行队列来源不同，这是决定不是疏漏**，注释写在 `playAt` 上：双击在线曲 → 队列 = 默认列表全部；双击**本地**曲 → 完全维持原样（队列 = 当前页 results），本地曲整库本来就常驻，塞进默认列表只会让它变成曲库的副本；「播放全部」→ 队列 = 当前页 results（本地+在线混排），**不写默认列表**，那是"这一屏都听一遍"不是"收藏起来"。判据是**被点的这一首是不是在线的**（`isLocalTrack`），不是当前页签——`全部` 页签两样混排，按页签判一定错。
  - 写入时机选在**开始播之前**。除了"用户表达了要听"这条软理由，硬理由是在线播放的失败经常是**延后**的（解析降级、逐家重试、`waiting` 挂起），`playQueue` 返回时并不等于没播成；按"播成才记"实现，回滚会把最终真的播出来的歌从列表里删掉。所以这个列表的语义是"我点过要听的在线歌"，不是"我听完成的歌"。实测里就出现了这一支：双击后队列建好、`playing=false`（那次在线没播起来），歌仍留在默认列表里。重复双击由 `addTracks` 按 id 去重兜住（实测 2 首点三遍仍是 2 首）。
  - 写入失败时**不退回去播默认列表的第一首**——那听起来像"双击放错了歌"。找不到刚点那首就只播这一首并提示。
  - 搜索页新增「播放全部」，位置与曲库页/流派页一致（视图标题块右侧、同一颗 `btn--primary`），没有结果时整块不出现。
  - 几百首队列的实测：队列 974 首时播放队列页 DOM 里只有 18 行（虚拟化生效），点「定位到正在播放的曲目」从 scrollTop 0 滚到 35773 并把第 500 行送进视口；`lastSession` 存下整条 974 首队列是 611 KB，清空后 `restoreSession` 把队列与当前位置都取回来。默认列表会长这个后果用户已接受，未做上限。
  - 验收 `.cache/default-list-check.mjs` **11 项全绿**，双击走真实输入（新增 `.cache/dblclick-at.ps1`，SendInput 连发两下）。**数据边界**：全程只跑 `.cache/ui-preview-profile`，开跑前备份它的 `playlists.json`、跑完还原（sha `eb1c9043…` → 测得 `9c7b1a74…` → 已还原 `eb1c9043…`）；用户自己的 `%APPDATA%/jj-music/playlists.json` 前后 sha256 与 mtime 一字未动（`30deccc9eaa2c5de` / 299092 字节 / 777 首 / default 仍 0 首）。
- **清空搜索框真的回到起始态**：`<input type="search">` 那颗原生 × 只派发 `input` 不派发 `submit`，所以以前它只让提示行回来，而列表、计数行（"本地 0 首 · 在线 2809 条"）、分页器（"1 / 94"）都还挂在屏幕上——页面同时处于"我该搜什么"和"这是上次的答案"两种状态。这段意图本来就在注释里写着，以前只实现了一半。
  - 清空时把 `submittedQuery / searched / onlineResults / total / allPage / page / searchError` 一并归位，并 `generation++` 作废在飞的在线请求（不然一次已经不该存在的答案会落回空输入框下面）。
  - **刻意不调用 `runSearch()`**：那会走 `syncUrl('')` 把地址栏的 `?q` 删掉。`?q` 是「返回」与侧栏记忆能回到这批结果的依据，所以留下一个看着别扭但正确的组合：**画面已回到起始态，地址栏仍写着上次那个词**。守住这件事的保险是 `watch(() => route.query.q)` 只在值变化时跑、而清空不动 URL——冒烟里直接断言 `route.query.q` 清空后仍是原词，把这条钉住。那道 `keyword.trim() === submittedQuery` 的 guard **不能当兜底**：清空把 `submittedQuery` 归位成 ''，一旦哪条路径真让 `route.query.q` 变了，它会照样重跑把结果拉回来。
  - 同类不一致顺手补了一处、刻意没补一处：**改了字没按回车**时列表还是旧词的答案，现在计数行末尾挂一句"关键词已改动，按回车搜索"（不另起一行）。没有改成"一改字就清列表"——那等于一边打字一边把结果拆掉，用户想对比两个相近的词就回不去了。切页签本来就重跑搜索，无需改。
  - 验收：冒烟新增 4 条（清空前确有结果 / 清空后列表与计数行收起 / `?q` 未被删 / 清空后重新搜索正常）。其中"分页器收起"在本地页签上是**空测**（那一页根本没有分页器），所以另加了"在真的会分页的在线结果上量"一条，拿不到分页就 SKIP 不用不可能失败的断言冒充验过——这一轮在线确实没分出页，那条如实报 SKIP。
- **导入歌单的预览头部压扁**：那块（封面 + 歌单名 + 三行计数 + 独立提示行 + 按钮）实测约 140px 高，把 116 首的列表挤得只看得见 2 行。现在封面 40px、计数与提示并成一行、按钮与标题垂直居中，`h2`/`p` 的默认外边距清掉，整块 40px；同一窗口下列表实测能看见约 4.9 行。
- **网络抓来的东西改成"抓一次、留着用"**：热门搜索词与艺术家头像以前都会因为进出页面、重启应用而重复问平台。
  - **热搜按自然日缓存**：每家平台每天最多问一次，结果写进 `library/hot-words.json`，所以"今天问过了"这件事也跨重启成立。跨过零点那一次不是"转圈等新词"，而是**先把昨天的显示出来、后台换成今天的**（`stale-while-revalidate` 那套形状，CDN 用了多年）；取不到就继续用旧的，失败只记一次半小时退避，绝不把今天写成"这家没有热搜"。以前是内存里十分钟、重启就忘。
  - **头像按歌手名记账，扫描时补查一次**：`artist-images.json` 里命中与没命中都算"查过了"，之后除了你点「更新网络头像」就不会再问。新加进来一轮扫描的名字由主进程后台补查（最多两个并发、每轮最多 120 个名字，不给平台来一场几百个名字的突袭）。艺术家页现在先用一次批量预读把已知的头像一次性画出来，不再一张张卡各问一遍——以前那一遍虽然没走网络，但每张卡一次 IPC、晚一帧才出现，看着就像"头像在反复刷新"。
  - 移除歌曲**不删**这份记录（它按名字存），所以"移除后重新导入"首帧就有头像；而清空封面缓存会让记录指向的文件消失，那时按"没查过"处理，会重新去问——这两条边界都有单测钉住。
  - 一个测试自己踩出来的坑：`tools/build-test.mjs` 是把依赖**打进每个测试 bundle** 的，所以测试里 `import { flushJsonWrites }` 拿到的是另一份副本、另一张 `pendingWrites` 表，等不到被测模块那侧的写盘。断言改成轮询文件本身，并把这个原因写在注释里，免得下次又用错。
- **数据不再固定住在 C 盘**（需求 2）：新增 `src/main/data-location.ts`，把"数据放哪"从一个写死的 `app.getPath('userData')` 变成一条有优先级的规则——命令行 `--user-data-dir` > 用户迁移过的记录（exe 旁 `data-location.json`）> **exe 同级的 `data/`**（便携版，或程序目录可写的安装版）> `%APPDATA%/jj-music`。便携版首次启动会把旧目录**复制**过来（原目录保留；`Cache`/`GPUCache`/`Local Storage` 等可再生数据不复制，免得为一次搬家先卡住几秒）。设置 → App 数据 现在显示当前目录与判定来源，并提供「迁移到其它目录」：选择框与复制都在主进程完成（这条路径决定应用所有文件往哪写，不能由渲染层报一个路径过来），迁移后需重启生效。因为 `userData` 是在 `app.ready` 之前定的，Chromium 自己的缓存也一起跟着走。
  - 边界：装在 `%LOCALAPPDATA%\Programs` 的安装版默认仍在 C 盘（那个目录本身就在 C 盘），要靠「迁移到其它目录」或改用 ZIP 便携版解决。开发态绝不写到 `node_modules/electron/dist` 旁边——这条有单测钉住，把 `packaged` 判定去掉测试立刻转红。
- **曲库支持移除歌曲**（需求 12）：曲目菜单新增「从曲库移除」，支持多选批量。**只删索引条目，绝不动磁盘上的文件**——按确认过的语义做：移除后重新扫描所在文件夹就会再收录，因此不需要一份"我到底隐藏了哪些歌"的名单要维护。有一个必须知道的边界：按单个文件导入、或文件已不在任何注册文件夹里的曲目，移除后重扫**不会**回来，只能重新导入该文件（第一次跑验证就撞上这个，看着像 bug，其实是设计）。确认框里写明了"音乐文件保留在原位置"。
- **歌单封面**（需求 8）：`Playlist` 增加 `coverPath`，图片经主进程选择框挑完后按内容哈希存进与曲库封面同一个目录，于是 `jjmedia://` 原有的白名单直接能用，不必为歌单再开一条口子。详情页与歌单网格都会显示，没有封面时仍是原来的音符图标；支持更换与移除。
- **搜索支持拼音**（需求 11）：本地曲库索引为每首曲目预存全拼与首字母，`yydgx` 能找到《爷爷的故乡》，`yeyedeguxiang` 也可以；艺术家字段同样参与匹配，所以 `zjl` 能列出周杰伦的歌。搜索框同时新增输入联想下拉（本地曲库，最多 8 条，上下键与点击都能选）。实现上放弃了 `Intl.Collator('zh-u-co-pinyin')` 方案——实测它把"晴天"算成 `rv`、"偏爱"算成 `qa`，不可靠；改为打包 `pinyin-pro` 的词典（devDependency，被 Vite 打进渲染层，不是运行时依赖，因此不影响 asarUnpack 与安装包）。代价是渲染层主包 523 KB → 956 KB。
- **音源支持从网址导入**（需求 14）：粘贴一个 http(s) 链接即可拉取脚本。抓取走既有的逐跳 SSRF 防护（`safeFetchBytes`：每一跳重新校验、拒绝内网与云元数据地址、4MB 上限），落库后仍默认停用并照常过启动前校验。顺带把原先在"粘贴 / 选文件 / 拖入"三处各写一遍的导入校验收敛成一个 `importScript()`，避免哪天只改了一处的闸门。
- **下载支持断点续传**（需求 4）：中断的下载不再从 0 重来。`.part` 文件在失败/取消后保留（成功落盘才删），重试时带 `Range: bytes=<已写字节>-` 续传。关键是**不能只相信服务器会理 Range**：用 `If-Range` 附上首次的 `ETag`/`Last-Modified`，只有 `206` 才当续传处理，返回 `200`（忽略 Range 的服务器）就截断文件重下——否则两份内容拼在一起是个"能播放但开头多了一截"的坏文件。续传时 `content-length` 只算剩余部分，进度总长改从 `content-range` 的尾巴取。此外新增完成度校验：字节数不足 `total` 时不再谎报完成，而是提示「下载未完成（x/y），重试将从断点继续」。上次退出时中断的任务，重启后的提示语也改成同样的意思。
- **网络导入支持咪咕，并同步封面、导入前可编辑**（需求 9）：
  - 咪咕走它自己的公开端点（歌单头 `resource/playlist/v2.0` 出名称与封面，`.../playlist/song/v2.0` 按 50 首一页翻到 `totalCount`），主机名是 `app.c.nf.migu.cn` 而非搜索用的 `app.u.nf.migu.cn`。歌曲行的 schema 与搜索完全一致，所以直接复用 `miguSongToInfo`，音质梯度、`copyrightId`、歌词直链一次到位，不再第二处猜字段。把写错主机名这件事钉在单测里（逐条断言请求 URL 的 host，改掉就红）。
  - 五家平台的封面都抓：网易云 `coverUrl`、QQ `imgurl`、酷狗 `{size}` 模板（填成 600）、酷我与咪咕的裸路径（补各自的图床域名），规范化后随预览一起返回，保存时经逐跳 SSRF 守卫下载、按 `content-type` 而不是 URL 后缀定格式、以内容哈希存进与曲目封面同一目录，于是 `jjmedia://` 白名单原样可用。抓封面失败只提示「封面获取失败」，不会让一百多首歌跟着丢。
  - 预览区从只读的 `<ol>` 换成共享的歌曲列表：可拖动排序、勾选后批量「从待导入列表移除」、点「恢复全部」找回。保存时渲染层只提交曲目 id 的顺序与子集，歌名/歌手/音质仍以主进程缓存的预览为准——未知 id 与重复 id 直接丢弃，所以伪造或过期的列表变不出新歌。
- **艺术家用网络头像，不再拿专辑封面顶替**（需求 10）：新增 `src/main/online/artist-image.ts`，按曲库里的歌手名去平台找头像。走的是"歌曲搜索行里本来就带的歌手标识"，不需要额外的搜索端点：咪咕行里有 `singerList[].img`，QQ 行里有 `singer[].mid`（拼成公开的 `T001R300x300M000<mid>.jpg`），网易云要先拿 `artists[].id` 再问一次 `api/artist/{id}`（它的不带后缀 `picUrl` 是 1.4MB 原图，所以强制加 `?param=320y320`），酷狗要 `SingerId` → `singer/info` 填 `{size}` 模板。酷我两条路都不通（搜索行不给艺术家图，艺人接口要签名），不参与。四家各自 `try`：本机实测 QQ 会连接超时，共用一个 signal 时它会替所有人把结果变成"没有头像"。抓回来的图经逐跳 SSRF 守卫下载、按内容哈希存进封面目录，于是渲染层看到的是 `jjmedia://` 而不是平台直链——离线可用，也不受各家防盗链规则影响。
  - 取用是懒的：艺术家卡进入视口才去查（`IntersectionObserver`），同时最多两桩，未命中的名字连同"确实没有"这个结论一起记进 `artist-images.json`，下次进这页一条请求都不发。实测本机 471 位艺术家：首屏只查 7 位、拿到 18 张头像，滚到底变成 33 位，重进这页不再新增请求。
  - 边界：没查到头像时仍显示该歌手某张专辑封面（而不是留白），因为本地曲库里有大量这种名字——现场录音、拼写奇怪的翻唱者、`G.E.M.邓紫棋` 这类带前后缀的标签。要 stricter 的话说一声，去掉这层回退只是一行。
- **在线歌词可以在几种来源之间切换**（需求 3）：以前在线播放的歌词只有一条固定链路（先问音源脚本，空了就问该平台接口），现在把它变成三处可选：`音源脚本` / `平台接口` / `联网匹配其他平台`（第三种是新增的：拿歌名与歌手去其余平台搜索，过 0.6 的置信度才采用，并排除曲目自己的平台）。设置·歌词 里选首选来源，并可关掉"其他来源兜底"——关掉就真的只问那一家，拿不到词就是拿不到，这一点有实测钉住。播放页 歌词 菜单还能只对**当前这首**指定来源（切歌即失效），因为"这句词不对"和"我永久改主意"是两件事。徽标不再只写「在线」，而是写「在线 · 平台接口」这样的具体来源：要知道该换成哪个，得先知道现在用的是哪个。下载时嵌词走的是同一条解析入口，所以设置一改，两边一起变。
- **封面与歌词现在记的是"在哪来的"，不是"有没有"**：索引里原先存三个字段描述同一件事——`lyricPath`（字符串，可能是空）、`hasEmbeddedLyric`、`hasSyncedLyric`。它们能回答"这首歌有没有词"，回答不了"这条词是从哪来的、下次该先找谁"。而封面和歌词恰好各有五种来源：音频文件自己的标签（内嵌）、同目录的 `.lrc`（sidecar）、应用抓下来存在数据目录的副本（缓存）、平台接口直接给的地址（远程）、用户手工挑的那一份（用户）。改成一张 `assets` 表：每首曲目按 `cover` 与 `lyrics.main / lyrics.translation / lyrics.pronunciation`（三个槽位对齐 OpenSubsonic 的主词/译词/发音词）各记一条**有序链**，链上每一环写明 `origin`、来源平台、是否带时间轴、记录时间。
  - 于是解析链、界面标签与后续的"导出到文件"用的是同一份数据，不再各推各的：设置·歌词里选的首选来源会真正排进链里；音轨信息新增「封面来源」「歌词来源」两行，播放页徽标把这条记录念出来（「同目录文件 → 文件内嵌（带时间轴）」这样的链，而不只是一个对勾）。
  - 这一步**只改记录与读取，不改任何写盘行为**：标签写入仍然只在用户明确点「写入标签」时发生，歌词仍然优先落在同目录 `.lrc`。
  - `INDEX_VERSION` 3 → 4，因此首次启动会自动在后台重读一遍曲库补上这张表（重读只跳过大小与修改时间都没变且**字段齐全**的条目；不需要用户点按钮）。实测 3368 首：升版后曲目一条不少，3336 首记着封面来源、3327 首记着歌词链，153 首有同名 `.lrc` 的曲目 sidecar 全部排在链首，旧的三个字段零残留。
  - 一个自己踩出来的回归：最初想让解析链"完全按记录走"，结果弄坏了"扫描之后才放进来的 `.lrc` 也要赢"——sidecar 是索引之外的文件，增量扫描看不见它（内嵌标签则不同，改标签必然动到音频文件，扫描看得到）。所以 sidecar 这一环始终实时探测、记录只补充非常规文件名，测试里钉住了这条优先级。
- **写封面与歌词只剩一个动作，落点变成一条设置**（第 2 步）：以前四条路径各写各的——歌词编辑器写 `.lrc`、候选歌词写 `.lrc`、标签匹配走 `writeTags`、下载按自己那两个开关两头写。四处的"该写到哪里"是四次独立判断，于是同一个"保存歌词"从不同菜单项进去会留下不同的来源记录。现在都走 `asset-export.ts` 的 `exportAssets()`：目标为 `文件标签 / 同名文件 / 两者都写`（设置·标签与文件），返回体说清"写到了哪几处"，并把来源链交回索引。四条路径各自的语义仍然保留：
  - 自己挑的歌词（候选、编辑、选文件、拖进来 `.lrc`）**一定**带上同名文件这一处。解析优先级里同名文件赢，所以只写标签的话它可能被用户很久前放的旧 `.lrc` 盖住——看起来就是"点了没反应"。
  - 下载走暂存文件名，所以那一趟**禁止**写同名文件：否则 `.jj-<id>.lrc` 会长在临时目录里。歌词等到定名之后再按 `不覆盖已有` 落盘——自动抓的东西不能吃用户手写的 `.lrc`（Picard 对 `cover.jpg` 也是同一态度）。
  - 格式不能改写的（OGG、M4A、WAV……）自动退到同名文件，并且提醒里说清是为什么；只有标题/艺术家这类文本字段没有别处可去，于是它说"标题等文本字段未写入"而不是什么都不说。
  - 顺手清了四处对不上的地方：① `tag-writer.ts` 的文件头声明"FLAC / OGG 都实现了"，但 `canWriteTags` 只认 MP3/FLAC——OGG 的 Vorbis 注释装在 Ogg 页里，改一次要重切包并逐页重算 CRC，没做也不该嘴上说做了；② 扫描器现在**读**封面同名文件（`<歌名>.jpg` 优先，再 `cover/folder/front/albumcover/album`），没有内嵌封面的文件不再一律显示占位音符；③ MP3 写歌词时同时写 `USLT`，FLAC 同时写 `LYRICS` 与 `UNSYNCEDLYRICS`（Audacious、Poweramp 只认后者）；④ MP3 **不写 `SYLT`**：实测 `node-id3` 的 `update()` 对数组型帧是**追加**而不是替换（写第二次会留下两份时间轴帧，读到的还是旧的那份），而空数组也删不掉——LRC 文本本身就带 `[mm:ss]`，这个应用和多数播放器都能从纯文本里读时间轴，所以那个帧买不到任何东西，只会留下越写越长的标签和顺序错乱的旧词。
- **联网拿到的歌词先进"待写入"，用户点头才落盘**（第 3 步）：播放一首本地歌时联网补到的词，过去只是显示一下就丢了；现在它进 `<数据目录>/library/pending-assets.json`，播放页徽标与音轨信息都会写「在线获取·联网匹配其他平台（待写入）」，设置·标签与文件 有一行说清现在等着的是哪些、并给「全部写入 / 全部丢弃」，歌曲菜单（含多选批量）则能逐首写。为什么不直接写：播放一首歌不等于同意修改这首歌的文件。音乐标签软件几乎都停在"暂存—显式保存"这一步（Picard 未点保存不改文件、Kid3"直到 Save 命令前文件不变"、Yate 可回到上次保存），只有网易云/QQ 这类云端播放器会静默写盘，而那正是它们被诟病的地方。
  - 队列刻意**不进索引**：否则每播一首没有歌词的歌都要重写一次整份 `index.json`。徽标的「待写入」来自解析结果，界面要列表时直接问队列。
  - 新增设置「可改写的格式」（默认 MP3 + FLAC）。这个开关只能**收窄** `tag-writer` 真能写的集合，交集在写入时取——所以渲染层即使把 `.wav` 写进去也不可能让应用去改一个没有经过验证的容器；测试钉住了这条方向。
  - `INDEX_VERSION` 4 → 5：封面同名文件是新读出来的字段，老索引里不会有它，靠启动时那次后台重扫补齐。
- **丢弃过的同一条歌词，不会在下次播放时又回来**：以前「全部丢弃」只清队列，那首歌再播一次就重新联网、把**同一条**词再摆回「待写入」，看着就像丢弃没生效。现在丢弃时按曲目记下这条词的指纹（对文本做行尾与首尾空白归一后取 SHA-256，所以同一个词从中转换个写法仍算同一条），再找到一模一样的就不再提示；**找到不一样的照常给**，因为第一次很可能压根匹配错了歌。歌词本身不受影响——待写入队列管的是"要不要写进你的文件"，播放与显示照旧。记录与队列存在同一个 `pending-assets.json` 里（文件形状从裸数组换成 `{entries, dismissed}`，旧文件照样读），上限 2000 条按最旧先丢，是"别再重复提同一条"的缓存，不是"我否决过哪些歌"的名单。要找回来的办法是让它给出不同的文本（例如换歌词来源），或者直接进歌词编辑器。
  - 边界：`写入`（全部写入 / 歌曲菜单）不会记进这份名单——确认过的东西不该被当成否决过，否则以后文件被删掉时应用不会再提醒你可以补写。
- **每个栏目记住你上次看到的页面，回来时还在原来的位置**：以前切走再切回，专辑网格回到顶部、搜出来的结果清空、点开的专辑自动关回去——滚动位置、下钻、关键词全存在组件里，而组件一卸载就带走它们。新增 `src/renderer/src/composables/use-scroll-memory.ts` 一份会话内的记忆，侧栏每一条都按它走。
  - **键是完整路径**，`/albums` 与 `/albums?album=X` 因此算两个页面：在专辑里往下滚，退回网格时网格的位置不会被详情页的位置盖掉。这条也顺带把"下钻"变成真的页面（`use-drilldown` 早就把它写进 query 了），所以标题栏 返回 仍然是 详情 → 网格 一步，没有为记忆改动历史。
  - **不靠路由钩子**：`beforeRouteLeave` 会漏掉一半场景——下钻只是 query 变化，组件根本不卸载。所以改成记 `scroll` 事件（捕获阶段，`scroll` 不冒泡），并用 `MutationObserver` 盯着内容区：滚动条位置一旦记下来，等对应区域"出现"时就贴回去。
  - **恢复是会重试的**，因为给一个还没内容的容器设 `scrollTop` 会被静默夹回 0（列表是虚拟化的，行高随数据才长出来；搜索页更要等平台答话）。所以一次没贴上去就留在待恢复队列里，随 DOM 变化与每帧再试，贴住了或元素没了就收手——实测搜索页 380px 的位置在结果到达后的第 3 帧落位。
  - **恢复是按"第几次访问"记认领，不是按 DOM 节点**。专辑网格和它上面的详情页共用同一个 `.view` 元素，所以"这个节点恢复过了"这句话必须只对**那一次访问**成立：否则第一次滚过网格就把节点用掉，标题栏 返回 回到网格时它已经"认领过"，位置停在 0。反过来，同一次访问内部的后续 DOM 变化（用户自己滚动带来的重绘）绝不能再去动它，那等于跟用户抢方向盘。这一条做了变异验证：把认领改回按节点记，实测返回后 `scrollTop=0`（正确实现是 320）。
  - 侧栏链接因此指向"这个栏目上一次打开的完整路径"：读过某张专辑之后点 专辑，回到那张专辑而不是网格顶部。没带过 query 的栏目（歌曲、发现音乐）行为不变。全局搜索页现在会把自己提交的关键词写回 URL（`replace`，翻页与换页签不新增历史条目），所以这条对搜索结果同样成立；它原先只存在组件的 `keyword` 里，返回和侧栏都无从还原。搜索页的"哪个页签"仍走会话记忆而不是 URL：它改变的是答案的形状，不是你在哪个地方。
- **锁屏、音量飞出菜单和硬件播放键现在能控制 JJ Music**（系统级媒体控件 / Windows SMTC）：新增 `src/renderer/src/composables/use-media-session.ts`，把播放器的状态投影成一份系统媒体会话——歌名、歌手、专辑、封面、播放/暂停状态，外加系统给的时间轴（时长与当前位置，飞出菜单里那根进度条就是照它画的）；反方向接上 播放 / 暂停 / 上一首 / 下一首 / 停止 / 快退 / 快进。因为音频本来就从一个 `<audio>` 元素起经（`createMediaElementSource`），Chromium 会替应用把会话登进系统，不需要任何辅助进程：动手测过——本应用正在播放时，用 `GlobalSystemMediaTransportControlsSessionManager` 反查就能看见它，只是那时标题还是页面标题；填上 `navigator.mediaSession.metadata` 之后标题与歌手立刻换成曲目自己的。
  - 封面这块踩了两个"看着设了其实没有"的坑。① `setActionHandler('next', …)` 这类名字要按 Media Session 规范写，我们惯用的 `next` 会直接抛错，而**没注册的动作在系统里是灰的**：实测 `可下一首=False`，这就是"名字写错"唯一的可见证据。② 本地封面是 `jjmedia://` 地址，而 `fetch()` 被本页 CSP 的 `connect-src` 挡在外面（这条不该为一个小图标放宽：那个 scheme 是能读磁盘文件的通道）。改为用 `<img>`（`img-src` 允许）画进 canvas 再取 `blob:`，320px 重编码；顺手把 Windows 上那份从一整张原图缩到 31KB。CDN 直链没有 CORS 会弄脏 canvas，那种情况退回原地址交给系统去抓。实测 `封面引用=True 封面字节=31545`。
  - **故意没有接系统侧的拖动进度条**。同一个反查工具要求把播放头挪到 75s 和挪到 150s，页面收到的都是 `seekTime: 0`——数值在 Chromium 这一层就丢了；如果注册 `seekto`，等于让用户在菜单上拖一下就把歌退回开头。而不注册也**不会**让那根条消失（`可拖动=True` 是跟着媒体元素自己的 seekable 走的，实测撤掉 handler 后仍然为 True），但至少我们这边不会照着那个错的数字动手。
  - 时间轴不是每 200ms 推进度就发一次：`setPositionState` 之后系统会自己外推，所以只在"外推开始对不上"（偏差超过 1.5 秒）、暂停、换曲、快退/快进时补发一次，另加一个 5 秒心跳兜住卡住的情况。
  - 11 项实机检查全绿（`.cache/smtc-app-check.mjs`）：正反两个方向都量——标题/歌手/封面/状态/时长/动作可用性，以及从**进程外面**按 下一首 与 暂停 之后应用真的换了歌、真的停下来（`TrySkipNextAsync` 之后当前曲目从《天若有情》变成队列里的下一首）。
  - **实体媒体键：接上了，但要在这台机器上量对不容易。** 注入一次 `VK_MEDIA_PLAY_PAUSE` 之后逐项对照，3/3 通过——暂停→恢复、`VK_MEDIA_NEXT_TRACK` 换到队列下一首，而"别的会话有没有跟着变"这张对照表只有 `electron.exe Playing → Paused`，说明键确实落在我们身上。之所以要这张表：前一天同样的检查里我们一动不动，倒是**网易云从 Playing 变成 Paused**——网易云和酷狗装了全局低级键盘钩子，在 SMTC 分发之前就把键截走了，而 `GetCurrentSession()` 当时报的当前会话明明是我们。把那两个关掉之后才量得准。**结论：开着网易云或酷狗时，键盘上的播放键大概率还是它们的**，这不是本应用能决定的（飞出菜单与锁屏的按钮是直接对会话调 `TryPauseAsync`，不受那个钩子影响）。另外这类检查不带对照就会给出假的 PASS——第一版"再按一次又播起来了"在什么都没发生时也算通过。
  - 一个探针环境留下的坑，写下来免得下次误判：用调试器直接 `playTrack()` 时，媒体的时钟几乎不走（20 秒墙钟走 2 秒曲目时间），而现场新建一个 `AudioContext` 又报 `running`。所以凡是"从元素读回一个位置"的断言都不可信，检查因此改成断言"页面被问到没有、带的是什么参数"。
- **任务栏缩略图预览上多了第四颗按钮：喜爱**（`♥ ⏮ ⏸ ⏭`）。这颗心只能做在 thumbar 上，做不在系统媒体控件上——`SystemMediaTransportControls` 与 `PlaybackControls` 里没有 like/favorite 这个属性，Media Session 的动作表里也没有（顺带：`PlaybackControls` 有 `IsShuffleEnabled`/`IsRepeatEnabled`，所以以后想往那张卡上加随机/循环是通的，喜欢不通）。
  - 图标是 `make-icons.mjs` 里新画的 `heart()`：隐函数 `(x²+y²−1)³ = x²y³` 逐像素判定，**空心**用"往外探 2.6px 出界才算边"得到等宽描边，而不是把场值卡在一个常数上——那个场离中心越来越陡，按常数取阈值会画成"两瓣肥、尖端细"。用描边而不是 `-f/|∇f|` 的牛顿距离估计，是因为这个隐函数在 `(0,0)` 与两瓣交界 `(0,±1)` 处梯度为零，估计会炸出洞。两态用同一颗心的包围盒（实测都是 x5..26 y5..24），空心 150 像素、实心 316 像素。检查 10 项（`.cache/heart-render.mjs`，自己 inflate 自己解 PNG）：尺寸、纯白无半透明、两态包围盒一致、左右对称、顶部两瓣分开（菱形与圆点过不了这条）、底部收成 2px 尖、中线那一行空心是两框实心是一整条。生成完之后**原有五张 PNG 字节分毫未动**。
  - 语义上它是这一排里唯一"显示状态"而不是"显示动作"的按钮：实心=已经在「我喜欢的」里，动词写在 tooltip（`喜爱` / `取消喜爱`，与右键菜单同一套措辞）。Windows 的 `THUMBBUTTONFLAGS` 根本没有 checked/pressed 位，所以"图与提示词一起换"是系统给的唯一状态通道（微软 `ThumbBarUpdateButtons` 的示例也是这么做的）。
  - 状态照旧由渲染层推：`setTaskbarState` 的入参从 `{hasTrack, playing}` 扩成 `TaskbarState`（新增 `favorite`），命令联合类型 `TransportCommand` 加 `'favorite'`，两个类型都放进 `@shared/ipc.ts` 让三端共用；四处 `webContents.send(IPC.trayCommand, …)` 收进一个 `sendTransport(command)`，这样命令名写错是编译错误而不是"点了没反应"。收藏一翻就重建整组按钮，和当年"播放键卡在 ▶"是同一条经验。
  - 少见的失败模式，加了一道闸：`createFromPath` 读不到文件时**不抛错**，返回一张空图，而空 HBITMAP 会让 `ThumbBarAddButtons` 把**整组**按钮退掉——心形那张 PNG 万一没进包，会连原来三颗一起消失。所以图标为空就干脆不插这一颗（`heart.isEmpty() ? [] : […]`）。
  - **这一轮真正修掉的东西比加的那颗按钮更重要。** 加完之后在应用里悬停，卡片上**一颗按钮都没有**——连原来那三颗也没有。改前版本（把心形整段拿掉）逐像素比对只差 24 个像素，说明这不是新按钮弄坏的，而是一直就坏的：本应用的 thumbar **从来没有画出来过**。归因结论是一句可以记住的话——**窗口还没有任务栏按钮时把按钮组交给 Windows，这批里第一次"空"的交付会把这个窗口的缩略图工具栏永久废掉**，之后每一次 set 都"成功"、什么都不画、也不抛错。应用恰好每次都踩：渲染层那个 `immediate: true` 的 watch 在 `show: false` 阶段就推了一次 `{hasTrack:false}`，于是 `setThumbarButtons([])` 在窗口还没上过任务栏时先交了出去。修法：第一次 show 之前只记下状态不交付，show 时补交（`taskbarWindowReady` / `pendingTaskbarState`）。
  - 归因是在一个 30 行的最小 Electron 窗口里一次改一个变量做出来的（`.cache/thumbar-window.cjs`，模式 a..z，同一批 PNG、同一个 `setThumbarButtons`），判据全部来自截图。顺带量到三个**让整排按钮消失**的独立条件：① **显式传 `flags: ['enabled']`**（文档说这就是默认值，实测传了反而整排没了，所以**什么都别传**才对）；② **`backgroundColor: '#00000000'`**（窗口材质为 mica/acrylic 时应用正是这个值）；③ 上面那条"空数组早交"。而 `frame: false`、`show: false`、`setBackgroundMaterial()`、以及"窗口已可见时先 `[]` 再给四颗"这几项**都不是**原因，逐个排掉了。
  - 验收 15 项全绿（`.cache/thumbar-accept.mjs`，判据是截图里的方框与加权墨量，`.cache/row-boxes.mjs`）：四颗都在、心形在最左；点一下 → 「我喜欢的」里真的有这首，图标同时变实心；再点 → 翻回来；**不碰任务栏、只在应用里收藏，图标也变**（说明状态是推给 Windows 的，不是点击顺手改的）；暂停 → 第三颗从两竖杠换成三角，恢复又换回来（和当初"卡在 ▶"是同一条重建通道）；换一首已收藏的歌自动变实心、换一首没收藏的自动空心；最小化时四颗还在、点心也真的翻；隐藏到托盘期间改的收藏，恢复窗口后第一颗已经是实心。空心/实心的判据是方框内高出自身底色的加权墨量 3955 → 9133（2.31 倍，与源图 150/316 像素的 2.11 倍对得上）——**没用"中心点亮度"**，因为心形的几何中心正好是两瓣之间那道凹口，填满之后中心仍然暗（138 → 182），分不开。
  - 测量环境自己贡献了两个坑，记下来：这台机器的显示配置在会话中途从 3840×2160@200% 变成 2800×1272@100%，之前所有按固定像素写的判据同时失效（而且第二轮的两次"最小窗口"结果因此是废的）；100% 下 Windows 把 32px 图标缩到约 13px 再画，"数纯白像素"数不到东西（心形 2.5px 描边变成 1px）。判据因此改成先按**卡片下边缘**定位那一排、再切四个方框量相对墨量。另外 `AutomationElement` 在托管 UIA 客户端里**没有** `FromNativeWindowHandle`，`FromPoint` 会被一个全屏透明的 `CEF-OSC-WIDGET`（NVIDIA 覆盖层）吃掉，所以卡片内的按钮只能从像素读——不要指望无障碍接口。
- **桌面歌词从"一行飘在桌面上"变成一张悬停才浮出的卡片**：不悬停时仍然只有歌词条（body 保持全透明，不是灰块）；鼠标移上去才浮出上面那一排——封面 + 歌名/歌手 + 字号 + 上一首/播放暂停/下一首 + 锁定 + 关闭；歌词行本身做**逐字渐进高亮**。控件条不常驻是刻意的（LX Music 也是这么做）：常驻的那条灰带会一直盖住桌面内容。
  - **卡片长在歌词行上方，不是下方**，而且用 `visibility` 不是 `display` 收放。窗口是 `justify-content: flex-end` 底部对齐的，所以浮出卡片时歌词行的位置一动不动（实测顶边 95 → 95）。反过来（居中）就会在鼠标到达的瞬间把正在读的那行字顶下去几行，比没有控件更糟。
  - 窗口从 820×104 长到 **820×156**，这个 156 是量出来的不是估的：特大字号 + 显示翻译的最坏情况需要 153px（卡片 52 + 歌词 58 + 翻译 31 + 间距与内边距）。`src/test/desktop-lyric.test.mts` 里那两处写死的落点（`{x:2062,y:2528}`、`{x:600,y:976}`）都是"屏底减窗高"，跟着一起改成 2476 / 924，并在注释里写明它们为什么随窗口高度动。
  - **封面走的是"主窗口把图重编码成 `data:` 再推过去"，没有动悬浮窗的 CSP。** 那个窗口的 `img-src` 只有 `'self' data:`，而这是刻意的：它连 `window.jj` 都没有，就是为了不给它读本地文件的能力；加 `jjmedia:` 等于把这条能力从后门还回去，加 `blob:` 则允许主窗口把任意对象 URL 指给它。也不能直接抄 SMTC 那份 helper——它最后一步是 `URL.createObjectURL(blob)`，那是 `blob:` 地址，搬过来照样被拦。代价写在注释里：CDN 封面若没有 CORS 会污染 canvas，而这里**没有"退化成正址"这条路**（CSP 连 `https:` 都不收），所以那种情况就是不显示封面、显示占位音符。实测本地/已缓存封面 3.2~3.6 KB、`naturalWidth=96` 解得开。
  - 逐字高亮的做法是**同一行字画两遍，亮的那遍用 `clip-path: inset(0 N% 0 0)` 从右边裁**，而不是给每个字上色：前者能把分界停在字**中间**，那才是参照图里"完｜美吧"那种感觉。时间轴优先用文件自带的逐字标记（`LyricLine.words`，`[mm:ss.mmm]<offset,dur>` 那种增强歌词），没有就在整行时长内线性插值；两行之间是休止时**停在上一字末尾**，不提前把没唱的字点亮。这套算术抽成 `lyricLit()` 放进 `@shared/desktop-lyric`，配 7 条单测钉住边界（其中两条一开始是我自己算错了：把 2/7 写成 2/9、把"第二个字内部按比例"理解反了——测试当场把它俩判红，正是想要的效果）。
  - 推进由悬浮窗自己的 `requestAnimationFrame` 做，主窗口只在换行和每秒两次修正时推 payload。暂停时 `heldMs` 冻住，实测暂停 1.5 秒后 `--lit` 仍是 4.07%（不冻就会在没出声的时候继续爬）。
  - **锁定态下卡片不出现，这是实测之后定的。** 之前 `style.css` 的注释写着"锁定的窗口完全忽略鼠标，光标根本到不了它"——**这句是错的**：`setIgnoreMouseEvents(true, { forward: true })` 仍然把指针移动转给页面（实测锁定悬停 `mousemove=4`），但点击一个都收不到（`pointerdown=0`，真的穿透到下面）。所以"锁定后浮出一排点不动的按钮"是撒谎，而为了让按钮能点就得停止忽略鼠标，那恰好毁掉 锁定位置 唯一的目的。解锁回主窗口做——和今天右键菜单在锁定时同样点不到是一致的。注释已按实测改写。
  - 顺手修掉一个**只有做了卡片才会暴露的真 bug**：`setIgnoreMouseEvents` 翻转之后，页面再也收不到 `pointerenter`（Chromium 认为指针一直在里面，那次本该通知它的 enter 被 ignore 状态吞了）。结果是**从锁定切回来之后卡片永远弹不出来**，实测三次重新悬停都失败。改成 `pointermove` 也会浮出卡片（收起仍然只认 `pointerleave`——指针停在条上不动时卡片必须留着）。
  - 命令通路：卡片控件 → `window.desktopLyric.command` → 主进程 `request()`（校验过：只放行 `toggle-lock` / `toggle-translation` / `close` / `set-font`(限白名单字号) / `transport`(限三个动作)，`moved` 这种主进程自己发的消息一律拒绝；锁定时除解锁外全部丢弃）→ 转发给主窗口 → 播放走 `player`、偏好走 `library.updateSettings`。悬浮窗依旧不持有任何状态。
  - 验收：`.cache/lyric-card-check.mjs` **17 项全绿**（真实鼠标 + 页面矩形 + store 回读），含几何三项、悬停浮出与"文字不跳"三项、高亮三项、四个控件各一项、锁定态两项、关闭一项；另加 `.cache/lyric-wipe-pixels.mjs` **4 项像素验证**（同一行取两张截图按列做差：变了的列是行首的 30px 前缀，点亮比例 0.09→0.20 与之吻合，证明 clip 真的画出来了而不只是一个样式值在动）；再加 `.cache/lyric-drag-check.mjs` **4 项拖动回归**（真实按下—分步移动—抬起，页面位置每 40ms 轮询一次：命令位移 (320,−70) 与窗口位移完全相同、中途 25 次位置变化说明是连续跟随而不是最后跳一格；从卡片按钮起手横拖 150px，窗口只动 0px 且没有误触发锁定——按钮的点击没被拖动手势吃掉，拖动也没被按钮挡住）。`npm run verify` 24/24。
  - 冒烟脚本自己有个潜伏的坑被这次踩出来：它取"第一个 page 目标"，而桌面歌词窗口也是一个 page——**只要用户的设置里 `desktopLyric` 是开的**（这台机器就是），冒烟就会连到悬浮窗上，第一条 `document.querySelector('#app')` 直接判 null 崩掉。现在 `waitForTarget` 显式排除 `desktop-lyrics`。另外卡片出现后 `#line.textContent` 是两遍字，那条"悬浮窗显示的是当前这行"的断言改成读暗层副本，并新增一条"亮层与暗层必须同字"。
  - **`the strip is transparent, not a grey box` 这条没有变红，但只改期望值是不够的。** 它量的是 `body`，而卡片底板在子元素 `#card` 上，所以空闲态依然全透明、断言照过——问题反过来了：这条现在只说了故事的一半，**一张永远画不出底板的卡片也能让它绿**。于是补成一对：用 CDP 的 `Input.dispatchMouseEvent`（走渲染器真正的输入管线，会产出 DOM 指针事件，与 `contextmenu`、Ctrl+A 那两处合成事件不同）把卡片浮出来，再断言 `#card` 确实有一块 `rgba(18,20,28,.74)` 的深色底板、`body` **仍然**全透明、歌词行顶边 117 → 117 不动。悬停那一步若没生效，报 SKIP 而不是 PASS 或 FAIL，所以 `check()` 加了第三态（并且现在只有严格 `true` 算通过）。锁定时同样用指针移动去试，断言卡片不弹（`display:none`，且页面自己也不给 `is-hover`）。冒烟因此从 48 项变 **53 项全绿**。
  - 参考图重截：`docs/research/screenshots/salt-ui/14-desktop-lyrics.png` 现在拍的是**浮出卡片的悬停态**（封面 + 歌名/歌手 + A ⏮ ⏸ ⏭ 🔒 ✕，歌词行里能看到高亮停在"达"字中间），空闲那张另存为 `14a-desktop-lyrics-idle.png`。要说明的是这个目录本身在 `.gitignore` 里（`docs/research/screenshots/`），所以两张是本地参考图、不随提交走——文档里没有指向它们的链接，也就没有"图与实现不符"留在仓库里。
  - **交付验收（`.cache/lyric-accept-final.mjs`，22 项全绿）**：跑在**不带 `VITE_E2E` 的正式构建**上，store 从 `__vue_app__.$pinia._s` 取，所以量的就是准备交付的那个包（打印 `typeof window.__jj_player === 'undefined'` 自证）。每一次真实点击都回读"落在哪个元素上"（运行时挂 `pointerdown`/`click` 捕获钩子，不改构建产物），因为只看队列 index 分不清"没点中"和"没路由到"。六条各自的数字见交付说明。
  - 这一轮**先出的是四条红字，其中三条是探针自己的错**，记下来免得下次重犯：① 那台机器的 `playMode` 是 `random`，随机模式"下一首"是抽袋、index 本来就不连续，所以"点了没换歌"很可能是抽中了别的位置——验收前先把模式钉成 `list`；② `previous()` 的契约是**两段**的（播过 3 秒先回到本曲开头，不到 3 秒才真的退一首），我只断言了后一段，于是把设计行为判成缺陷，而第二次的 400ms 停顿加"读坐标 + 分步移鼠标"一共约 3.3 秒，又越过 3 秒门槛，改成 `seek(0)` 之后立刻按；③ 第一次报"封面是空串"是因为当时那首歌（《爷爷的故乡》）**根本没有封面文件**，那是设计内的降级——所以第 3 条改成有封面/没封面**各量一项**，外加"换回有封面的会重新画上"，只测一种的话两种坏法都看不出来。
  - **卡片排布按参照图改正：控件簇回到画面中部。** 参照图里五颗控件（A ⏮  ⏭ 🔒）是围着画面中线的一簇，✕ 单独留在最右；做完之后实际是**整簇挤在最右**（量过：A 在 75.4%、锁在 90.6%），中间空一大片。根因是 `.card` 用 flex 且文字块 `flex:1 1 auto` 吃掉了所有剩余空间，把控件顶到末端。改成三列网格 `minmax(0,1fr) auto minmax(0,1fr)`：左列放封面+文字，中列放控件（`justify-self:center`），右列放 ✕（`justify-self:end`）。两侧列同为 `1fr`，所以**中列恒在正中**，与窗口宽度、与歌名长短都无关。改后量到 42.2%–57.8%、中心 50.0%（参照图约 43%–60%、中心 51.4%）；特大字号下中心仍是 50.0%，拿曲库最长的那条 65 字歌名试也仍是 50.0%（`scrollWidth > clientWidth`，文字自己截断了）。`minmax(0,…)` 与 `.card__left{min-width:0}` 是"长标题截断而不是把按钮挤走"的两道闸，少一个就会被内容撑开。
  - **热区从整块窗口缩到可见内容。** 用户说"能触发抓取的范围比较大"——是真的：窗口固定 820×156 且从不随内容缩放，而悬停/拖动/右键四个监听器全挂在铺满窗口的 `#lyric` 上，所以卡片四周的透明边距、歌词两侧留白、卡片收起时上方那一整块空白**都能**弹卡片、也都能拖窗口。用户看到一行字，能抓的是一整块隐形矩形。改法取"缩小热区"而不是"窗口跟随内容"：`.lyric{pointer-events:none}`，`.card`/`.lyric__line`/`.lyric__sub` 各自 `pointer-events:auto`。容器不再是命中目标，但子元素的事件仍然冒泡到它，所以监听器一行都不用搬。`grab` 光标也从 `body` 挪到这几个盒子上——否则透明边距仍然显示"可以拖"。
    - 边界信号从 `pointerenter`/`pointerleave` 换成会冒泡的 `pointerover`/`pointerout`，并且 `pointerout` 只在 `relatedTarget` 已经不在悬浮条内时才收起卡片：从歌词行移到卡片上是一次 out 紧跟一次 over，把它当"离开"就会在用户正伸手的地方闪一下。`pointermove` 仍然参与浮出——那是"解锁之后 `pointerenter` 永不再到"那个实测 bug 的修法，不能因为换了信号就退回去。
    - 指针捕获改交到**真正被按到的那个元素**（`#lyric` 现在 `pointer-events:none`，捕获它没有意义），并且 `pointerup`/`pointercancel` 收在 `window` 上：热区缩小后，一次拖动的抬手可能整个发生在 `#lyric` 子树之外，收在条上就等不到，于是"松手之后歌词条还在追着鼠标跑"这个老缺陷会回来。
    - 反例约束有实测钉住：**卡片收起时按在歌词行上必须仍然拖得动**（命令 (−200,−70) DIP → 窗口精确 (−200,−70)，中途 25 次位置变化说明是连续跟随）。留白处按下拖动则窗口一格都不动。
    - 这个方向的代价说清楚：**它缩的是页面会响应的范围，不是窗口会遮挡的范围。** 留白里点一下仍然落在这个窗口上、因此什么都不发生，并没有穿透到下面的桌面。要治那一半只有两条路——窗口按内容量测尺寸，或者按指针位置动态切 `setIgnoreMouseEvents`——都比缩热区大，这次没做。
  - 这一轮**先坏在探针上，而且坏法是"假绿"而不是报错**：这台机器的缩放率在两次运行之间从 100% 变成 200%（`devicePixelRatio=2`），而 `SetCursorPos` 走物理像素、`screenX`/`getBoundingClientRect` 是 DIP。直接拿页面坐标喂鼠标会整段打空，打空在"不该弹"这类负断言上长得和"改对了"一模一样。四个探针（hotzone / accept-final / card-check / drag-check）统一加了**倍率定标**：拿"哪个倍率真能浮出卡片"当判据，定不上就整段 SKIP 或抛错，绝不让负断言白过。定标成功后同一批数字才可信。
  - 顺带清掉两处探针自己的过期假设：冒烟脚本的悬停点是写死的 `(410,130)`，热区一缩就打不到东西，改成运行时读歌词行中心；而它默认 profile 是未锁定的——上一次运行残留了 `desktopLyricLocked=true`，于是"悬停浮出卡片"长期 SKIP，看着像热区改动弄坏了悬停，其实压根没量到。现在这一段显式写 `desktopLyricLocked:false`。
  - 验收：`.cache/lyric-hotzone-check.mjs` **18 项全绿**（控件簇 6 项 + 最坏情况 2 项 + 热区 10 项），`.cache/lyric-accept-final.mjs` **23 项全绿**（含新增的"坐标系定标成功"前置项），`.cache/lyric-card-check.mjs` **18 项全绿**，`.cache/lyric-drag-check.mjs` **5 项全绿**，`npm run verify` 24/24，冒烟 0 失败。窗口尺寸没动，所以 `src/test/desktop-lyric.test.mts` 里那两处落点断言不需要重算。
- **锁定的桌面歌词现在能自己解开**：以前锁定后把鼠标划上去**什么都不出现**，解锁只能回主窗口点设置。用户的话是"锁定了在划过去没呼出解锁的图标，改不了"。这不是 bug，是当初实测后刻意定的规则（`setIgnoreMouseEvents(true,{forward:true})` 下页面收得到指针移动、收不到点击，所以弹一排点不动的按钮等于撒谎），但那条规则把"锁上之后再也解不开"也一起锁进去了。现在改成**只浮出一颗"解锁"**：整条工具栏在锁定时仍然不出现，出现的那一颗真的点得动。
  - 点得动靠的是把点击穿透**只在指针压在解锁入口的那一小段时间**里收回来：页面从 `pointermove` 上报"进/出这颗按钮"（不是 enter/leave —— 翻标志位恰恰是让边界事件不再到达的那个操作，本项目已实测过），主进程据此切 `setIgnoreMouseEvents`。主进程侧的通道本来就留着：`request()` 里 `toggle-lock` 早已被排除在锁定拦截之外，缺的一直只是"让点击真的落在按钮上"。这条命令不是能力授予 —— 悬浮窗本来就能发 `toggle-lock` 把穿透永久关掉，它要的只是临时开一下。
  - 反面守住了：**锁定时窗口其它地方仍然点击穿透**。判据不是读属性而是隔着一行字按主窗口的播放键 —— 播放状态翻了才叫还在穿透。压过解锁按钮再移回歌词行，穿透要恢复（`blur`/`pagehide` 也各兜一次，免得指针"消失"时窗口一直吞点击）。未解锁时这条命令被主进程直接丢弃，页面不能反过来把穿透打开。
  - 一个挡道的旧代码：`applyState` 里那句 `if (locked) remove('is-hover')`。它是"锁定什么都不给"那个前提的产物，而现在主窗口每秒推两次 payload，等于把刚浮起来的解锁入口在几百毫秒内又抹掉 —— 第一次实测就是这条让 `is-hover` 恒为 false。删掉之后卡片仍然靠 `body.is-locked .card{display:none}` 消失，两件事本就该分两层。
  - **一条假绿被抓住并改掉了**：探针原先用"矩形宽>8 且高>8"当"按钮可见"。它放过了一个真 bug —— `.tool` 在 CSS 里声明得更靠后，同特异度下把解锁按钮覆盖成 30×30 **透明底**：按钮在、位置对、点得动，但完全没有底板，在白桌面上就是个隐形方块。改成要求底色非透明且宽度装得下图标加"解锁"两个字，规则也改成 `.tool.unlock-bar__btn` 两类名才压得住。截图确认现在是一颗深色小药丸。
  - 三处编码了旧规则的断言是**改了判据**而不是改期望值：`card-check` 第 16 项以前读 `visibility`，那个代理只在"锁定时悬停不成立"的前提下有效，现在悬停成立、`.lyric.is-hover .card{visibility:visible}` 会命中，真正挡住卡片的是 `display`，所以改量 `display`；`accept-final` 4c 的 `hover===false` 翻成 `true`，但同时新增"卡片必须仍然不出现 + 解锁条必须可见"，否则那个"撒谎"的反对理由就又成立了；冒烟里那条同样补上后半句。
  - 验收：`.cache/lyric-unlock-check.mjs` **8 项全绿**（含"真实鼠标点下去、设置真的翻"这一条唯一算数的证法，和两轮隔窗按播放键的穿透对照），`lyric-card-check` 18/18、`lyric-accept-final` 23/23、`lyric-hotzone-check` 18/18、`lyric-drag-check` 5/5、`npm run verify` 24/24、冒烟 0 失败。截图 `.cache/accept/unlock-locked-hover.png`（锁定·悬停）、`unlock-locked-on-unlock-btn.png`（指针压上解锁）、`unlock-unlocked-hover.png`（解锁后完整工具栏）。


### 修复

- **播放页收起不再分两拍：封面搬到「视口锚定的飞行层」，与页面滑动同时开始、同时到位**。
  - 上一轮只把两段时长各自对齐到 420ms，用户实测的结论是**「动画看起来效果很割裂」**（串行 = 911ms 的两拍，参照是一次 420ms）。这次换机制，参数一个字没再动。
  - 为什么上一轮判为"做不到"：封面是那张正在滑动的纸的**子孙**，`transform` 会叠加 —— 并行的目标框必须减掉页面的行程，而补偿后的落点在页面顶边之上约 65px，正被 `.np { overflow: hidden }` 裁掉（`.cache/anim-t-check.mjs` 的 T7 量到的就是那 65px，落点帧封面自身只有 49px 高，等于整张已经没了）。
  - 新机制：`components/CoverFlightLayer.vue` —— `<Teleport to="body">` + `position: fixed` + `z-index: 51`（`.np` 是 50）+ `pointer-events: none`，自己承担圆角与倒影，**不继承任何 transform、也不在 `.np` 的裁切里**，于是整段飞行都在视口坐标系，裁切问题从根上消失。飞行期间 `.np__art` 用 **`visibility: hidden`**（不是 `opacity: 0`：那会让封面下面那片模糊倒影跟着淡，读起来是一次闪烁）；层在 `transitionend` 之后**先让调用方恢复真封面、下一帧自己才退场**，所以两者之间不存在空档帧。
  - **端点在开动之前就已知**：`artBox()` 拿 `getBoundingClientRect()` 再减去 overlay 自己的 `translateY`（`DOMMatrixReadOnly(...).m42`），面板还在 `translateY(100%)` 的时候终点就已经算出来 —— 所以 `onMounted` 里那 60ms 的等待**删掉了**（它存在的唯一理由就是"滑动没落定量不到盒子"）。⚠️ 两处刻意偏离了方案里写的做法，都在注释里说明：`transform-origin` 用默认的中心而不是 `0 0`（位移是按中心算的，origin 放左上角会让缩放偏出去）；终点用「视觉盒减平移」而不是沿 `offsetParent` 链累加 `offsetLeft/Top`（后者会把中间每一层的 border 与 margin 一起吃进来）。
  - 实测（同一支 `.cache/anim-t-check.mjs`，改成逐帧采层的盒 + 两处的 `visibility`，**17 项全绿**）：打开 —— 层第一帧 t=39ms 就落在 49px 的小图位置上（改前 248ms 才起飞），420ms 飞到 377px，起点/终点各自与目标盒差 ≤6px；收起 —— 滑动起于 57ms、元素在 463ms 被摘掉，**整程 ≈460ms**（改前 911ms），落点与底栏小图中心差 ≤6px，全程**没有任何一帧被裁**；交接 —— 「真封面被藏起来的每一帧层都在画」，空档帧数 0；`reduceMotion` 下两层都不出现、也不藏封面，直接只滑页面。
  - 连带简化：`App.vue` 的 `closeNowPlaying()` 不再 `await`（`playArtworkFlightBack()` 现在是同步发射），`closingNowPlaying` 那个连按保护随之删掉 —— 它挡的是"await 期间又按一次"，现在没有 await 了。`.np__art` 上的 `is-flying` / `is-flying-out` 两条 CSS 规则一起删除。
  - ⚠️ **并行之后紧接着暴露出第二个问题：用户说「收起有点快」**。根因不是时长，是**离场用错了曲线方向** —— 进出共用 `--ease-out`（`cubic-bezier(0.16,1,0.3,1)`，减速），减速曲线做离场就是**开头 8% 的时间走完一半路程**（实测旧值：起滑后 ≈35ms 页面就过半），剩下的 385ms 在屏幕外慢慢收，感觉就是"啪地没了"。改成 Material 对**离场且可能回来**的元素给的 sharp 曲线 `cubic-bezier(0.4,0,0.6,1)`（加速），并给离场单独一档时长 `--dur-overlay-out: 460ms`（进入保持 420ms 不变，参照实测就是它）。飞行层的曲线/时长跟着走（`FlightSpec.ease`），否则封面与页面同时开始却不同时刻到位。实测：**起滑后 205ms 才走完一半**（旧值 ≈35ms），整程 460ms；进入侧同一量是 150ms（减速，就该前半程快）。探针新增 T8b 把「半程时刻」钉成断言（150–330ms），T13 改判 0.46s。
- **播放页开 / 关的过渡按椒盐音乐 1.10.0 EA 的实测值校准：去掉淡入淡出，页面滑动提到 420ms**（S 轮之后用户逐条实测复核提的）。
  - 三处都在参数层：`App.vue` 的 `.slide-up` 删掉 `opacity` 过渡和 `enter-from/leave-to` 里的 `opacity: 0`（参照里页面**全程不透明**，是"盖住"不是"淡入"，半透明的头几帧会让底下的歌单闪出来）；新增 `--dur-overlay: 420ms` 而**不是**改 `--dur-slow` —— 后者还担着上百处小组件过渡，不能跟着拉长；`FLIGHT_OUT_MS` 300 → 420，`.np__art.is-flying-out` 的 CSS 时长跟着走（两边刻意写成同一个值，注释里点明了）。
  - 实测 `.cache/anim-t-check.mjs` **7/8**（独立 profile + 副本曲库；用页面内的 rAF 采样器逐帧记盒子和 computed style，不读 class —— CDP 往返 20ms 以上的抖动量不出 420ms）：打开 397ms 到位、整程 `opacity: 1`、底下的侧栏一帧不动（`48,0` 一个值）、封面 49px → 377px 且飞行期 transition 正是 `transform, border-radius / 0.42s, 0.42s`。
  - **红的正是第 8 条，也就是这一轮的结论：「收起时封面和页面同时走」在现有机制下做不到**。目标框本来就能一次量好（`flightBoxes()`），但封面是那张正在滑动的纸的**子孙**：要在滑走的 420ms 里同时落到小图上，飞行位移得减掉页面的行程，而补偿后的落点在页面顶边之上 65px —— 那里正被 `.np` 自己的 `overflow: hidden` 裁掉。实测从第 140ms 开始被吃，落点帧封面高 49px、裁掉 65px = 整张已经不在。要真并行得把飞的那颗封面搬到 overlay 之外（克隆 / Teleport 到 `body`），那是换机制不是调参数。
  - ⚠️ **代价得说清：串行两段现在各 420ms，收起整程实测 911ms**（椒盐是 420ms 一次走完，改动前是 300+320=620ms）。不接受这个长度的话两条出路：把封面搬出 overlay 做真并行，或让收起那一程单独回到短时长。
  - 顺带量到一处**本轮没动的旧偏差**：进入时封面要到第 248ms 才起飞（`onMounted` 里那 60ms 延迟，加上挂载首帧被全屏 `blur(72px)` 背景挡住了 184ms —— 帧时刻 64ms → 248ms 直接跳空），落位 539ms 而页面 377ms 就到了；参照里这两件事是同时起止的。
- **给正在播放的歌写标签不再失败**（用户报的截图：toast 里是 `EPERM: operation not permitted, rename '…flac.jjtmp' -> '…flac'`）。
  - 根因是**应用挡了它自己**：写标签的最后一步是把 `X.flac.jjtmp` 改名盖回原文件，而媒体协议是用 `createReadStream` 直接从这首歌读（`media-response.ts:131`），正在放的那首就有一个本进程的打开句柄挂在上面；Windows 不允许把一个正被打开的文件改名覆盖，于是报 EPERM。在 `.cache` 的副本上复现过：开着读流时 `rename` → EPERM 且临时文件残留，关掉句柄后同一个 `rename` → 成功。
  - 修法是写之前先松手：新增 `src/main/media/file-release.ts` 记着"哪条流正在服务哪颗文件"，`exportAssets` 在做内嵌写之前调 `releaseFileForWrite(path)` —— 关掉本进程的流，并通过新通道 `player:release-file` 请渲染层放下这首歌（不先停的话，播放器下一次 range 请求又会把文件打开一遍）。路径按 Windows 的写法比对（大小写与分隔符不同也是同一颗文件），否则会漏。
  - **只有当前加载的那首会被停下**：写别的歌的标签不该打断音乐（实测 T8：正在播《勇气》，写那首 MP3，播放继续）。
  - `rename` 失败时按 `EPERM/EBUSY/EACCES` 退避重试 4 次（150/300/450ms），挡杀毒与索引器那一瞬间的占用；**放弃时删掉 `.jjtmp`** —— 那是新标签本身，但留在用户音乐文件夹里就是每失败一次多一个 22MB 孤儿，而原文件两种情况下都没动过、`.bak` 也还在。
  - 文案换成能照着做的中文：「文件正被其他程序占用，无法替换（通常是这首歌正在播放）。已停止播放并重试仍未成功——请暂停或切到其他曲目后再试一次。」MP3 那条 `NodeID3.update` 返回 false 时原来只说「ID3 写入失败」，现在同样给出占用与下一步。
  - ⚠️ **只有"被占用"这一种失败改成返回提示，其他错误继续抛**：`src/test/downloads-import.test.mts:185` 钉的就是"结构不合法的 FLAC 必须 reject 且一个字节都不改"，下载路径靠这个 reject 保证坏文件不被静默改写。第一版我把整个 `writeTags` 包进 try/catch，那条测试立刻红了 —— 于是给锁定的错误打了个 `locked: true` 标记，只放行这一种。
  - 实测 `.cache/tag-lock-check.mjs` **12 项全绿**（写正在播的那首成功、副本 sha/mtime/尺寸都变、无 `.jjtmp` 残留、`.bak` 里是写之前的字节、索引重读后界面显示新标签、写另一首时播放不停）；单测在 `asset-export.test.mts` 新增一节 9 条，含"能写的 FLAC 写得进"这条正对照 —— 没有它，"失败了"什么也证明不了。
- **清空搜索框之后，侧栏「全局搜索」不再把上次的搜索又跑一遍**（用户报的 bug；这是 K 条那个决定的直接代价，用户看过取舍之后推翻了它）。
  - 因果链四段：`resetToStart()` 刻意不动 URL → `use-scroll-memory` 的 `afterEach` 把带词的 `fullPath` 记成 `/search` 的目的地 →
    侧栏那条 `RouterLink :to="lastPageOf('/search')"` 因此指向 `/search?q=…` → 重新挂载时 `onMounted` 那句 `if (keyword) runSearch()`
    把词填回去并搜一遍。修法只动一处：清空时 `syncUrl('')`，把 `?q` 一起删掉。
  - ⚠️ **顺序有真实依赖**：必须先把 `submittedQuery` 归位成 `''` 再动 URL。改 URL 会触发 `watch(() => route.query.q)`，
    它的早退条件正是 `keyword.trim() === submittedQuery`；反过来写时比的是 `''` 与旧词，watcher 当场重跑搜索，
    把刚清掉的结果又拉回来 —— 那就是症状本身换了一条路径重现。
  - **`use-scroll-memory` 一行没改**：修复是穿过它的既有逻辑生效的 —— 地址栏回到 `/search` 之后 `afterEach` 走删除分支，
    那份带词的记忆自己就没了。探针 P2a 读的就是侧栏链接的 `href`（`#/search`，不带 `q=`），机制看得见。
  - 「不清空离开、点侧栏要回到那批结果」这条能力保住了（P3：`href=#/search?q=…`、点回来 16 行结果在），顶栏放大镜那条路也没坏
    （P4：回车跳进搜索页并自动出结果）——**`onMounted` 那句自动搜索依赖 URL，删不得**，这次修的是"清空时把 URL 弄干净"。
  - **已知且被接受的代价**：清空之后点标题栏「返回」回不到那批结果了（P5 量了：清空前 16 行、返回后 0 行且 URL 是干净的）。
    那是用户自己拍板的取舍 —— 那批结果本来就是被主动叉掉的。
  - 冒烟里 `tools/probe/salt-ui-smoke.mjs` 那条断言**方向正好相反**（原来钉的是"`?q` 不许被删"），已反转成"清空后 `?q` 没了"。
    顺带修掉一个会假红/假绿的顺序问题：读 `?q` 之前那次清空现在**无条件**做一次，否则 `.load-more` 那一支 SKIP 时
    读到的是上一轮遗留的带词 URL。
  - 验收 `.cache/search-clear-check.mjs` **10 项全绿**（跑在独立 profile、`libraryFolders` 清空：不扫描真实曲库，
    免得启动期的"伪 FLAC 修复"去写用户的音乐文件）；截图在 `.cache/search-shots`，窗口顶部注入了当前 URL 好让地址栏状态可见。

- **桌面歌词"拖着拖着越来越偏"，而且拖动时帧率很低**：两个抱怨是同一个 bug。拖动循环里有一道保护——"单帧光标位移超过 120 DIP 就判定为混合 DPI 接缝造成的坐标跳变，丢掉这一帧"。它丢的方式是**把那一帧的位移直接作废**（同时把锚点重设到窗口当前这个已经落后的位置），所以每吃一帧就永久差一截，越甩越偏。实机量法（`.cache/lyric-follow-check.mjs` + 真实系统光标，CDP 的合成事件根本不会移动 `getCursorScreenPoint()` 读到的那个光标）：**修复前**，以每步 300px 的频率甩动歌词条，16 次采样里窗口**一次都没动过**（光标每帧走 150 DIP，全部撞上那道闸）——快拖时它只在手慢下来的时候才跳一下，这就是"帧率低"的观感来源。**修复后**：停下后最大偏差 **0px**，16 帧里有 11 帧在 15ms 内就已经到位。
  - 判据换了：一个大步长**就是**大步长，照它移动。真正的坐标跳变用几何条件识别，而不是用手速猜测 —— 只有当**光标所在显示器的 `scaleFactor` 变了**才重设锚点（`getCursorScreenPoint()` 在 200%/100% 接缝处会不连续：光标竖直下移却报出 544 DIP 横向跳变）。单屏用户从此不可能触发这条路径，也就是你说的"一块屏、快速上下拖"那种情况被彻底排除。
  - 每帧的开销也砍了一道：原来一帧要做三次跨进程同步读取（`getCursorScreenPoint` + `getBounds` + `getAllDisplays`）。现在桌面几何在**手势开始时**读一次缓存，窗口位置用"我们上一次要求的那个值"，每帧只剩一次光标读取；轮询间隔从 16ms 改成 8ms。实测轮询 114 次/秒、中位间隔 13.9ms —— 这个底是 Windows 的时钟粒度（15.6ms），不是这段代码。
  - 顺手量掉了另一条看似更简单的路：**让系统自己移动窗口**（`-webkit-app-region: drag`，走消息循环，天然满帧）。用真右键打在挂着这条属性的歌词条上：页面收到的 `contextmenu` 是 **0 次**，弹出来的是**窗口系统菜单**（`#32768`，174x142）——恢复/移动/大小/关闭那个，不是我们的锁定与关闭。所以这条路对这条没有标题栏的悬浮条不成立，轮询得留着，结论写在常量注释里。
  - 一次差点骗过我的假否证：`lyric-right.ps1` 第一版把坐标算成了 `($parts[0] + $parts[2]) / 2`，PowerShell 里那是**字符串拼接**，右键被点到 (2750685, 376928) 这种地方，菜单当然不弹——看起来正好支持"drag 区吃掉右键"。是那个荒谬的坐标让我发现量法是错的；另外这个 .ps1 缺 UTF-8 BOM 时会静默算错而不是报错。
  - 顺带修掉的：`endDrag()` 现在会把缓存的几何、锚点一起清掉，不会把上一次手势的锚点带进下一次。
- **设置页不再整页能被 Ctrl+A 涂蓝**：外壳本来定了 `user-select: none`（桌面窗口不是文档），但为了让"说明文字可以复制"开的白名单里写了 `.setting-label` —— 而设置页**每一行**的标题与副行都在这个 `<span>` 里。于是键盘快捷键页一按 Ctrl+A 全部变蓝（应用只拦了 Space、Ctrl+←/→ 等，没拦 Ctrl+A，浏览器内建的全选照跑），并且浏览器把选中的文字当成合法拖拽源，长按拖动就在设置页里搬文字。白名单改成显式点名的 `.copyable`，只给真正需要粘贴进反馈里的东西：下载目录、数据目录、迁移提示、输出设备说明四行。代价是以后新增可复制文字要手动加类——这正是要的：可复制是一处决定，不是一片继承来的默认值。
  - 全选用 `document.execCommand('selectAll')` 量，不用合成按键：`dispatchKeyEvent` 发出的按键不触发浏览器内建的编辑命令，那样量"能不能全选"会得到一个假的绿。
  - 第一轮漏了两处，都是同一个道理：设置页副标题走的是白名单里的 `.settings-heading p`（那是装饰），音乐库的 `.description` 里则混了四种东西——三句帮助话和**一串文件夹路径**。现在白名单只剩 `.folder-path` 与 `.copyable`：**值靠自己是值来点名，不靠和旁边的句子共用一个类**。
  - 音轨信息对话框是第三处：白名单里写着 `.salt-dialog dt`，等于把**标签列**（标题、艺术家、文件地址…）也放开了，于是整张表一按全蓝，选区又正好是拖拽源，长按就能把这段文字拖着满窗口走。去掉之后标签不可选，值列仍然可复制——那是 `DialogHost` 自己给 `dd` 点的名，本来就不需要 base.css 代劳。顺带确认这里"全屏拖动"只是浏览器自己的文字拖拽：导入遮罩只认带文件的拖拽（`App.vue:217` 的 `isFileDrag`），拖一段文字过去不会武装任何放置目标。
  - 检查因此是 7 项，成对出现（`.cache/verify-copyable.mjs`）：快捷键行、设置页副标题、音乐库帮助文字、对话框标签都不可选；数据目录行、文件夹路径、对话框的值列仍然可选且全选后确实包含那串路径（实测 `D:\Music\华语歌曲`）；最后一条反向对照——临时禁掉 `.copyable`，第二项必须转红。只测"不可选"会把"整页都选不了"当成改对，所以每一条收紧都必须配一条"这个还得能复制"。
- **发现页「在线平台」那张卡不再自带一圈琥珀色边框**：0 个在线平台对只用本地曲库的人不是错误状态，而这圈边框既只落在四张卡片中的一张，又会被 `.stat:hover` 覆盖（一悬停就变回普通边框），看起来更像渲染故障而不是提示。要不要引导用户去导入音源，交给下面那份「开始使用」清单和卡片副行的 `点击导入`——两处都还在。
- **拖动进度条时不再一路 seek**：过去指针每动一下都调用一次 `seekRatio`，于是一次拖动要把音频解码重开几十遍，听起来就是整首歌在手指底下抖。现在拖动只画预览，松手才 seek 一次。共享滑块组件因此多了一个 `commit` 输入：`live` 是音量的行为（每一步又便宜又听得见），`release` 是进度的行为（手势是有代价的）。键盘与滚轮在两种模式下都立即生效——按一次方向键本身就是一个完整的意图。指针被别的窗口抢走（`pointercancel`）算取消而不是提交，把手弹回播放器说的位置。
  - 预览期间读数（`03:28 / 04:20`）也跟着把手走，否则数字还在按真实位置一秒一跳，而人正盯着那根已经离开原位的把手。
  - 实机量法：把进度拖到 80%，过程中 `currentTime` 停在 3.00s 一动不动，松手后一次落到 208.17s（时长 260.22s）。
- **「全部」页签翻到第 2 页不再把本地结果整摞置顶**：本地命中此前无条件排在每一页最前面，于是第二页开头又出现同一批本地歌，下面的在线行也跟着挪位——同一首歌这一页是 01、下一页是 15，看着像分页没生效。现在本地只在第一页置顶（`本地音乐` 页签照旧全部显示）。计数行同时改口径：它报的是"曲库里匹配多少首"，不是"这一页摆了几行"，所以翻页时不会再冒出一句 `本地 0 首`，仿佛翻页把本地的结果翻没了。
- **写完歌词之后，待写入那条还留在队列里**：第一次跑第 3 步的真实验证时抓到的——用歌曲菜单「写入封面与歌词」写完，设置页仍显示它有东西待写入，再点「全部写入」会把同一条词写第二遍（多出一个备份、白改一次文件）。现在写入成功后会按"这次真的带了歌词/封面"清掉对应条目；清完为空时两个动作按钮自动禁用。
- **第二次读同一首歌，徽标就不说"待写入"了**：解析服务缓存的是**加装饰之前**的结果，所以联网取词当下徽标正确，再进一次播放页（走缓存）就变成一条普通的「在线获取」。现在入队这一步会把带「待写入」的结果放回缓存，让界面每次读到的是同一句实话——写完之后再解析会从同名文件/标签拿到新结果，标记自然消失。
- **预览标签匹配时把"将要写入"写成"已写入"**：`dryRun` 走的是同一条返回路径，措辞却只有过去时。现在按 `dryRun` 分别写「将写入 …」「已写入 …」，两处都列出实际落点。
- **清空封面目录后，曲库会自己长回来而不是留一屏破图**：封面是索引之外的派生文件，删掉它不会改动音频的大小与修改时间，而增量扫描恰恰按这两项跳过文件——所以"删封面腾地方"之后重扫永远看不见这件事；同时索引里仍写着指向已消失文件的 `coverPath`，列表画出来的是浏览器那枚破图图标，而不是应用自己的占位音符。现在 `MusicLibrary.load()` 会把这种死引用抹掉并把索引标成待刷新，于是启动时那次后台扫描顺手把封面重新抽出来，用户不需要点任何按钮。歌单封面（`playlists.json`）与艺术家头像记录（`artist-images.json`）同样按"文件不在就当没这回事"处理：前者回落到音符占位，后者下次进这页重新解析。三条都有回归测试；实测把封面清空后启动，首屏 17 行全是占位图标、破图 0 个，应用自己在后台抽回 1390 张封面后正常显示。
- **便携版在「迁移目标那块盘没插」时不再回 C 盘**：`resolveDataDir` 里，指针记录的目录今天不存在时一律退回 `%APPDATA%/jj-music`——于是装在移动硬盘里的便携版换台机器插不上，就悄悄在 C 盘另起一份库，正是需求 2 要消灭的行为。现在改成先问"能不能退回程序旁边"：便携版回 `<exe>/data`（迁移是复制不是搬家，那里本来就有上次留下的副本），安装版没有可写的程序目录才回系统应用数据目录；两种情况都在提醒里写清本次用的是哪一个目录。这条是端到端驱动「迁移到其它目录」按钮时测出来的——之前只分别验过指针解析、复制过滤和按钮禁用态，没走过完整的点击链。
- **全局搜索的平台页签不再混入本地结果**：结果列表此前无条件等于「本地命中 + 在线命中」，所以切到酷狗/QQ 这类单个平台页签时，本地歌曲仍然排在最前面，且计数行照打「本地 N 首」——看起来就像页签筛选没生效。现在具体平台页签只列该平台的结果，`全部` 与 `本地音乐` 才列本地命中。另外顶部的「本地歌曲优先显示」这句其实是误导：设置里那个开关是 `优先播放本地文件`，只决定播放时选哪个文件，从不参与列表排序，措辞已改为「切换平台可只看该来源的结果」。
- **拖入文件夹不再被静默丢弃**：主进程按扩展名给拖入项分类，目录没有扩展名，于是直接落进 `skipped`，用户看到的是「没有可导入的文件」。现在目录走曲库文件夹那条路（复用已有的递归 `walk()`，夹内音频按标签入库），并在主进程弹一个原生确认框——加入曲库等于把整棵子树交给 `jjmedia://`、歌词读取与标签写入，比单个文件的授权范围大得多，而路径是从渲染层来的，所以要由用户亲自点一次确认。取消时不报错，也不再显示成"导入失败"。
- **随机播放改为「洗一整轮再重洗」**：此前每次切歌都重掷一次随机数、只排除当前曲，短队列会在几秒内重复同一首，而排在后面的歌整个会话都可能听不到；同时 `上一首` 走的是同一个分支、忽略方向，等于向后跳随机曲，听过的顺序找不回来。现在按洗牌袋（Fisher-Yates）一轮一轮播，两轮相接处不重复，并记录抽取顺序，`上一首` 回到刚才那首、再按一次继续往前退。队列由其他页面增删时按曲目 id 跳过失效项。
- **桌面歌词悬浮窗的拖动**：原来列了五处，接上第二块屏之后又测出两处更深的（⑥⑦），一起修。① 每个 `pointermove` 都同步发一次 IPC 并 `setPosition`——现在页面只在按下与松开时各发一条，中间由主进程按帧跟随系统光标：一次 3 秒拖动从 304 次 IPC 降到 2 次。② 按住鼠标时呼出右键菜单或窗口失焦，`pointerup` 永远不会来，指针捕获被悄悄拿走，松手后歌词条还在跟着鼠标跑——现在 `blur` 与 `lostpointercapture` 都会收尾（主进程随之停止轮询）。③ 目标位置被夹在「窗口当前所在那块屏」的工作区内，于是永远拖不到第二块显示器上；一块窄于 820px 的屏幕上横向坐标还会直接冻住——改为对全部显示器工作区的并集取夹。实测（主屏 200% + 副屏 100%）：把记忆位置推到极远，窗口落在 (2062, 2528)，正是副屏的右下角；旧代码会把它夹回主屏。④ 重新打开时，上次拖到的位置按「新建窗口此刻所在的那块屏」夹，而新窗口还在系统默认位置，结果停在第二块屏上的位置每次启动都被拽回主屏，看着像位置没保存——改为对并集校验。实测副屏上的 (1500, 2000) 关掉再开仍然精确回到 (1500, 2000)。⑤ 锁定位置后拖动路径不做任何检查——渲染层与主进程各设一道闸。实测锁定后真实鼠标拖一次：窗口不动，且页面收到的 pointermove 为 0（鼠标穿透本身就挡住了，主进程那道是纵深防御）。⑥ **并集是个矩形，而两块错位的屏会在矩形里留下不属于任何屏幕的 L 形空隙**——拖进那条空隙，歌词条就凭空消失了，而且位置已经落盘，重启还是看不见。现在要求窗口中心必须落在某块真实屏幕上，否则贴到最近的那块（宁可丢掉用户拖的坐标）。⑦ **混合 DPI 的接缝会让 Electron 自己的坐标发生跳变**：光标竖直向下拖过 200%/100% 两屏的交界时，窗口横向平移了 544 DIP、纵向反向退了 22px，而光标 x 全程没动。第一版改成"页面发位移、主进程加到位移基准上"并没有治好，第二版改成"主进程轮询系统光标"也仍然跳——根因不在页面坐标也不在窗口换屏，而是 `getCursorScreenPoint()` 在光标跨到不同缩放率的屏那一刻本身就不连续。现在每帧只叠加**当帧**位移（基准每帧重读），并且丢掉单帧超过 120 DIP 的步长——那只可能是坐标空间重映射，不是手速。复测：同一次跨缝拖动 x 保持 1000 不动 ✓，跨完之后一次 +600 的横向拖动窗口精确 +600 ✓；代价是越过接缝的那一帧会少跟一段距离（实测 98px 只跟了 22px），下一帧起恢复正常。
- **艺术家头像：查不到不等于没有**：以前"连不上网""平台没答上""这位歌手确实没照片"三件事在 `artist-images.json` 里长成同一条记录——第一次进这页时赶上断网，这个名字就被永久标成"没有头像"，只有手点「更新网络头像」才会再问。现在四家平台全都抛错时 `resolveArtistImage` 报的是失败而不是"没有"，只要有一家真正答了才记结论；图找到了却存不下来也算失败。渲染层补了三处：① 点「更新网络头像」原来绕过那条并发闸直接发请求，现在与滚动进入视口走同一排队（同时两桩），但会插到队首——整页滚到底会排进几百个后台预取，让它们先跑完的话这一下要好几分钟才有回应（第一版就是只"并入"不插队，被端到端验证当场抓到点下去没反应）；② 提示分成三种——已更新 / 各平台都没有 / 查询失败，最后一种不再冒认前两种；③ 离开这页时还没出发的预取会被丢掉，不会留一个后台队列继续问平台。实测（预览 profile、472 位艺术家）：清空头像记录后首屏 0 → 18 张、滚到底 33 位，「更新网络头像」确实把记录时间推后了一格；把封面目录临时换成同名文件让存盘必然失败时，提示是「头像查询失败」而不是"没有头像"，且这次失败没有写进记录（桥是深冻结的，换不了桩，所以只能让主进程这一次真的失败）。
- **任务栏播放控制条的三个图标一直是向上的三角形**：`make-icons.mjs` 的 `triangle()` 按"行宽随 y 增大"画，于是 `play` 是 ▲ 而不是 ▶，`previous`/`next` 同样是 ▲。改成按"距垂直中线的距离"决定行宽后重新生成 PNG，`pause.png` 与 `tray.png` 字节分毫未动（说明错的确实只有这三张）。
- **首屏之前不再被索引里的封面路径拖住**：`MusicLibrary.load()` 要逐条确认 `coverPath` 指向的文件还在，用的是 `existsSync`——几千条就是几千次同步 stat，全部发生在第一次画界面之前。改成一次异步批量 `stat`，判定与副作用（清死引用、把索引标成待刷新）一字不变。
- **批量写入的提示会张冠李戴**：「写入封面与歌词」的 toast 把整批结果里第一条 `note` 直接拼在计数后面，而那条 note 可能来自一首**没写**的歌，于是能出现"3 首未写入任何内容"和 3 首已写入同时成立。现在按结果分组分别计数；IPC 失败（例如一次超过上限）也有提醒了，原先是点了没反应。
- **设置页几处点了没反应**：数据目录位置、输出设备列表、下载目录名称这三处都是 `await` 一个可能拒绝的 IPC 却没有 `catch`。现在都收口成 toast；「可改写的格式」这类多选项补上 `aria-pressed`——读屏原先只能念出"有个按钮"，念不出它是否选中。
- **键盘焦点不再改变控件形状**：全局 `:focus-visible` 里写了一行 `border-radius`，本意是让焦点环带圆角，实际是 Tab 聚焦时把控件本身捏圆（Chromium 的 outline 本来就跟着元素自己的圆角走，这行是多余的）。
- **音源链接导入不再被一个 `%` 打断**：链接末段带非法百分号转义（`a%zz.js` 这类）时 `decodeURIComponent` 抛 `URIError`，整次导入以"URI malformed"失败。现在按原样把那段文字当文件名。
- **验证与诊断脚本自己会骗人**（都是"看起来绿"这一类，逐条改）：
  - `run-tests.mjs --online`：套件名改错一个，选中集合就成了空集，而空集按算术等于全通过。现在选不到预期的套件直接非零退出。
  - `run-e2e.mjs`：`probe-hook` 崩溃或被信号杀掉时退出码不是 1，于是被判成"生产构建里钩子不可达"——把一次根本没跑成的验证当成结论。现在只认 0 和 1，其它一律失败。
  - `e2e-verify.mjs`：`APPDATA` 没设置时"真实 profile 未被写入"会变成一项无物可比的空比较，现在直接失败退出；把副本里的绝对路径改写时原来只认反斜杠，漏一条就等于让"隔离"运行去改开发者自己的曲库，现按两种分隔符、不区分大小写匹配；被指纹保护的 profile 文件从 3 个补到 6 个（加 `downloads.json`、`pending-assets.json`、`artist-images.json`），并且一个都比不到的时候会明说。
  - `diagnose-restricted.mjs` / `diagnose-online.mjs`：一个音源脚本解不开、或 `user_api.json` 不是合法 JSON，都会把整轮诊断砸在半张表上；错误音源那一档出异常时还会留下一个 source 子进程和一个临时目录。现在各自 try/finally 收口，异常本身作为一条结果打印。
  - `capability-probe.mjs`：注释写着"两种结果都立刻清理"，可那半行 `require()` 在 ESM 里根本不存在（异常被自己吞掉），`HKCU\Software\JJProbeTest` 每跑一次留一次；写到 %TEMP%／文档／AppData 的探针文件也从没删。现在结束时会删，删不掉写进报告的 `cleanupFailed`。另外 `elevated` 字段在 Windows 上恒为 `undefined`（等于没有这项），现在按 `HKU\S-1-5-19` 是否挂载实测。
  - `privilege-probe.mjs`：受限 token 下写不出报告文件时进程直接崩，最有价值的那次测量反而丢了；现在失败会把报告打到 stdout 并非零退出。`whoami /priv` 的失败也不再伪装成一条名叫 `error: …` 的特权。
- **迁移数据目录不再允许选到自己里面**（审阅复核确认，high）：`dataDirMove` 原先只挡"目标就是当前目录"，没挡互相包含。而迁移用的是 `cp(current, target, {recursive:true})`——一边走一边写——挑了当前数据目录里的一个子文件夹，就会把刚建出来的目录再复制进它自己，一层层套到路径超长或磁盘写满；反过来挑上级目录，等于把应用的文件散到那个目录里。新增纯函数 `relocationProblem(target, current, caseInsensitive)`（same / nested / null，大小写与两种分隔符、结尾斜杠都算过），判定放在弹确认框之前。
- **「写入位置=同名文件」不再让标签匹配空转**（high）：`标题 / 艺术家 / 专辑` 这类文本字段只有文件标签装得下，可写入目标是按设置取的，于是设置选"同名文件"时 `标签匹配` 连 embedded 这一路都不再尝试——用户点了应用，一个字段没写，连"未写入"那句提醒都不发（那句只在目标里本来含 embedded 时才说）。现在带文本字段的补丁一律要求尝试标签（`writableFormats` 的交集照旧生效），写不进去才说清为什么。
- **试运行（预览）不再改动任何东西**：`dryRun` 也会把 `written` 置真，而调用方拿这个值去决定"要不要重读文件、要不要清掉待写入队列"——于是标签匹配的预览对话框一旦打开，索引被改写、那首歌的待写入条目消失。现在 `commitAssetWrite` 在 `dryRun` 下直接返回；`paths` 只列真正改过的文件（预览里为空，将来时由 `note` 表达）；"这次是否真改了标签"改用一个明确字段 `embeddedWritten`，不再拿界面文案 `'文件内嵌'` 去比——措辞改一个字就会让刷新静默失效。顺手把 `skipBackup` 与暂存文件绑死：没有 `stagingPath` 就不许跳过备份，避免以后有调用方直接改用户的文件而不留退路。
- **下载在最常见的那种失败下不再丢掉已下载的字节**：`keepPart` 原来要到文件被打开那一刻才置真，于是解析地址失败、连接失败、返回 403、请求中止这些"响应还没开始"的情况一律走清理分支，把 `.part` 删掉——断点续传恰好在它最该起作用的时候归零。现在从算出 `.part` 路径那一步起就保留。
- **续传时校验服务器给的是哪一段**：以前只要看到 206 就当续传。现在要求 `content-range` 的起点等于我们请求的偏移；没有 `ETag`/`Last-Modified` 可发（不少中转就是不给）时，再要求这一句声明的整份长度等于首答记下来的 `remoteSize`（新字段，随任务记录持久化，不随重试清空）。对不上就取消响应、删掉那段拼不上的字节，并说明"重试会重新下载整份"——宁可慢一次，也不要一个能播放但开头多了别人一段的文件。四条续传路径都有测试，并用变异验证确认过它们真会红。
- **酷狗/酷我歌单在平台没报总数时会静默截断**：`total` 缺失时旧代码把"已经拿到的数量"当总数，于是第一页刚好满 100 就收工——500 首导入 100 首，一声不响。现在只有空页才是硬停，平台报了总数就按总数走（有些接口末页本来就短），没报才用短页作结束标志；页大小抽成一个常量，防止"请求里的 100"和"判断里的 100"各改一处。
- **网易云歌单封面原来一直抓不到**：v6 详情里那个字段叫 `coverImgUrl`，代码读的是 `coverUrl`/`cover`——本机实测线上响应确认。因为封面是可选项，界面只是不显示图，所以这个错误完全看不出来；连测试的假响应都是照着错的字段写的，等于给错行为盖了个章。现在按真实字段取，另两个留作回退。
- **桌面歌词窗口的两处生命周期漏洞**：① 拖动时每 16ms 轮询一次系统光标，而窗口的 `closed` 事件只把 `this.window` 置空、没停这个定时器——进程还在转，若在这一趟拖动结束前重开悬浮窗，旧定时器会去挪新窗口；现在 `closed` 与 `stepDrag()` 都收尾（后者顺带检查 `isDestroyed()`）。② `锁定位置` 的门只在收到 IPC 消息那一刻查，而一次拖动从头到尾只发两条消息——手指还按着的时候从设置页勾上锁定，歌词条照样跟着鼠标走；现在每帧重查。
- **丢弃待写入之后徽标不会再继续喊「待写入」**：徽标读的是解析服务的缓存而不是队列，`全部丢弃` 只清了队列，所以要点回那首歌播一次才会消失，看起来像按钮没生效。现在按被丢弃的曲目逐条清缓存。（顺带一个产品问题留给用户定：丢弃之后再次播放同一首仍会重新联网找词——要不要记住"这首不要问"，是另一件事。）
- **一次拖多个文件夹不再把整个曲库扫 N 遍**：`scan()` 每次都覆盖全部已注册根目录，所以逐个 `addFolder` 就是 N 次完整遍历（代码注释原本写的正是"一次遍历"，但实现不是）。新增 `addFolders(list)`：先登记再扫一次，进度也只报一次。
- **`assets:export` 的 kinds 校验补上**：数组先判长度再过滤，于是 `['foo']` 过滤成空集合、写回一个空结果，界面只显示"没有需要写入的内容"。现在非数组直接报错，过滤后为空也报错——坏请求要说坏在哪里。
- **渲染层一批**（审阅逐条复核后修）：切到随机播放后的第一次 `上一首` 会跳过刚才正在听的那首（历史是从 `playTrackAt` 才开始记的，现在进入随机时用当前曲目打底）；在线曲目查到的来源写不回队列行（`'assets' in queued` 恰好把"本来什么都没有"的那些行挡在外面）；设置保存失败会弹两条一样的提醒；待写入队列查询失败被显示成"没有等待写入的内容"（现在是另一句话并禁用两个按钮）；迁移成功后刷新目录失败会被说成"迁移失败"；歌单的「移除封面」「从歌单移除」缺 `catch`，失败时界面停在半成品状态；歌单拖动排序并发发送两个请求时可能后发的先落库、把旧顺序留下（现在串成一条链，每次都是整表顺序）；网络导入的"改过了"只比数量，纯拖动改顺序不算改；导入预览的封面字段与破图回退（`<img>` 拿不到文件时改用应用自己的占位图标，而不是浏览器的破图）。
- **两条审阅意见经复核不成立，记在这里免得下次再报**：① `flags` 勾选连点会丢更新——`createSettingsWriter` 是乐观应用（`apply({...read(), ...snapshot})`），第二次点击读到的已经是第一次的结果；② 咪咕分页会在 `totalCount` 缺失时停在第一页——那一行早就带着 `total &&` 的守卫和解释它的注释。另有一条（渲染层的头像查询失败提示）在本轮之前已修，审阅跑的是旧提交。
- **播放页左上角叠出两个「JJ Music」**：这是同一轮里我自己带进去的回归。顶栏的联想面板要浮到页面内容之上，而承载它的胶囊本身是 `overflow:hidden`（宽度动画需要），所以当时给整条 `.titlebar` 加了 `position:relative; z-index:120`。标题栏于是压到了播放覆盖层上面——而那个覆盖层是**故意**自绘一份品牌文字和窗口按钮的（它接管整个窗口），结果两份文字同一位置上下错开叠在一起。改成把标题栏恢复原样、层级只给真正需要浮起来的那个下拉面板（`.suggest` 自己，`position:absolute` 且外层没有栈上下文困住它）。中途还有一次没修对：把层级放到下拉的包裹层 `.search-slot` 上，等于让那颗放大镜胶囊单独透在覆盖层之上——你第二次截图看到的就是它。两条实机命中测试钉住：正常页面上联想面板要能被 `elementFromPoint` 命中；播放页里**逐个控件**命中测试——品牌、放大镜、前进后退箭头都必须命中不到（被覆盖层压着），且 `.np__caption` 只有一份。第二条正是靠"逐个控件"而不是"只看一个点"才拦得住今天这个漏网的放大镜。
- **一处测试自己走了真实网络**：「指向已被删除文件的头像记录作废」重建 store 时没带 `fetch`，于是那一步是去问线上平台"周杰伦有没有头像"——通过与否取决于当天平台答什么。补上假端点并断言"确实重新问过"之后，这一条从 539ms 变成 10ms。
- **列表页"滚轮滚动时滑块不见了"**：不是没画，是画得太短。滑块长度按 `可视 / 总高` 比例给，歌曲页把 3368 行塞进约 500px 的窗口，实测滑块只有 **9px**——在 DOM 里，在屏幕上看不见，更抓不住。给 `::-webkit-scrollbar-thumb` 加 `min-height: 34px`（上下各 3px 是那条透明边框，真正画出来约 28px），悬停与滚轮滚动时都量到 28px。反证：现场把最小长度改回 0，同一处立刻退回 9px，这条断言才不算空。
  - 量它的时候踩了一次"绿色是假的"：滑块色是 `rgba(255,255,255,.12)`，半透明，合成到深色页面上根本不是白色，按颜色去截图里找像素会得到 0 条——看着像"滑块真的没画"。改成按轨道条内的**行亮度**找连续一段才量得准。
- **同一屏出现两条不同位置的滚动条**：列表页的滚动条属于内层列表（它自己滚，虚拟滚动需要一个高度稳定的容器），于是落在离窗口右缘 28px 处；发现 / 设置 / 曲风这些页的滚动条属于页面本身，贴着窗口右缘。两条轨道并存就是"设置页的滑块像是另一套"的来源。现在把列表容器向外撑这 28px，再把同样的 28px 补回行的右内边距——**轨道移到窗口右缘，而行上的时长、⋯、表头列一个像素都没动**（实测 ⋯ 右缘仍是 x=1228，距窗口右缘 52px），也没有多出横向溢出。当时按"悬停就画滑块"来统一，结果你在几个页面看见的滑块时有时无——那一半见下面"统一到侧栏那套"。
  - 差点漏掉一个：`.tracklist` 下面有三个可渲染的兄弟块（表头、行、以及勾选后出现的"已选择 N 个项目"工具栏），只补前两个的话，工具栏最右边那个 ✕ 会跟着右移 28px 正好压在轨道底下。补上第三条之后实测内容右缘仍是 1240（距窗口右缘 40px），探针里按 Ctrl+A 真的把它叫出来量了一遍，而不是"看代码觉得没问题"。
  - **第二天又改了一次，统一到侧栏那套**：改成"悬停驻留"之后你发现有的页面出滑块、有的不出，决定全应用统一走侧栏原来那套——滚动时现身、停止 0.9 秒淡出。于是 CSS 里那条"悬停容器就画滑块"换成了 `.is-scrolling::-webkit-scrollbar-thumb`，`.is-scrolling` 由一个新的 `utils/scroll-reveal.ts` 打上：在 document 上用**捕获阶段**监听 `scroll`（`scroll` 不冒泡，不用捕获就只能给每个滚动元素单独绑一个监听——侧栏之前正是这么孤立的），谁滚就给谁加类，0.9 秒没有新事件就摘掉。侧栏自己那 15 行 JS 与三条 scoped 覆盖规则随之删除，现在全应用只有一套。滑块仍然能在指针压到它身上时现身（`::-webkit-scrollbar-thumb:hover` 留着），不然抓不到一个透明的滑块。
    - 六个滚动面（歌曲列表 / 最近播放 / 设置 / 艺术家 / 发现 / 侧栏）各量"滚动中"和"停止 1.6 秒后"两种状态，14/14 一致：滚动中分别有 56 / 62 / 143 / 56 / 669 / 494 行比基线亮，停止后全部回到 0 行。这里的判据是"轨道条里有多少行比基线亮"，不是整条平均亮度——长页面的滑块只占轨道 6%，平均下来差 0.7，量不出有没有画。
    - 一处没有编进断言的东西：同一个"指针压到滑块上"的检查，在单独脚本里稳定量到 794 行变亮（x 在轨道内亮、轨道外 12px 不亮），放进这条长流程里却恒为 0，我没查出原因。因为它是**一条全局 CSS 规则**、没有"每页各一套"的可能，所以按单独那次的结果认定它工作，长流程里只保留能稳定复现的两项。
- **歌单九宫格的封面只显示正中间一块**：`.plcard__art` 有 `aspect-ratio:1` 与 `overflow:hidden`，却**没有** `.plcard__art img` 的尺寸规则。仓库里其余每一处封面（专辑、艺术家、歌单详情页头部、迷你播放条、导入预览）都写了 `width/height:100%; object-fit:cover`，只有这一处漏了。没尺寸的 `<img>` 保留原始大小（实测一张 1200×1200 的封面），152px 的方格把它裁到只剩正中那块，所以看到的"封面"其实是整张图的一小格。补上同样三行。
  - 顺带记一件影响验证方式的事：`PlaylistStore.load` 会把指向**不存在文件**的 `coverPath` 直接删掉（settings-store.ts），所以"封面文件被清空"这一种情形在主进程就消化掉了，渲染层的 `@error` 分支根本走不到。探针因此用的是一个**存在但不是图片**的文件——不然那条断言测的是加载器，不是回退逻辑。第一次跑就是这么蒙过去的：三条"退回图标"全绿，而当时侧栏其实一张封面都没渲染出来。
  - 这一条有变异验证：把那三行删掉重新构建，实测 img 回到 1200×1200，"等大"与"按 cover 裁"两条同时转红；"退回图标"那几条不受影响（它们量的本来就不是尺寸）。

### 界面

- **播放页：封面的颜色真的成为背景，封面下面有了倒影**：这一页本来就有"来自封面的虚化背景"（设置里的 霞光特效），但它被自己那层遮罩压死了——虚化层 `opacity: .5`，上面的遮罩在中段就到 92% 不透明，结果整页看着是一片接近 #1a1c25 的灰黑，只有边缘隐约有点颜色。现在改成"该暗的是封面本身，不是遮罩"：虚化层走满不透明，压暗交给 `brightness(.78) saturate(1.65)`，遮罩只负责上下两条要放小字的地方（中段 54% 而不是 92%）。用 `brightness` 而不是 `opacity` 压暗是关键——透明度会把颜色往页面的灰底上洗，那正是要消掉的平感。实测顶部色带平均彩度 10 → 25.1。
  - 封面下方是引擎自带的 CSS 倒影（`-webkit-box-reflect`）：它画的是元素渲染盒的翻转副本，所以圆角、圆形唱片模式、旋转中的唱片都自动跟着走，不需要第二个 `<img>` 去同步。只有拿到真封面时才加（占位音符不该有自己的镜像）。
  - 两个坑都留了注释，因为是量出来而不是想出来的：① 倒影要占的那段高度最初写成 `calc(var(--art-size) * 36%)`，而 `calc()` 不能拿长度乘百分比，整条 margin 被**静默丢弃**、位置全乱（改成无单位系数才对，后来频谱挪到底部后这段预留干脆删了）；② 倒影遮罩的渐变方向与直觉相反——它的 `0%` 在**远离封面那一端**，把不透明写在 `0%` 会得到一张掉到窗口底部的独立小封面，所以渐变是"从透明爬到 100%"。
  - 倒影是绘制不是布局：它不进盒模型，所以叠在谁上面取决于 z-index。现在它直接压在频谱带上（两者都是"画面"），只有真封面才有倒影——占位音符不该有自己的镜像。
  - 检查全在像素层做（`.cache/verify-round21.mjs`，16 项）：倒影与封面"对锚 vs 竖向错锚"的相关 0.394 / 0.103，贴边与远处 0.621 / 0.007，居中歌词对比度 5.30:1；频谱那几条是底行覆盖率 42.5 / 中间 1.6 / 顶部 0.0（镜像版是中间最满，所以"只往上长"量得出来），剪影比周围背景亮 10.6、最亮处只有 81（白字是 255）。第一版用"逐行平均亮度相关"，倒影明明错位了还能量到 0.48——那张封面大半是纯粉底，任何竖向渐变都能相关到这个数，所以换成 2D 图块 + 错锚对照。把 `-webkit-box-reflect` 删掉重跑，两条倒影检查同时转红（r 掉到 −0.099），确认它们不是真空的。
- **频谱从封面下面的一条小插件，改成从进度条长出来的剪影**：以前它是 20 根圆角柱子挤在封面下方 44px 里，既看不清形状，又把封面顶得偏离了页面正中。现在它铺满整条窗口宽度，**基线就是进度条那条线**（实测带底 738 vs 进度轨道中线 740），柱子只往上长，落在歌词与封面的**后面**（`z-index: 1` + `pointer-events: none`），看起来是声音从进度条里冒出来，而不是页面下方多了一个控件。
  - 中途先做过"以自身中线上下镜像"的版本，实机看过之后否掉了：镜像把最宽的那一圈留在页面中段，正好压在歌词上抢注意力。改成单向往上之后，填充用一条竖向渐变（基线处 16% 透明度 → 顶部 0），所以再响的段落也是**渐隐着消失**，不会在字上横着切出一道硬边。
  - 柱子数量跟着宽度走（约每 11px 一根，1280px 窗口约 116 根）：整条宽度上放 20 根柱子只会变成 20 个宽台阶，看着是一块板。相邻不留缝（缝是它像均衡器插件的主要原因）。颜色深浅按高度算而不是按音量算——按音量调 alpha 会画出一道道竖条纹。
  - 因为频谱不再占左栏的空间，封面为倒影预留的那段 margin 一并删掉，封面回到内容盒垂直居中（实测内容盒内上下留白 142 / 142）。倒影叠在频谱带上面是刻意的：两者都是"画面"，不是控件。
  - 窄屏单列布局下原来那条"藏掉频谱"的规则删了——它以前占一行高度才需要藏，现在只是背景。
  - 圆形唱片模式保留倒影（量过截图：镜像的是一个圆，读起来像唱盘放在亮面上），没有为它单独加开关。
  - 柱子之间的竖线去掉了：那正是"每根柱子各自 `fillRect`"留下的接缝。半像素重叠是为了盖住分数宽度产生的细缝，但重叠那一列被画了两遍，alpha 从 41 涨到 75，于是每隔一根柱子就有一条更亮的竖线。改成**一条阶梯路径、一次填充**之后不可能再有接缝（没有相邻的图形可缝）。变异验证：把填充换回逐根 `fillRect`，底行 alpha 立刻变成 41–75；单次路径版是 41–41（横跨 2560px）。
  - 因为这次改动，一条原本"绿着"的检查被揭穿是假的：它量的是"每列最亮的像素"，而竖向渐变里每一列最亮的都是贴底那一格，所以它量的其实是接缝——接缝一去掉，它反而"失败"了（差值从 10.6 掉到 0.8）。现在换成开/关对照：同一批像素在频谱带显示与隐藏时各量一次（带内底部平均亮度 62.4 vs 39.2，差 23.3）。中间还试过"带内底部 vs 带内顶部"，那条量到的是遮罩自己的竖向渐变（页面越往下越暗），算出 −6.4 这种"频谱让底部变暗"的荒唐数。
  - 顺带修了 UI 冒烟里 `application shuts down cleanly` 这条检查本身：它的 `check(name, ok)` 连 detail 参数都没有，"16 秒才退"和"永远不退"是两件事却报同一个 FAIL；而且它把"读现场状态"放在 `close()` 之后，读到的永远是空。现在按顺序改成 读状态 → 起表 → 关闭 → 计时，超预算后再多等 35 秒只为分清"慢"和"挂住"，FAIL 仍然按 10 秒算。现在报的是 `playing=false 未完成任务=0，退出码 0，用时 442ms`。之前那几次红仍没能复现，按"已装表、原因未定"记录。
- **时长统一成 `00:00`**：分钟也补零，于是 `3:48` 变成 `03:48`，一秒以内、空时长、坏数据一律是 `00:00`。之所以要管这一位：同一列里在线曲目显示的是平台自己给的 `interval` 字符串（本来就是 `03:47` 这种形状），本地行补零与否直接决定这一列是齐的还是参差的——等宽数字在这种错位里救不了你。播放条读数、曲目行、歌词编辑器、下载弹窗共用这一个函数，新增 `src/test/format.test.mts` 钉住 `03:48 / 00:09 / 59:59 / 1:00:00 / 23:59:59` 与各种坏输入。播放历史里那条 `^\d+:\d{2}$` 的匹配不受影响（它只解析、不显示），刻意没跟着改。
- **全局搜索页的结果区拿回了属于它的高度**：先把这一页竖向空间逐项量了一遍——内容区 690px，结果列表只分到 355px（行高 72px，也就是 4.9 行），剩下 335px 全是页面外壳：标题块 76、独立的搜索框行 46、页签行 46、计数行 25、列表表头 32、分页 54、上下内边距 56。于是"一页 40 首歌却只看得见 5 首"不是错觉，是排布。改动全部落在外壳上，没有动行高、字号或控件位置：
  - ~~搜索框并进标题那一行（左侧标题+说明，右侧输入框+按钮）：省 46px。~~ **这一条已按用户要求回退**：搜索框回到标题下方独立一行，宽度回到原来的 `max-width:620px`（并入 header 时被压到 420px）。为这次挪动加的两条规则（`.search-view .view__header{margin-bottom:16px}`、`.search-view__title{min-width:0}`）一并删除，不留死代码。代价是**重新量的**，不是引用本条目那个 46px：同一个 1280×820 窗口、同一个查询、同一份构建，只换 SearchView 这一处，结果列表 **541px → 491px，即 50px ≈ 0.69 行**（可视 7.51 → 6.82 行，行高 72px）。这里刻意不用别的手段把那 50px 补回来。
  - 计数行贴到页签行的右端（它说的就是当前页签的答案，本来就该在一行上）：省 25px。
  - 分页与列表之间 22px→10px、页面底部内边距 32px→14px、页签行下方 18px→12px：省 43px。分页**留在列表下面**，因为读到页底之后眼睛就在那儿找 下一页，位置换来的高度不值。
  - 结果：同一窗口下列表 355px → 466px，可视 4.9 行 → 6.5 行（实测）。**这组数是上面三档一起量的，第一档已经回退，所以现在拿到的是后两档**；另外它和上一条那对 541/491 不是同一个绝对值，因为量 355→466 时那个窗口里页签行是折成两行的——两对数各自可比，跨着比不行。再想要更多行的话，开关在 设置 → 行密度 = 紧凑（54px），同一屏能到 8.6 行——那是全局观感的取舍，涉及每一张列表，所以留给你决定而不是我替你改。
- **歌单里的歌曲可以拖动排序**（需求 8）：共享的歌曲列表新增 `reorderable`，行可以按住拖到任意位置，插入位置用一条高亮线指示，松手后整表顺序落库。此前 `playlists.reorder` 这条 IPC 与主进程的持久化逻辑都是好的，缺的只是渲染层从来没人调用它——所以这次是"接上"而不是"新写"。
- **歌单页去掉无条件「清空」，改为批量移除**：多选机制（Shift 连选、Ctrl 加选、Ctrl+A、复选框列、选择工具栏）本来就在共享的歌曲列表里，缺的只是让歌单页的动作跟上了选择——「从歌单移除」此前只作用于右键那一条，`清空` 又无论选没选都整表清掉。现在两处都按当前选择生效（多条时菜单项写作「从歌单移除 N 首」并先确认，说明歌曲仍留在曲库、可再加回来），`清空` 按钮连同 `playlist:clear` 通道一起删除：想要清空，Ctrl+A 再移除即可。主进程侧 `removeTrack` 相应改为 `removeTracks`（一次删除只落盘一次，并对渲染层传来的 id 数组做类型过滤）。顺带清掉 `QueueView` 里早已没有对应 DOM 的 `.reorder__*` 样式与歌单页的 `.bulk`。

- **歌单名前面现在就是它的封面**：侧栏里带封面的歌单，名字前面那 18px 的一格换成 18×18 圆角缩略图；没有封面的仍然是原来的音符／爱心图标，内置的「我喜欢的」如果自己设过封面也照样显示。刻意与图标共用同一个盒子尺寸，两种行混排时名字才对得齐（实测两行的名字左边界都是 61px；不给尺寸时缩略图按原图 1200px 展开，把名字直接推出侧栏）。读不出来的封面（文件在但解不开）退回图标，不留破图。`<img>` 显式 `draggable="false"`：图片自己也是拖拽源，而内置那两条本来就是不可拖的行，不关掉就能把它们的封面拖到导入区去。

- **播放条右侧那一簇控件重新分配了呼吸空间**：时间 / 输出规格 / 更多 三段之间的间距各 +2px（实测 12→14、5→7、5→7），而音量组内部反过来收 4px（静音键↔滑杆、滑杆↔百分比各 12→8，整组宽度 133→125）。理由不是"这里太挤那里太松"两句感受：簇里的控件是各干各的，靠 gap 拉开才分得清；而静音键、滑杆、百分比说的是同一件事，被拉开之后就变成"另外三个控件"。图标按钮本身带 7px 内边距，所以它那一侧的可见间距要按 `gap − 7` 读，这也是为什么声明的 gap 从 12 变 8 之后，视觉上正好也紧 4px。
- **无损这个维度从概览里撤掉**：歌曲页的「仅无损」勾选去掉（连同它记在页签状态里的开关、以及"定位正在播放的歌时顺手把它关掉"那半条逻辑），发现音乐第一张卡的副行也不再报无损曲目数——上一轮刚把无损数从统计卡挪到副行，这一轮按你的意思整条撤掉，副行只剩 `3 个文件夹 · 约 237 小时`。曲目行上的音质徽标、下载音质选择、Hi-Res 判定都没动：撤掉的是"按无损筛全库"这个入口，不是无损本身。

## 0.1.7 — 2026-09-21

### 功能

- **桌面歌词**：新增独立悬浮窗，在应用之外置顶显示当前歌词行。设置·播放、播放条「词」按钮、下栏 ⋯ 菜单三个入口共用同一份状态；悬浮窗可拖动改位置（位置写入设置，重开后回到原处），「锁定位置」后鼠标点击穿透到下层窗口，字号分小/中/大/特大四档且改动实时生效。未播放时显示「曲名 – 艺术家」占位而不是空白条。悬浮窗只暴露 `onState / dragTo / openMenu` 三个方法，拿不到曲库与音源桥。

### 界面

- **标题栏**：LOGO 与「JJ Music」固定在最左，前进/后退/搜索整体右移并与侧边栏右边界对齐；窗口按钮抽成共享组件，标题栏与播放页复用同一套，最大化后按钮会跟着变成「还原窗口」并换字形（此前播放页那份是复制的，永远显示最大化）。
- **专辑按专辑名合并**：同名专辑只出一张卡（1357 → 1242），封面取该专辑下占多数字，卡上仍标出主唱。代价是同名不同歌手的专辑会合到一张卡里，这是按你选的「按专辑名合并」规则的结果。
- **艺术家**：头像由圆形改方形，且可以点击进入该歌手的作品页（此前只是图片）。
- **播放模式**：改为点击直接循环切换，上方浮出当前模式名提示 1.5 秒，不再呼出菜单；四种模式换上互不相同的字形（顺序/列表循环/单曲循环/随机）。
- **播放页**：下栏换成与主界面同款（无封面的版本，保留艺术家等信息），小歌词行不再显示；歌词来源标签移到曲名信息列末尾；原来那排歌词图标按钮收进 ⋯ 菜单与右键菜单；新增全屏按钮。
- **侧栏**：歌单管理改成与导入歌单同款的卡片式列表；除歌单外的栏目一律不可长按拖动（此前在专辑等栏目上按下拖动会误触发文件导入浮层）；搜索按钮未聚焦时也保持高亮。
- **发现页**：状态卡此前是 `minmax(170px, 1fr)`，四张卡会平分整个视口（2200px 宽时单张拉到 472px），现改为固定四列并给整行设 1076px 上限——始终一排、单张最宽 260px；曲库与音源都就绪时设置引导区不显示、页面只剩四张卡加约 520px 空白，现补一条「最近播放」封面横向轨（最多 12 张，点击即从该处播放，「查看全部」进独立页）。该轨复用「最近播放」页的同一套回查逻辑，历史里已移动或改标签的本地文件不会按旧快照播放。
- **设置·播放的「睡眠定时」行脱离分组**：它渲染在 `.settings-items` 之外，于是不带分组的底板、宽度和分隔线，看着像另一个区块。移进组内后与其它行完全一致。
- **「下载设置」按钮带下划线**：它是 `RouterLink`，即真正的 `<a>`，被 UA 样式加了下划线而 `.btn` 从未复位（侧栏与音源页各自局部补过 `text-decoration: none`）。改为在 `.btn` 上一次性复位。

### 修复

- **播放页顶部的按钮真实点击没有反应**：「收起」以及播放页那组全屏/最小化/最大化/关闭此前只能靠脚本触发，鼠标点下去没有响应。根因是 `-webkit-app-region` 既不继承、也不会因为覆盖层画在上面就让下层让位——标题栏声明了整条顶部拖拽区，播放页是盖在它上面的 `position: fixed` 覆盖层，于是那 48px 内的真实输入全被当成窗口拖动。`no-drag` 只写在容器上不够，必须写在真正接收点击的元素上（标题栏之所以正常，正是它对 `button` 逐个声明过）。已用真实系统级点击验证：改动前点「收起」`nowPlaying` 仍为 true，改动后同一坐标即收起。
  - 这类缺陷自动化看不见：CDP 的 `Input.dispatchMouseEvent` 注入渲染进程、绕过浏览器的拖拽区处理，所以「元素在最上层、可命中、pointer-events 正常」全都成立，而人就是点不动。
- **同一栏目内下钻后「返回」会直接跳出该栏目**：专辑/艺术家详情与「音乐库」打开的文件夹此前是组件内的一个 `ref`，网格原地被替换、历史里不留痕迹；而标题栏的返回是真的 `router.back()`，于是从详情按一次返回会跳过网格、直接落到上一个栏目。现在这些层级写进查询串（`?album=` / `?artist=` / `?folder=`）成为真正的历史条目：详情 → 网格 → 上一栏目。顺带把内容区的 `:key` 从 `route.fullPath` 改为 `route.path`——否则改查询会重挂视图，筛选文字与滚动位置会在每次下钻时丢失；`route.path` 仍含具体参数，`/playlist/a` → `/playlist/b` 照常重挂（实测两个歌单连续切换，标题与内容都跟着换）。内容区不重挂后，`CollectionView`（读 `route.query.q`）与 `SearchView`（`watch` 查询）本就对查询响应，无需改动。
- **开着桌面歌词时，关闭主窗口会把应用变成一条没人能关的歌词条**：`window-all-closed` 要等**所有**窗口都没了才触发，而悬浮窗也是一个 `BrowserWindow`。于是在"关闭窗口时最小化到托盘"关着（默认）时点 ✕：主窗口被销毁，`window-all-closed` 因为悬浮窗还活着而不触发，托盘图标又只在托盘模式下才会创建——结果是进程仍在跑、桌面上只剩一条歌词，既没有窗口也没有任何入口。实测：主窗口 target 消失、只剩 `desktop-lyrics.html`、进程存活。现在主窗口的 `closed` 会顺带销毁悬浮窗，实测同一操作后进程直接退出。
  - 同时补上 `mainWindow` 此前从不置空：销毁后的窗口对象留着，`second-instance` 调 `isMinimized()`、IPC 来源校验读 `webContents` 都会抛「Object has been destroyed」。现在 `closed` 里置空，`second-instance` 与 `activate` 遇到没有可用主窗口的情况会重建窗口（`quitting` 中则忽略）。
  - 托盘模式不受影响：那条路径是隐藏而不是关闭，`closed` 不触发，所以收进托盘时歌词照常在桌面继续走（已实测）。
- **收进托盘后进度条与桌面歌词会冻住（音频还在放）**：播放位置是用 `requestAnimationFrame` 采样，而 Chromium 在窗口隐藏后就不再发帧。实测：可见时 5 秒推进 5.00 秒、歌词行 0→6；关窗到托盘后同样 5 秒推进 **0.00 秒**、歌词行不动，但频谱峰值仍是 174/175、`playing` 仍为 true——即声音正常，只有界面时钟停了。这恰好打在桌面歌词最典型的用法上：把播放器收起来、只留歌词。改为定时器采样（窗口隐藏时会被节流到约 1Hz，对只用来读位置足够）。
  - 实测修复后：隐藏期间 5 秒仍推进 5.00 秒；端到端验证"隐藏后歌词继续走"——索引 0→7，悬浮窗显示的文本与播放器当前行一致。
  - 回归中 `player-regression` 先红了 5 项：测试桩里 `globalThis.window` 是手搓对象，只补过 `requestAnimationFrame`。按同样的做法补上惰性的 `setInterval/clearInterval`（这些测试不断言进度循环，真跑起来反而会在用例结束后继续发 `progress`），并删掉已无人用的两个 rAF 桩。
- **曲库文件夹白名单可以由渲染进程自己扩大（任意文件读取的第二道门，比第一道更宽）**：`libraryFolders` 就是文件访问白名单本身（`jjmedia://`、歌词读取、标签写入都以它为根），但它有两道渲染进程可开的门。一是 `library.addFolder(path)` 直接收路径；二是更宽的 `settings:update`——它会把 patch 里点名的每个文件夹 `existsSync` 一下就 `addFolder()` 并扫描，等于完全绕开选择器还能对任意目录发起全盘扫描。修复前实测：把 `.cache/outside-lib` 写进去，该目录下的文件立刻可读。现在选择器改由主进程自己打开（与 `导入文件` 同一套做法），`addFolder` 桥面不再收路径；`settings:update` 则丢弃 `libraryFolders` 键。
  - 丢弃而不是报错：调用方一次性回写整套设置（恢复默认、测试收尾还原）不该因为顺带带了这个字段就整体失败；保证的是"白名单不可能经此改动"，不是"点名它就要受罚"。返回的 `after` 带的是真实列表，渲染进程的副本同一次调用就被纠正。
  - 移除文件夹仍走专用通道（它只会收窄，且对未知目录是空操作）。
  - 真实流程已用系统级点击走通：点「添加音乐文件夹」→ 原生「选择音乐文件夹」对话框打开 → 选定目录 → 该目录进入曲库与设置；随后用 `removeFolder` 移除，列表回到原样。
  - 回归：e2e 新增两条（经设置通道扩大白名单无效 / 同一次写入的其它设置仍然生效），UI 冒烟 1 项。原先那条 `reactive folder settings save through IPC` 是**恰好**借 `libraryFolders` 当载荷的，改用渲染进程确实拥有的列表字段来证明同一件事，另加一条断言白名单纹丝不动。
- **下载目录同样不再由渲染进程给路径**：`downloadFolder` 此前只过 `isAbsolute`，而它是下载音频的落盘位置、也是标签写入的输出目录，渲染进程却可以经 `settings:update` 直接写。现在选择目录的对话框由主进程打开、由主进程写入（与曲库文件夹同一套做法），`settings:update` 一并丢弃该键。
  - 真实流程用系统级点击走通：设置→下载 点「选择目录」→ 原生「选择下载目录」对话框 → 选定 `.cache\dl-test` → 主进程记录、`settings.json` 落盘、界面该行显示新目录、下载器实际使用的也是它，五项全过。
  - 渲染进程的设置副本经"空补丁回读"同步：走设置写入器而不是直接赋值，这样与仍在途的写不冲突（写入器只在没有写进行中时才从本地重新取基准，直接赋值会与在途批次互相覆盖）。
- **曲风 / 专辑 / 艺术家详情页没有「播放全部」**：这三处进入二级后只有一张曲目列表，想整张播放只能从第一首开始手动连着听——此前唯一的整播入口是卡片上的双击，而那个双击已被证实是死的（第一次点击就把网格换成了详情，第二次点不到卡片）。现在三个详情页表头都有「播放全部」，双击那条死代码一并移除。
- **「当前播放」从歌曲页表头改为右下角悬浮小按钮**：原来它是歌曲页表头里一个带文字的按钮，于是这个"找到正在播的那首"的动作只有歌曲页有，而且位置在已经滚过三千行的列表顶端之外。现在它是固定在右下角的圆形图标按钮，歌曲页、曲风、专辑、艺术家四处都有；后三处只在正在播放的曲目确实属于这一列表时出现（否则无行可滚）。歌曲页保留原有语义：正在播放的被筛选条件挡掉时，点定位会先清掉筛选再滚过去。
  - 定位走的是列表自己的 `reveal()`，会把该行滚到视口中央并标记选中。实测滚到列表末尾取那一行来播、再跳回顶部点定位：`scrollTop 0 → 242000`，该行落在可视区内并被标记选中。
- **歌词通道不再按渲染进程给的路径读盘（任意文件读取）**：`lyric.resolve` / `lyric.searchOnline` / `lyric.candidates` 此前接收整个 `LocalMusicInfo`，而服务会照着里面的 `path` 与 `lyricPath` 去读文件——`readSidecar` 又没有白名单、扩展名与大小限制。修复前实测：传一个 `{id:'x', lyricPath:'曲库外某文件'}` 就能把该文件内容当歌词取回（同一路径走 `lyric.readFile` 则被正确拒绝）。现在三条通道只收曲目 id，由主进程从自己的索引里取回权威记录（`indexedTrack()`），索引里没有就拒绝；渲染进程再递整个对象会被直接拒掉而不是照单执行。附带把 `readSidecar` 改成 `stat` + 4MB 上限（`readFile` 自身没有上限），同步的 `existsSync` 探测一并换成异步。
  - 索引只能由"扫描用户选定的文件夹"或"对话框选中的文件"写入，所以按 id 取回等于把权威交还给主进程。`resolveLocalLyric` 的注释写明了这条前置条件——它仍会读 `track.lyricPath`，该字段全仓库只有扫描器写、且取值恒等于 `sidecarPathFor(path)`。
  - 没有做"索引查不到就退回渲染进程给的路径"：值得放行的路径 `allowedMediaPath` 本来就放行，加这条退回等于把刚关的洞再开一次。
  - 回归：e2e 新增三条（索引外的 id 被拒 / 旧的整对象调用被拒 / 真实索引曲目仍能解析到歌词），离线套件新增"超大侧车被忽略"。实测播放取词、活动行跟随与三条通道的正常路径都不受影响。
- **已索引但所在文件夹已移出配置的歌曲，可以正常写标签与歌词了**：0.1.5 引入路径白名单时，`jjmedia://` 认曲库索引（索引里的路径放行），而标签/歌词/打开位置这几条 IPC 只看配置文件夹与对话框选中的文件，两者对同一个文件判定相反——歌能播，写标签却报「路径不在允许的媒体范围内」。现在这些通道采用与协议相同的权威，渲染进程仍然无法凭一个自造的 `path` 触达索引之外的文件（e2e 增补了这条反向断言）。
- **初始化超时的音源会永久留下一个子进程**：15 秒超时只把 `ready` 拒掉，而失败分支随后是把该音源标记为错误并停用，这条路径**不经过** `stop()`——于是没有任何人回收那个进程，它连同自己的内存上限一起留到应用退出；每个卡死的脚本都是一个泄漏。拆出 `teardown()` 供 `stop()` 与启动失败分支共用，它只处理进程与在途请求，**不删** `this.runtimes` 里的记录（音源管理页正是在启动失败时才去读那份日志）。
- **启动成功后才死掉的音源，界面永远显示「已启用」**：`scriptError` 在主进程没有订阅者，`getCrashReason()` 没有调用方，于是退出原因算出来就被丢掉。现在退出处理会写错误并回退启用开关（与启动失败分支一致），但仅当这次死亡不是 `stop()` 要的——这个意图现在在进入时就记录，而不是事后推断。顺带：`stop()` 此前对已经退出的子进程也要空等满 2 秒 SIGKILL 宽限，重启崩溃音源和退出应用（`before-quit` 等 `stopAll()`）都被拖慢；启动前校验的风险缓存无界，而它的键是整份编码后的脚本（实际样本约 740 KB 一条），改为 32 条并淘汰最旧。
- **取流地址与下载不再绕过 URL 守卫**：`isValidMusicUrl` 只判协议，脚本递回 `127.0.0.1:8080` 或云元数据地址应用照抓；下载器则用裸 `fetch` 抓脚本给的地址，而主进程其它出网路径都过守卫（逐跳重校验）。两处现在都走 `assertPublicHttpUrl` / `safeFetchResponse`。
- **桌面歌词可以拖出屏幕且再也抓不回来**：`place()`（创建与重开时）会钳制位置，`dragTo()` 不会。那条 820px 的歌词条能被拖到只剩几像素可见，本次会话内无法再抓取；因为 `place()` 也钳制，重开应用看着像"自己好了"。现在拖动同样按所在显示器工作区钳制，并且这两个通道除 `sender` 外还校验 `senderFrame`——沙箱 preload 会交给窗口内每一个 frame。
- **静音后音量条变成死控件**：工具栏把音量条渲染成 `muted ? 0 : volume`，所以静音时把手停在 0；往上拖会真的设音量却仍不解静音，于是把手弹回 0——看起来是控件坏了，而不是提示你静音键开着。现在把音量抬离 0 即解除静音（拖到 0 仍是静音，那不是"要声音"的请求）。
- **清空队列挡不住下次启动的整队恢复**：`clearQueue()` 调 `stop()`，而 `stop()` 第一件事就是写会话快照——它记下的正是刚被清掉的那一队。现在快照在停止之后被丢弃。顺带两件事：为这一队设的睡眠定时不再掐掉你之后新开的播放；`lastSession` 的"清空"必须是显式 `null`，因为设置写入器会把每个补丁经 JSON 快照一遍（为了剥掉 Vue 代理与调用方数组），而 `JSON.stringify` 会丢掉 `undefined`——写 `{lastSession: undefined}` 时它与 `stop()` 刚写的快照合进同一批，然后被快照覆盖。这条是测试抓到的，不是读代码读出来的。
- **曲库与歌单列表的旧响应会盖掉新响应**：`refreshSources` 有代际保护，`refreshLibrary` / `refreshPlaylists` 没有。扫描完成事件与用户操作同时索要同一份列表时，先发的后到就会赢，表现为"刚导入的歌不见了，下次刷新又回来"。歌单与收藏现在一次并发读取、一起赋值，中间不会再出现"新列表配旧收藏"。
- **专辑 / 艺术家页的筛选框对查询串失去反应**：把下钻层级写进查询串那次（`?album=` / `?artist=`）没有给这两页补 `route.query.q` 的监听，而 `:key` 又已改成 `route.path`（不再因查询变化重挂组件）。于是标题栏的前进/后退——正是靠查询变化来走层级——会让筛选文字停在旧值上。这是我自己在这一批修复里引入的回归，回环测试中由 `?q` 相关断言抓到。
- **换强调色时桌面歌词不跟着换**：悬浮窗的载荷里那句 `getComputedStyle(...)` 读的是文档上的 CSS 变量，但整个 computed 没有任何依赖会因强调色改变而重算——封面取色又是异步的。表现是改了颜色（或换到取色不同的专辑）后，歌词条停在旧色，最长要到下一句歌词才跟上。现在它依赖主题代码在颜色落地时递增的计数。
- **`dialog` 桥面整组已死**：`dialog.openFolder` 在两个目录选择器移进主进程后就没人调用，`openFiles` / `openLyric` 更早就没有调用方（`library.importFiles`、`lyric.importFile` 各自在主进程开框）。三条通道都留着 `rememberPicked()` 副作用——即渲染进程可以借原生对话框把任意路径送进媒体白名单——一并删掉。另外 `TitleBar` 注册的 `router.afterEach` 从不解绑（全局守卫活得比组件久），`TransportControls` 上那档没人用的 `size="lg"` 与 `hideMode` 也删了。
- **主窗口隐藏后定时器仍被节流**：进度时钟改成定时器之后，`backgroundThrottling` 仍为默认的 `true`，隐藏时会被压到约 1Hz（5 分钟后约 1 分钟一次）。现在显式关掉，位置读数不再依赖窗口是否在前台。

### 测试

- **UI 冒烟里「发现页不得出现 `.recent`」这条断言改为断言真实契约**：它写于最近播放整张列表被挪出发现页那次，判据是"发现页没有任何 `.recent` 标记"。发现页补回紧凑封面轨后它会误红。现改为"发现页不得内嵌历史 TrackList 行，且轨道要交棒给 /recent"——当初要防的是重复那份列表，不是这块数据本身。
- **桌面歌词新增离线与运行时两层覆盖**：`desktop-lyric.test.mjs` 测位置钳制（含多屏负坐标工作区）、静止位置、当前行取值与旧设置文件缺字段的合并；UI 冒烟新增 7 项，实测开关会开出/销毁第二个窗口、悬浮窗拿不到 `jj` 桥、显示的是播放器当前那一行、窗口透明而非灰块、字号与锁定状态会传过去。
- **`npm run test:e2e` 不再使用你正在使用的数据目录**：它此前直接对 `%APPDATA%\jj-music` 运行，并在结束时把开跑时抓到的**整份设置快照**写回去——期间应用自己写入的播放历史（`recentPlayed`、`lastSession`）会被回退，若你同时开着应用，新增的曲库文件夹也会被抹掉（曲库文件夹在 `settings.json` 与 `index.json` 两处各存一份，启动时按并集纠偏，所以两边同时为空时才会留下"歌还在、文件夹没了"的状态）。现在它与 UI 冒烟一样跑在副本 profile 上（`--keep-profile` 可保留），并新增一条断言实测真实目录未被写入；副本用完自动删除。冒烟脚本同样补上了清理——它每次会复制约 950 MB（含封面缓存），此前 7 次运行在 `.cache` 留下 6.6 GB。
- **联网搜索不再因偶发抖动听红**：酷狗会对正常关键词偶发返回空列表（6 次运行中 2 次，且 1.5 秒内的两次重试都空），QQ 端点偶发连接重置。这类上游抖动原先直接判为失败，现按 1.5 秒、6 秒的退避最多试三次，并在输出中标注。
- **`salt-ui-smoke` 依赖真实曲库数据**：其中 2 项检查需要多歌手、多曲目的曲库，profile 被清空成 1 首时必然失败（并在后续选择器上崩溃）。恢复曲库后 35 项全部通过。
- **钉住酷狗字面量解析器不变成 RCE**：那个端点返回的是 JS 对象字面量，解析等于执行来自网络的一段文本，而挡在它与主进程之间的只有沙箱对象的形状——`Object.create(null)` 没有可走的原型链；换成普通 `{}` 时它被 contextify 到宿主 realm 的 `Object.prototype` 上，`(function(){return this})().constructor.constructor` 就是主进程自己的 `Function`，响应体可以直接读 `process`。两种形状都实测过：空原型抛异常，`{}` 返回真实 pid。
  - 这条断言第一版**是空过的**：它写成了 `this.constructor(...)`（把 `Object` 当函数调用），拿到的是 TypeError 而不是宿主对象，于是把沙箱换成 `{}` 它照样绿。改法是把内置产物里的沙箱改弱后重跑，看着它变红（`sandbox leaked process.pid = 17004`）再恢复。
- **静音与清空队列补两条离线断言**：`player-regression` 新增"抬音量即出静音"和"清空队列丢掉续播点与睡眠定时"。睡眠定时那条不只看 `sleepAt` 标签被清空——定时器被惰化记录，断言的是 30 分钟那个回调确实不再挂着，否则只清标签也能过。两条都做了同样的变异验证：从构建产物里删掉解静音与 `cancelSleepTimer()` 调用后转红，恢复后转绿。
- **探针里两处把实现细节当契约的地方**：`verify-round13` 断言的是 `.transport--md` 这个类名，而"两栏控件尺寸一致"真正的意思是渲染出来的盒子相同——改为量取 `.icon-btn` 与播放键的实际尺寸和间距并比较（实测两栏都是 6×32px + 40px 圆盘、间距 9px）。`verify-round14`/两个截图脚本按 `.transport--md` 找播放模式按钮，随那档尺寸一起改掉。
  - 顺带记一次误判：这批探针一度集体报"页面切换后选择器为空"。原因是预览实例被前面某个脚本最小化进了后台——最小化的窗口只拿约 1fps 的帧，Vue 的过渡要等帧才推进，1.5 秒的 `sleep` 就不够了。把窗口还原后同一批全绿；这不是应用缺陷，但探针"睡固定时间"的写法在无人值守时会自己制造红。
- **审阅后确认不是缺陷的三条**：主进程文本解码固定按 UTF-8（实测 QQ 歌词、QQ 搜索、内置端点都回 `charset=utf-8`，而真正会遇 GBK 的音源侧 `lx.request` 早已用 iconv 按头解码）；`scanExtensions` 是写入但从不参与扫描的僵字段（UI 也没暴露它，不构成"设了没反应"的谎言）；`filesDropped` 的路径来自 preload 的 `webUtils.getPathForFile`（真实拖放），不是渲染进程自造的字符串。

### 发布与 CI

- **`SHA256SUMS.txt` 由 `npm run dist` 生成**：此前脚本只*报告*产物，校验文件每次手工拼——而 README 把它作为发行资产链接着，于是它可能悄悄指向已经不存在的文件（0.1.6 那份在仓库里就 `sha256sum -c` 不开）。现在按 `package.json` 的版本挑选 `.exe` / `.zip` 写哈希，**一个产物都没有时直接失败退出**；`release/` 里留着 0.1.2 到 0.1.6 的旧文件（还有两枚空格命名的 0.1.2 遗留），按版本筛选正是为了防止它们混进校验文件。
- **CI 现在会构建用户真正下载的那两个文件**：`packaged-sources` 作业此前只跑 `npm run pack`（产出 `win-unpacked`），而发行页上是 NSIS 安装包与 Portable ZIP——`nsis` / `zip` 配置坏掉、或校验文件指向不存在的文件，CI 都不会红。现在追加 `npm run dist`，再用 `sha256sum -c` 校验，并断言校验文件恰好两行、都属于本次版本。
- **`verify-package.mjs` 补上 `app.asar.unpacked`**：它只解 `app.asar` 找特性标记，而 0.1.1–0.1.5 那次故障恰好在这条视线之外——归档本身看起来完全正常，缺的是归档*旁边*的 `iconv-lite`（fork 出来的宿主以纯 Node 运行，没有 asar 支持）。现在直接断言宿主脚本与两个依赖包在磁盘上存在，另加一条桌面歌词标记。

## 0.1.6 — 2026-09-19

热修复：已发布的 Windows 包在干净电脑上启动不了任何音源。

### 修复

- **打包版音源不再全数启动失败**：音源子进程在加载自己的依赖时就中止（`ERR_MODULE_NOT_FOUND`），界面表现为「音源进程退出（退出码 1）」，任何脚本都跑不起来。根因是宿主 `source-host.ts` 顶层 `import iconv from 'iconv-lite'`，而 `asarUnpack` 只解出了宿主文件本身。子进程以 `ELECTRON_RUN_AS_NODE` 运行，是纯 Node、不认 asar，于是沿宿主真实路径逐级向上找 `node_modules`——而那个依赖还封在 `app.asar` 里面。现已把 `iconv-lite` 与它自己的依赖 `safer-buffer` 一并解包。
- **影响范围：0.1.1 / 0.1.2 / 0.1.3 / 0.1.5 的全部 Windows 包**。这三行的配置与宿主 import 逐版相同，所以从 0.1.1 起就这样。它也解释了此前那份「另一台电脑音源开不起来」的报告：本机一切正常，是因为 `release/win-unpacked` 位于仓库之内，祖先目录里的开发用 `node_modules` 悄悄补上了这个依赖。
- 实测数据：从 GitHub 取回的 0.1.5 免安装包，21 个脚本一个都起不来；只在解包目录旁放上这两个包、其余字节完全不动，`tx, wy, kw, kg, mg` 立即全部上报。修复后的 0.1.6 包在仓库外运行时，20 个采样中 19 个启用成功、上报 7 个平台、并解析到一个真实的在线播放地址（第 20 个是被启动前校验拦下的 `独家音源`，属预期）。

### 测试

- **打包版检查改为在仓库之外运行**：`clean-profile-sources` 现在先把应用复制到临时目录再启动，只有确认祖先目录里没有开发用 `node_modules` 时才就地运行（`--no-isolate` 可关）。此前这条检查——连同 CI 的 `packaged-sources` 作业——永远在仓库内跑，对本次这类缺陷完全免疫；合成音源也测不出来，因为崩溃发生在宿主的 import 阶段，早于任何脚本代码。加固后的检查经反向验证：把有缺陷的 0.1.5 布局放进仓库内跑，仍报 3 项失败并提示依赖解析问题。
- **检查结论更贴近构建本身**：被启动前校验主动拦下的音源、以及每个配置最多 20 个音源的产品上限，不再计为构建失败；子进程报 `ERR_MODULE_NOT_FOUND` 时会打印定位提示。

## 0.1.5 — 2026-09-19

咪咕在线音源、访问边界加固、启动失败可见化、打包版音源门禁，以及一批测试与诊断工具的修复。

### 功能

- **咪咕音乐在线搜索与歌词**：全局搜索新增咪咕平台，在线歌词覆盖扩展到 QQ、网易云、酷我、咪咕四家。适配器使用咪咕网页播放器当前调用的接口（无需签名与鉴权）；此前广为流传的移动端接口已经废弃，只会拿到重定向后的 HTML。咪咕的搜索响应本身就携带歌词地址，因此歌词不需要第二次查询。
- **咪咕平台抽测恢复**：平台验证靠宿主搜索取测试曲目，而咪咕此前没有搜索适配器，只能一直显示「无法取得该平台的测试歌曲」。现在能真实解析地址并读到音频数据。
- **全局搜索的平台标签改由主进程上报**：标签列表原先是一份手写副本，与适配器列表各自漂移；现在直接取主进程报告的适配器，以后新增平台无需改动界面即可出现。

### 移除

- **副歌识别**：歌词重复段落的副歌推断、进度条上的副歌高亮色带与「跳到副歌」入口一并删除。说明开销量级以免误读：检测挂在派生值上，只在歌词变化时算一次（300–1000 行实测 6.6–15.1 ms），并非常驻负担；移除是因为价值不足以抵偿维护成本，而不是因为拖慢了播放器。附带消除了 `LyricLine` 的重复声明及其毫秒/秒单位换算。

### 安全加固

- **本地文件访问统一收口**：歌词读写、标签写入与「打开文件所在位置」此前接受任意绝对路径，现在与 `jjmedia://` 共用同一套曲库白名单——解析符号链接后再判定边界，用户通过对话框主动选中的文件单独放行并设有数量上限。
- **出网地址受限**：封面抓取与平台抽测不再接受任意地址。拒绝非 http(s)、携带凭据、以及回环 / 私网 / 链路本地 / 元数据端点；重定向逐跳重新校验（公开主机的一次跳转即可绕过只看初始地址的检查）；响应体施加大小上限。
- **酷我搜索改用 HTTPS**：其响应内容会被求值解析，明文传输意味着链路中间人可注入可执行代码。实测两种协议返回逐字节一致。

### 修复

- **启动失败不再静默**：初始化抛错过去会留下一个没有窗口、没有托盘、也没有任何提示的进程，与"应用打不开"无从区分。现在写出 `<数据目录>/startup-error.log`（含堆栈）并弹出错误对话框后退出。

### 其他

- **新增 CI**：向 main 推送或提交 PR 时，在 Windows runner 上执行类型检查、生产构建与全部无头套件。此前 README 与 docs 宣称的各项验证结果全部依赖单机手工执行。
- **二进制镜像迁移**：Electron 与 electron-builder 的下载镜像从 `.npmrc` 改到环境变量（`ELECTRON_MIRROR`、`ELECTRON_BUILDER_BINARIES_MIRROR`）。npm 11 已在警告将不再向生命周期脚本透传未知配置键；迁移后仓库默认从官方源获取，需要镜像的机器自行设置。
- **`npm run diagnose` 恢复可用**：该脚本因编码损坏存在语法错误而无法启动；修复后又暴露出它导入音源后从不启用（新版导入默认停用），于是一个脚本都没启动、反倒把空结果报成 5 个引擎故障。
- **UI 冒烟检查恢复完整**：自 0.1.3 播放页底栏重构后，脚本点击的选择器所指的 class 已不存在，导致其后 **13 项** 检查从未执行——含 EQ 生效与持久化、播放列表切歌、最小窗口抽屉、内置收藏无删除按钮、设置突发写入。现已全部执行并通过。
- **端到端断言纠正**：「平台必须全部属于 LX 固定五家」的检查与产品自身的类型定义和测试相互矛盾（音源脚本声明私有平台是有意支持的行为），改为校验 `normaliseSources` 实际保证的性质。
- **无头套件构成变化**：新增 URL 守卫与路径作用域两组离线断言，移除副歌套件，共 17 个套件。

## 0.1.3 — 2026-09-18

下边栏统一、播放体验与在线播放修复。

### 界面

- **下边栏统一**：主界面与播放页的下边栏由同一个组件渲染——收藏（❤）移入控制按钮组最左侧，播放队列按钮移入最右侧，两处按钮序列完全一致，仅尺寸与封面不同。
- **播放页布局**：控制与进度移到底部独立控制栏；左侧只保留大封面；封面进入播放页时有自下而上的飞入过渡。
- 播放页歌名移入底栏（突出显示），艺术家 / 专辑 / 音质作为次要信息随行展示。
- 设置 → 外观 → 渲染 新增「界面文字大小」（85%–150%，与窗口缩放独立，默认不变）。

### 主界面

- 侧边栏移除「文件夹」入口：文件夹浏览并入音乐库页（点击文件夹即可查看其中曲目），`/folders` 旧链接自动跳转。
- 「歌曲」与「播放队列」页新增「当前播放」按钮，可定位并选中正在播放的曲目（曲库中被筛选隐藏时会先清除筛选）。
- **记住上次播放**：关闭应用时的曲目、队列与进度会被记录，下次打开自动恢复到上次位置（不自动出声，按播放继续）。

### 播放页

- **修复歌词无法滚动**：歌词列高度不再被布局压缩，上下留白改用随容器高度解析的占位元素——此前固定 `40vh` 内边距在窗口较矮时会让歌词区完全无法滚动。
- 歌词行距收紧（默认约为原来的 70%），并在 设置 → 外观 → 歌词 新增「行距」调节（1.2×–2.6×）。
- 歌词中重复段落自动识别为副歌，进度条上以高亮带标注，点击直接跳到副歌。

### 修复

- **修复在线播放报「不支持的音频格式或地址不可用」**：部分音乐 CDN 的响应自带跨域许可头，播放器此前会再追加一个，导致头值重复而被浏览器拒绝。现在仅在响应缺失时补充。
- 修复恢复上次播放时进度被归零的竞态（恢复位置现在随音频加载直接应用）。
- 修复初始化完成后崩溃的错误被吞掉、进程变僵尸的问题。

### 性能

- 副歌识别速度提升：500 行歌词从约 143ms 降至约 9ms（长歌词不再阻塞界面）。

### 其他

- 移除从未生效的「桌面歌词」设置数据项（功能本身在后续路线中）。

## 0.1.2 — 2026-09-18

音源安全与稳定性更新：新增启动前校验与隔离，修复音源启动失败、多音源可用性和播放器交互问题。

### 音源安全

- **启动前校验**：启动音源前执行静态检查，按「阻断 / 提示 / 说明」分级。阻断项包括系统关机与重启命令、Win32 关机 API、执行外部程序、加载 `child_process`、终止自身进程、访问原生绑定、电源控制调用。
- **组合特征拦截**：对「服务端授权下发 + 代码加密不可审阅」这一组合特征的脚本直接拒绝启动。该特征来自一次实测事故：某音源在启动时调用了系统关机命令。
- **隔离机制**：校验未通过的音源会被自动停用并持久隔离，重启后不会重新启用；需在界面显式「解除隔离」才能恢复，且解除后仍需单独启用。
- **导入默认停用**：新导入的音源默认不启动，避免导入即运行。
- 支持批量「全部启动 / 全部关闭」，每个音源仍单独执行校验。

### 音源可用性

- 修复部分音源初始化失败的问题：运行环境补齐 `require` / `__filename` / `__dirname` / `module` 等 CommonJS 全局对象。带环境自检的脚本此前会因此静默退出。
- 多个音源支持同一平台时，按启用顺序依次尝试，某个音源失败会自动切换下一个。
- 音源启动失败时，界面开关会正确回退为停用状态。
- 修正 `musicInfo.id` 缺失导致的取流失败（已安装音源中有 11 个会读取该字段）。
- 保留脚本声明的非标准平台标识，不再被静默丢弃。
- 收窄「执行外部程序」校验规则，修复其误拦正常音源的问题。

### 播放器

- 播放页音量改为竖向推子，悬停扬声器图标弹出，支持点击、拖动、滚轮调节；主界面为常显横条加数值。
- 播放页右上角补充最小化 / 最大化 / 关闭按钮。
- 修复重新进入播放页时歌词先跳到 0 秒再滚回播放位置的问题。
- 固定底部播放控件位置，修复歌词长度变化导致控件左右移动、以及控件点击无效误触发进入播放页的问题。
- 在线取流增加超时与卡顿检测，失败时自动尝试其他音源。

### 说明

- 本版本曾实现基于受限令牌的启动隔离（剥离系统关机特权），因实测使用体验不佳已停用，实现保留但默认关闭。当前音源安全依赖启动前校验与隔离机制。
- 已知限制：静态校验无法读取加密脚本中的字符串，因此「校验通过」不等于绝对安全，请只使用可信来源的音源。

## 0.1.1 — 2026-09-17

首次完整发布：本地曲库、在线搜索与播放、音源管理、下载、歌词与公开歌单导入。

### 功能

- 主界面、播放界面、设置页面及多级菜单；发现音乐与最近播放。
- 分类、歌单和队列数量；在线平台自动抽测状态。
- 本地优先全局搜索、播放页面 EQ 与播放列表。
- 在线歌曲按音质下载、批量任务、LRC、MP3 / FLAC 歌词及封面嵌入。
- 网易云、QQ、酷狗、酷我公开歌单预览和导入。
- Windows x64 安装版、完整免安装 ZIP。

### 终版修复

- 修正 MPEG-4 / M4A 与 ADTS 文件格式识别。
- 为不支持硬链接的磁盘提供下载保存回退，同时避免覆盖同名文件。
- 退出时中止下载调度、清理临时文件并等待数据保存。
- 修复同路由切换搜索词不刷新、下载取消提交竞态及重试遗留路径。
- 校正标签写入能力，拒绝损坏 FLAC 或备份失败时修改原文件。
- 为封面和歌单响应增加读取大小限制，保持完整响应超时约束。
- 版本号由构建配置统一提供，打包前移除测试环境开关。

验证：14 个离线测试套件、30 项播放端到端检查、40 项实际打包程序检查通过。在线服务状态可能随平台及网络变化。
