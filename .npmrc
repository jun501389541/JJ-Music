# Project-local npm configuration.
#
# The npm cache is kept inside the workspace so installs work in sandboxed
# environments where the global %LOCALAPPDATA%\npm-cache is not writable.
cache=.cache/npm

# Electron's prebuilt binaries are fetched from a mirror that is reachable
# without a proxy. If you have direct GitHub access you can remove this and
# re-run `node node_modules/electron/install.js`.
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
