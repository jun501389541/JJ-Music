# 更新日志

## 未发布

### 功能

- **数据不再固定住在 C 盘**（需求 2）：新增 `src/main/data-location.ts`，把"数据放哪"从一个写死的 `app.getPath('userData')` 变成一条有优先级的规则——命令行 `--user-data-dir` > 用户迁移过的记录（exe 旁 `data-location.json`）> **exe 同级的 `data/`**（便携版，或程序目录可写的安装版）> `%APPDATA%/jj-music`。便携版首次启动会把旧目录**复制**过来（原目录保留；`Cache`/`GPUCache`/`Local Storage` 等可再生数据不复制，免得为一次搬家先卡住几秒）。设置 → App 数据 现在显示当前目录与判定来源，并提供「迁移到其它目录」：选择框与复制都在主进程完成（这条路径决定应用所有文件往哪写，不能由渲染层报一个路径过来），迁移后需重启生效。因为 `userData` 是在 `app.ready` 之前定的，Chromium 自己的缓存也一起跟着走。
  - 边界：装在 `%LOCALAPPDATA%\Programs` 的安装版默认仍在 C 盘（那个目录本身就在 C 盘），要靠「迁移到其它目录」或改用 ZIP 便携版解决。开发态绝不写到 `node_modules/electron/dist` 旁边——这条有单测钉住，把 `packaged` 判定去掉测试立刻转红。
- **曲库支持移除歌曲**（需求 12）：曲目菜单新增「从曲库移除」，支持多选批量。**只删索引条目，绝不动磁盘上的文件**——按确认过的语义做：移除后重新扫描所在文件夹就会再收录，因此不需要一份"我到底隐藏了哪些歌"的名单要维护。有一个必须知道的边界：按单个文件导入、或文件已不在任何注册文件夹里的曲目，移除后重扫**不会**回来，只能重新导入该文件（第一次跑验证就撞上这个，看着像 bug，其实是设计）。确认框里写明了"音乐文件保留在原位置"。
- **歌单封面**（需求 8）：`Playlist` 增加 `coverPath`，图片经主进程选择框挑完后按内容哈希存进与曲库封面同一个目录，于是 `jjmedia://` 原有的白名单直接能用，不必为歌单再开一条口子。详情页与歌单网格都会显示，没有封面时仍是原来的音符图标；支持更换与移除。
- **搜索支持拼音**（需求 11）：本地曲库索引为每首曲目预存全拼与首字母，`yydgx` 能找到《爷爷的故乡》，`yeyedeguxiang` 也可以；艺术家字段同样参与匹配，所以 `zjl` 能列出周杰伦的歌。搜索框同时新增输入联想下拉（本地曲库，最多 8 条，上下键与点击都能选）。实现上放弃了 `Intl.Collator('zh-u-co-pinyin')` 方案——实测它把"晴天"算成 `rv`、"偏爱"算成 `qa`，不可靠；改为打包 `pinyin-pro` 的词典（devDependency，被 Vite 打进渲染层，不是运行时依赖，因此不影响 asarUnpack 与安装包）。代价是渲染层主包 523 KB → 956 KB。
- **音源支持从网址导入**（需求 14）：粘贴一个 http(s) 链接即可拉取脚本。抓取走既有的逐跳 SSRF 防护（`safeFetchBytes`：每一跳重新校验、拒绝内网与云元数据地址、4MB 上限），落库后仍默认停用并照常过启动前校验。顺带把原先在"粘贴 / 选文件 / 拖入"三处各写一遍的导入校验收敛成一个 `importScript()`，避免哪天只改了一处的闸门。
- **下载支持断点续传**（需求 4）：中断的下载不再从 0 重来。`.part` 文件在失败/取消后保留（成功落盘才删），重试时带 `Range: bytes=<已写字节>-` 续传。关键是**不能只相信服务器会理 Range**：用 `If-Range` 附上首次的 `ETag`/`Last-Modified`，只有 `206` 才当续传处理，返回 `200`（忽略 Range 的服务器）就截断文件重下——否则两份内容拼在一起是个"能播放但开头多了一截"的坏文件。续传时 `content-length` 只算剩余部分，进度总长改从 `content-range` 的尾巴取。此外新增完成度校验：字节数不足 `total` 时不再谎报完成，而是提示「下载未完成（x/y），重试将从断点继续」。上次退出时中断的任务，重启后的提示语也改成同样的意思。
- **网络导入支持咪咕，并同步封面、导入前可编辑**（需求 9）：
  - 咪咕走它自己的公开端点（歌单头 `resource/playlist/v2.0` 出名称与封面，`.../playlist/song/v2.0` 按 50 首一页翻到 `totalCount`），主机名是 `app.c.nf.migu.cn` 而非搜索用的 `app.u.nf.migu.cn`。歌曲行的 schema 与搜索完全一致，所以直接复用 `miguSongToInfo`，音质梯度、`copyrightId`、歌词直链一次到位，不再第二处猜字段。把写错主机名这件事钉在单测里（逐条断言请求 URL 的 host，改掉就红）。
  - 五家平台的封面都抓：网易云 `coverUrl`、QQ `imgurl`、酷狗 `{size}` 模板（填成 600）、酷我与咪咕的裸路径（补各自的图床域名），规范化后随预览一起返回，保存时经逐跳 SSRF 守卫下载、按 `content-type` 而不是 URL 后缀定格式、以内容哈希存进与曲目封面同一目录，于是 `jjmedia://` 白名单原样可用。抓封面失败只提示「封面获取失败」，不会让一百多首歌跟着丢。
  - 预览区从只读的 `<ol>` 换成共享的歌曲列表：可拖动排序、勾选后批量「从待导入列表移除」、点「恢复全部」找回。保存时渲染层只提交曲目 id 的顺序与子集，歌名/歌手/音质仍以主进程缓存的预览为准——未知 id 与重复 id 直接丢弃，所以伪造或过期的列表变不出新歌。
- **艺术家用网络头像，不再拿专辑封面顶替**（需求 10）：新增 `src/main/online/artist-image.ts`，按曲库里的歌手名去平台找头像。走的是"歌曲搜索行里本来就带的歌手标识"，不需要额外的搜索端点：咪咕行里有 `singerList[].img`，QQ 行里有 `singer[].mid`（拼成公开的 `T001R300x300M000<mid>.jpg`），网易云要先拿 `artists[].id` 再问一次 `api/artist/{id}`（它的不带后缀 `picUrl` 是 1.4MB 原图，所以强制加 `?param=320y320`），酷狗要 `SingerId` → `singer/info` 填 `{size}` 模板。酷我两条路都不通（搜索行不给艺术家图，艺人接口要签名），不参与。四家各自 `try`：本机实测 QQ 会连接超时，共用一个 signal 时它会替所有人把结果变成"没有头像"。抓回来的图经逐跳 SSRF 守卫下载、按内容哈希存进封面目录，于是渲染层看到的是 `jjmedia://` 而不是平台直链——离线可用，也不受各家防盗链规则影响。
  - 取用是懒的：艺术家卡进入视口才去查（`IntersectionObserver`），同时最多两桩，未命中的名字连同"确实没有"这个结论一起记进 `artist-images.json`，下次进这页一条请求都不发。实测本机 471 位艺术家：首屏只查 7 位、拿到 18 张头像，滚到底变成 33 位，重进这页不再新增请求。
  - 边界：没查到头像时仍显示该歌手某张专辑封面（而不是留白），因为本地曲库里有大量这种名字——现场录音、拼写奇怪的翻唱者、`G.E.M.邓紫棋` 这类带前后缀的标签。要 stricter 的话说一声，去掉这层回退只是一行。
- **在线歌词可以在几种来源之间切换**（需求 3）：以前在线播放的歌词只有一条固定链路（先问音源脚本，空了就问该平台接口），现在把它变成三处可选：`音源脚本` / `平台接口` / `联网匹配其他平台`（第三种是新增的：拿歌名与歌手去其余平台搜索，过 0.6 的置信度才采用，并排除曲目自己的平台）。设置·歌词 里选首选来源，并可关掉"其他来源兜底"——关掉就真的只问那一家，拿不到词就是拿不到，这一点有实测钉住。播放页 歌词 菜单还能只对**当前这首**指定来源（切歌即失效），因为"这句词不对"和"我永久改主意"是两件事。徽标不再只写「在线」，而是写「在线 · 平台接口」这样的具体来源：要知道该换成哪个，得先知道现在用的是哪个。下载时嵌词走的是同一条解析入口，所以设置一改，两边一起变。
- **封面与歌词现在记的是"在哪来的"，不是"有没有"**：索引里原先存三个字段描述同一件事——`lyricPath`（字符串，可能是空）、`hasEmbeddedLyric`、`hasSyncedLyric`。它们能回答"这首歌有没有词"，回答不了"这条词是从哪来的、下次该先找谁"。而封面和歌词恰好各有五种来源：音频文件自己的标签（内嵌）、同目录的 `.lrc`（sidecar）、应用抓下来存在数据目录的副本（缓存）、平台接口直接给的地址（远程）、用户手工挑的那一份（用户）。改成一张 `assets` 表：每首曲目按 `cover` 与 `lyrics.main / lyrics.translation / lyrics.pronunciation`（三个槽位对齐 OpenSubsonic 的主词/译词/发音词）各记一条**有序链**，链上每一环写明 `origin`、来源平台、是否带时间轴、记录时间。
  - 于是解析链、界面标签与后续的"导出到文件"用的是同一份数据，不再各推各的：设置·歌词里选的首选来源会真正排进链里；音轨信息新增「封面来源」「歌词来源」两行，播放页徽标把这条记录念出来（「同目录文件 → 文件内嵌（带时间轴）」这样的链，而不只是一个对勾）。
  - 这一步**只改记录与读取，不改任何写盘行为**：标签写入仍然只在用户明确点「写入标签」时发生，歌词仍然优先落在同目录 `.lrc`。
  - `INDEX_VERSION` 3 → 4，因此首次启动会自动在后台重读一遍曲库补上这张表（重读只跳过大小与修改时间都没变且**字段齐全**的条目；不需要用户点按钮）。实测 3368 首：升版后曲目一条不少，3336 首记着封面来源、3327 首记着歌词链，153 首有同名 `.lrc` 的曲目 sidecar 全部排在链首，旧的三个字段零残留。
  - 一个自己踩出来的回归：最初想让解析链"完全按记录走"，结果弄坏了"扫描之后才放进来的 `.lrc` 也要赢"——sidecar 是索引之外的文件，增量扫描看不见它（内嵌标签则不同，改标签必然动到音频文件，扫描看得到）。所以 sidecar 这一环始终实时探测、记录只补充非常规文件名，测试里钉住了这条优先级。
- **写封面与歌词只剩一个动作，落点变成一条设置**（第 2 步）：以前四条路径各写各的——歌词编辑器写 `.lrc`、候选歌词写 `.lrc`、标签匹配走 `writeTags`、下载按自己那两个开关两头写。四处的"该写到哪里"是四次独立判断，于是同一个"保存歌词"从不同菜单项进去会留下不同的来源记录。现在都走 `asset-export.ts` 的 `exportAssets()`：目标为 `文件标签 / 同名文件 / 两者都写`（设置·标签与文件），返回体说清"写到了哪几处"，并把来源链交回索引。四条路径各自的语义仍然保留：
  - 自己挑的歌词（候选、编辑、选文件、拖进来 `.lrc`）**一定**带上同名文件这一处。解析优先级里同名文件赢，所以只写标签的话它可能被用户很久前放的旧 `.lrc` 盖住——看起来就是"点了没反应"。
  - 下载走暂存文件名，所以那一趟**禁止**写同名文件：否则 `.jj-<id>.lrc` 会长在临时目录里。歌词等到定名之后再按 `不覆盖已有` 落盘——自动抓的东西不能吃用户手写的 `.lrc`（Picard 对 `cover.jpg` 也是同一态度）。
  - 格式不能改写的（OGG、M4A、WAV……）自动退到同名文件，并且提醒里说清是为什么；只有标题/艺术家这类文本字段没有别处可去，于是它说"标题等文本字段未写入"而不是什么都不说。
  - 顺手清了四处对不上的地方：① `tag-writer.ts` 的文件头声明"FLAC / OGG 都实现了"，但 `canWriteTags` 只认 MP3/FLAC——OGG 的 Vorbis 注释装在 Ogg 页里，改一次要重切包并逐页重算 CRC，没做也不该嘴上说做了；② 扫描器现在**读**封面同名文件（`<歌名>.jpg` 优先，再 `cover/folder/front/albumcover/album`），没有内嵌封面的文件不再一律显示占位音符；③ MP3 写歌词时同时写 `USLT`，FLAC 同时写 `LYRICS` 与 `UNSYNCEDLYRICS`（Audacious、Poweramp 只认后者）；④ MP3 **不写 `SYLT`**：实测 `node-id3` 的 `update()` 对数组型帧是**追加**而不是替换（写第二次会留下两份时间轴帧，读到的还是旧的那份），而空数组也删不掉——LRC 文本本身就带 `[mm:ss]`，这个应用和多数播放器都能从纯文本里读时间轴，所以那个帧买不到任何东西，只会留下越写越长的标签和顺序错乱的旧词。
- **联网拿到的歌词先进"待写入"，用户点头才落盘**（第 3 步）：播放一首本地歌时联网补到的词，过去只是显示一下就丢了；现在它进 `<数据目录>/library/pending-assets.json`，播放页徽标与音轨信息都会写「在线获取·联网匹配其他平台（待写入）」，设置·标签与文件 有一行说清现在等着的是哪些、并给「全部写入 / 全部丢弃」，歌曲菜单（含多选批量）则能逐首写。为什么不直接写：播放一首歌不等于同意修改这首歌的文件。音乐标签软件几乎都停在"暂存—显式保存"这一步（Picard 未点保存不改文件、Kid3"直到 Save 命令前文件不变"、Yate 可回到上次保存），只有网易云/QQ 这类云端播放器会静默写盘，而那正是它们被诟病的地方。
  - 队列刻意**不进索引**：否则每播一首没有歌词的歌都要重写一次整份 `index.json`。徽标的「待写入」来自解析结果，界面要列表时直接问队列。
  - 新增设置「可改写的格式」（默认 MP3 + FLAC）。这个开关只能**收窄** `tag-writer` 真能写的集合，交集在写入时取——所以渲染层即使把 `.wav` 写进去也不可能让应用去改一个没有经过验证的容器；测试钉住了这条方向。
  - `INDEX_VERSION` 4 → 5：封面同名文件是新读出来的字段，老索引里不会有它，靠启动时那次后台重扫补齐。

### 修复

- **写完歌词之后，待写入那条还留在队列里**：第一次跑第 3 步的真实验证时抓到的——用歌曲菜单「写入封面与歌词」写完，设置页仍显示它有东西待写入，再点「全部写入」会把同一条词写第二遍（多出一个备份、白改一次文件）。现在写入成功后会按"这次真的带了歌词/封面"清掉对应条目；清完为空时两个动作按钮自动禁用。
- **第二次读同一首歌，徽标就不说"待写入"了**：解析服务缓存的是**加装饰之前**的结果，所以联网取词当下徽标正确，再进一次播放页（走缓存）就变成一条普通的「在线获取」。现在入队这一步会把带「待写入」的结果放回缓存，让界面每次读到的是同一句实话——写完之后再解析会从同名文件/标签拿到新结果，标记自然消失。
- **预览标签匹配时把"将要写入"写成"已写入"**：`dryRun` 走的是同一条返回路径，措辞却只有过去时。现在按 `dryRun` 分别写「将写入 …」「已写入 …」，两处都列出实际落点。
- **清空封面目录后，曲库会自己长回来而不是留一屏破图**：封面是索引之外的派生文件，删掉它不会改动音频的大小与修改时间，而增量扫描恰恰按这两项跳过文件——所以"删封面腾地方"之后重扫永远看不见这件事；同时索引里仍写着指向已消失文件的 `coverPath`，列表画出来的是浏览器那枚破图图标，而不是应用自己的占位音符。现在 `MusicLibrary.load()` 会把这种死引用抹掉并把索引标成待刷新，于是启动时那次后台扫描顺手把封面重新抽出来，用户不需要点任何按钮。歌单封面（`playlists.json`）与艺术家头像记录（`artist-images.json`）同样按"文件不在就当没这回事"处理：前者回落到音符占位，后者下次进这页重新解析。三条都有回归测试；实测把封面清空后启动，首屏 17 行全是占位图标、破图 0 个，应用自己在后台抽回 1390 张封面后正常显示。
- **便携版在「迁移目标那块盘没插」时不再回 C 盘**：`resolveDataDir` 里，指针记录的目录今天不存在时一律退回 `%APPDATA%/jj-music`——于是装在移动硬盘里的便携版换台机器插不上，就悄悄在 C 盘另起一份库，正是需求 2 要消灭的行为。现在改成先问"能不能退回程序旁边"：便携版回 `<exe>/data`（迁移是复制不是搬家，那里本来就有上次留下的副本），安装版没有可写的程序目录才回系统应用数据目录；两种情况都在提醒里写清本次用的是哪一个目录。这条是端到端驱动「迁移到其它目录」按钮时测出来的——之前只分别验过指针解析、复制过滤和按钮禁用态，没走过完整的点击链。
- **全局搜索的平台页签不再混入本地结果**：结果列表此前无条件等于「本地命中 + 在线命中」，所以切到酷狗/QQ 这类单个平台页签时，本地歌曲仍然排在最前面，且计数行照打「本地 N 首」——看起来就像页签筛选没生效。现在具体平台页签只列该平台的结果，`全部` 与 `本地音乐` 才列本地命中。另外顶部的「本地歌曲优先显示」这句其实是误导：设置里那个开关是 `优先播放本地文件`，只决定播放时选哪个文件，从不参与列表排序，措辞已改为「切换平台可只看该来源的结果」。
- **拖入文件夹不再被静默丢弃**：主进程按扩展名给拖入项分类，目录没有扩展名，于是直接落进 `skipped`，用户看到的是「没有可导入的文件」。现在目录走曲库文件夹那条路（复用已有的递归 `walk()`，夹内音频按标签入库），并在主进程弹一个原生确认框——加入曲库等于把整棵子树交给 `jjmedia://`、歌词读取与标签写入，比单个文件的授权范围大得多，而路径是从渲染层来的，所以要由用户亲自点一次确认。取消时不报错，也不再显示成"导入失败"。
- **随机播放改为「洗一整轮再重洗」**：此前每次切歌都重掷一次随机数、只排除当前曲，短队列会在几秒内重复同一首，而排在后面的歌整个会话都可能听不到；同时 `上一首` 走的是同一个分支、忽略方向，等于向后跳随机曲，听过的顺序找不回来。现在按洗牌袋（Fisher-Yates）一轮一轮播，两轮相接处不重复，并记录抽取顺序，`上一首` 回到刚才那首、再按一次继续往前退。队列由其他页面增删时按曲目 id 跳过失效项。
- **桌面歌词悬浮窗的拖动**：原来列了五处，接上第二块屏之后又测出两处更深的（⑥⑦），一起修。① 每个 `pointermove` 都同步发一次 IPC 并 `setPosition`——现在页面只在按下与松开时各发一条，中间由主进程按帧跟随系统光标：一次 3 秒拖动从 304 次 IPC 降到 2 次。② 按住鼠标时呼出右键菜单或窗口失焦，`pointerup` 永远不会来，指针捕获被悄悄拿走，松手后歌词条还在跟着鼠标跑——现在 `blur` 与 `lostpointercapture` 都会收尾（主进程随之停止轮询）。③ 目标位置被夹在「窗口当前所在那块屏」的工作区内，于是永远拖不到第二块显示器上；一块窄于 820px 的屏幕上横向坐标还会直接冻住——改为对全部显示器工作区的并集取夹。实测（主屏 200% + 副屏 100%）：把记忆位置推到极远，窗口落在 (2062, 2528)，正是副屏的右下角；旧代码会把它夹回主屏。④ 重新打开时，上次拖到的位置按「新建窗口此刻所在的那块屏」夹，而新窗口还在系统默认位置，结果停在第二块屏上的位置每次启动都被拽回主屏，看着像位置没保存——改为对并集校验。实测副屏上的 (1500, 2000) 关掉再开仍然精确回到 (1500, 2000)。⑤ 锁定位置后拖动路径不做任何检查——渲染层与主进程各设一道闸。实测锁定后真实鼠标拖一次：窗口不动，且页面收到的 pointermove 为 0（鼠标穿透本身就挡住了，主进程那道是纵深防御）。⑥ **并集是个矩形，而两块错位的屏会在矩形里留下不属于任何屏幕的 L 形空隙**——拖进那条空隙，歌词条就凭空消失了，而且位置已经落盘，重启还是看不见。现在要求窗口中心必须落在某块真实屏幕上，否则贴到最近的那块（宁可丢掉用户拖的坐标）。⑦ **混合 DPI 的接缝会让 Electron 自己的坐标发生跳变**：光标竖直向下拖过 200%/100% 两屏的交界时，窗口横向平移了 544 DIP、纵向反向退了 22px，而光标 x 全程没动。第一版改成"页面发位移、主进程加到位移基准上"并没有治好，第二版改成"主进程轮询系统光标"也仍然跳——根因不在页面坐标也不在窗口换屏，而是 `getCursorScreenPoint()` 在光标跨到不同缩放率的屏那一刻本身就不连续。现在每帧只叠加**当帧**位移（基准每帧重读），并且丢掉单帧超过 120 DIP 的步长——那只可能是坐标空间重映射，不是手速。复测：同一次跨缝拖动 x 保持 1000 不动 ✓，跨完之后一次 +600 的横向拖动窗口精确 +600 ✓；代价是越过接缝的那一帧会少跟一段距离（实测 98px 只跟了 22px），下一帧起恢复正常。
- **艺术家头像：查不到不等于没有**：以前"连不上网""平台没答上""这位歌手确实没照片"三件事在 `artist-images.json` 里长成同一条记录——第一次进这页时赶上断网，这个名字就被永久标成"没有头像"，只有手点「更新网络头像」才会再问。现在四家平台全都抛错时 `resolveArtistImage` 报的是失败而不是"没有"，只要有一家真正答了才记结论；图找到了却存不下来也算失败。渲染层补了三处：① 点「更新网络头像」原来绕过那条并发闸直接发请求，现在与滚动进入视口走同一排队（同时两桩），但会插到队首——整页滚到底会排进几百个后台预取，让它们先跑完的话这一下要好几分钟才有回应（第一版就是只"并入"不插队，被端到端验证当场抓到点下去没反应）；② 提示分成三种——已更新 / 各平台都没有 / 查询失败，最后一种不再冒认前两种；③ 离开这页时还没出发的预取会被丢掉，不会留一个后台队列继续问平台。实测（预览 profile、472 位艺术家）：清空头像记录后首屏 0 → 18 张、滚到底 33 位，「更新网络头像」确实把记录时间推后了一格；把封面目录临时换成同名文件让存盘必然失败时，提示是「头像查询失败」而不是"没有头像"，且这次失败没有写进记录（桥是深冻结的，换不了桩，所以只能让主进程这一次真的失败）。
- **任务栏播放控制条的三个图标一直是向上的三角形**：`make-icons.mjs` 的 `triangle()` 按"行宽随 y 增大"画，于是 `play` 是 ▲ 而不是 ▶，`previous`/`next` 同样是 ▲。改成按"距垂直中线的距离"决定行宽后重新生成 PNG，`pause.png` 与 `tray.png` 字节分毫未动（说明错的确实只有这三张）。
- **首屏之前不再被索引里的封面路径拖住**：`MusicLibrary.load()` 要逐条确认 `coverPath` 指向的文件还在，用的是 `existsSync`——几千条就是几千次同步 stat，全部发生在第一次画界面之前。改成一次异步批量 `stat`，判定与副作用（清死引用、把索引标成待刷新）一字不变。
- **批量写入的提示会张冠李戴**：「写入封面与歌词」的 toast 把整批结果里第一条 `note` 直接拼在计数后面，而那条 note 可能来自一首**没写**的歌，于是能出现"3 首未写入任何内容"和 3 首已写入同时成立。现在按结果分组分别计数；IPC 失败（例如一次超过上限）也有提醒了，原先是点了没反应。
- **设置页几处点了没反应**：数据目录位置、输出设备列表、下载目录名称这三处都是 `await` 一个可能拒绝的 IPC 却没有 `catch`。现在都收口成 toast；「可改写的格式」这类多选项补上 `aria-pressed`——读屏原先只能念出"有个按钮"，念不出它是否选中。
- **键盘焦点不再改变控件形状**：全局 `:focus-visible` 里写了一行 `border-radius`，本意是让焦点环带圆角，实际是 Tab 聚焦时把控件本身捏圆（Chromium 的 outline 本来就跟着元素自己的圆角走，这行是多余的）。
- **音源链接导入不再被一个 `%` 打断**：链接末段带非法百分号转义（`a%zz.js` 这类）时 `decodeURIComponent` 抛 `URIError`，整次导入以"URI malformed"失败。现在按原样把那段文字当文件名。
- **验证与诊断脚本自己会骗人**（都是"看起来绿"这一类，逐条改）：
  - `run-tests.mjs --online`：套件名改错一个，选中集合就成了空集，而空集按算术等于全通过。现在选不到预期的套件直接非零退出。
  - `run-e2e.mjs`：`probe-hook` 崩溃或被信号杀掉时退出码不是 1，于是被判成"生产构建里钩子不可达"——把一次根本没跑成的验证当成结论。现在只认 0 和 1，其它一律失败。
  - `e2e-verify.mjs`：`APPDATA` 没设置时"真实 profile 未被写入"会变成一项无物可比的空比较，现在直接失败退出；把副本里的绝对路径改写时原来只认反斜杠，漏一条就等于让"隔离"运行去改开发者自己的曲库，现按两种分隔符、不区分大小写匹配；被指纹保护的 profile 文件从 3 个补到 6 个（加 `downloads.json`、`pending-assets.json`、`artist-images.json`），并且一个都比不到的时候会明说。
  - `diagnose-restricted.mjs` / `diagnose-online.mjs`：一个音源脚本解不开、或 `user_api.json` 不是合法 JSON，都会把整轮诊断砸在半张表上；错误音源那一档出异常时还会留下一个 source 子进程和一个临时目录。现在各自 try/finally 收口，异常本身作为一条结果打印。
  - `capability-probe.mjs`：注释写着"两种结果都立刻清理"，可那半行 `require()` 在 ESM 里根本不存在（异常被自己吞掉），`HKCU\Software\JJProbeTest` 每跑一次留一次；写到 %TEMP%／文档／AppData 的探针文件也从没删。现在结束时会删，删不掉写进报告的 `cleanupFailed`。另外 `elevated` 字段在 Windows 上恒为 `undefined`（等于没有这项），现在按 `HKU\S-1-5-19` 是否挂载实测。
  - `privilege-probe.mjs`：受限 token 下写不出报告文件时进程直接崩，最有价值的那次测量反而丢了；现在失败会把报告打到 stdout 并非零退出。`whoami /priv` 的失败也不再伪装成一条名叫 `error: …` 的特权。
- **迁移数据目录不再允许选到自己里面**（审阅复核确认，high）：`dataDirMove` 原先只挡"目标就是当前目录"，没挡互相包含。而迁移用的是 `cp(current, target, {recursive:true})`——一边走一边写——挑了当前数据目录里的一个子文件夹，就会把刚建出来的目录再复制进它自己，一层层套到路径超长或磁盘写满；反过来挑上级目录，等于把应用的文件散到那个目录里。新增纯函数 `relocationProblem(target, current, caseInsensitive)`（same / nested / null，大小写与两种分隔符、结尾斜杠都算过），判定放在弹确认框之前。
- **「写入位置=同名文件」不再让标签匹配空转**（high）：`标题 / 艺术家 / 专辑` 这类文本字段只有文件标签装得下，可写入目标是按设置取的，于是设置选"同名文件"时 `标签匹配` 连 embedded 这一路都不再尝试——用户点了应用，一个字段没写，连"未写入"那句提醒都不发（那句只在目标里本来含 embedded 时才说）。现在带文本字段的补丁一律要求尝试标签（`writableFormats` 的交集照旧生效），写不进去才说清为什么。
- **试运行（预览）不再改动任何东西**：`dryRun` 也会把 `written` 置真，而调用方拿这个值去决定"要不要重读文件、要不要清掉待写入队列"——于是标签匹配的预览对话框一旦打开，索引被改写、那首歌的待写入条目消失。现在 `commitAssetWrite` 在 `dryRun` 下直接返回；`paths` 只列真正改过的文件（预览里为空，将来时由 `note` 表达）；"这次是否真改了标签"改用一个明确字段 `embeddedWritten`，不再拿界面文案 `'文件内嵌'` 去比——措辞改一个字就会让刷新静默失效。顺手把 `skipBackup` 与暂存文件绑死：没有 `stagingPath` 就不许跳过备份，避免以后有调用方直接改用户的文件而不留退路。
- **下载在最常见的那种失败下不再丢掉已下载的字节**：`keepPart` 原来要到文件被打开那一刻才置真，于是解析地址失败、连接失败、返回 403、请求中止这些"响应还没开始"的情况一律走清理分支，把 `.part` 删掉——断点续传恰好在它最该起作用的时候归零。现在从算出 `.part` 路径那一步起就保留。
- **续传时校验服务器给的是哪一段**：以前只要看到 206 就当续传。现在要求 `content-range` 的起点等于我们请求的偏移；没有 `ETag`/`Last-Modified` 可发（不少中转就是不给）时，再要求这一句声明的整份长度等于首答记下来的 `remoteSize`（新字段，随任务记录持久化，不随重试清空）。对不上就取消响应、删掉那段拼不上的字节，并说明"重试会重新下载整份"——宁可慢一次，也不要一个能播放但开头多了别人一段的文件。四条续传路径都有测试，并用变异验证确认过它们真会红。
- **酷狗/酷我歌单在平台没报总数时会静默截断**：`total` 缺失时旧代码把"已经拿到的数量"当总数，于是第一页刚好满 100 就收工——500 首导入 100 首，一声不响。现在只有空页才是硬停，平台报了总数就按总数走（有些接口末页本来就短），没报才用短页作结束标志；页大小抽成一个常量，防止"请求里的 100"和"判断里的 100"各改一处。
- **网易云歌单封面原来一直抓不到**：v6 详情里那个字段叫 `coverImgUrl`，代码读的是 `coverUrl`/`cover`——本机实测线上响应确认。因为封面是可选项，界面只是不显示图，所以这个错误完全看不出来；连测试的假响应都是照着错的字段写的，等于给错行为盖了个章。现在按真实字段取，另两个留作回退。
- **桌面歌词窗口的两处生命周期漏洞**：① 拖动时每 16ms 轮询一次系统光标，而窗口的 `closed` 事件只把 `this.window` 置空、没停这个定时器——进程还在转，若在这一趟拖动结束前重开悬浮窗，旧定时器会去挪新窗口；现在 `closed` 与 `stepDrag()` 都收尾（后者顺带检查 `isDestroyed()`）。② `锁定位置` 的门只在收到 IPC 消息那一刻查，而一次拖动从头到尾只发两条消息——手指还按着的时候从设置页勾上锁定，歌词条照样跟着鼠标走；现在每帧重查。
- **丢弃待写入之后徽标不会再继续喊「待写入」**：徽标读的是解析服务的缓存而不是队列，`全部丢弃` 只清了队列，所以要点回那首歌播一次才会消失，看起来像按钮没生效。现在按被丢弃的曲目逐条清缓存。（顺带一个产品问题留给用户定：丢弃之后再次播放同一首仍会重新联网找词——要不要记住"这首不要问"，是另一件事。）
- **一次拖多个文件夹不再把整个曲库扫 N 遍**：`scan()` 每次都覆盖全部已注册根目录，所以逐个 `addFolder` 就是 N 次完整遍历（代码注释原本写的正是"一次遍历"，但实现不是）。新增 `addFolders(list)`：先登记再扫一次，进度也只报一次。
- **`assets:export` 的 kinds 校验补上**：数组先判长度再过滤，于是 `['foo']` 过滤成空集合、写回一个空结果，界面只显示"没有需要写入的内容"。现在非数组直接报错，过滤后为空也报错——坏请求要说坏在哪里。
- **渲染层一批**（审阅逐条复核后修）：切到随机播放后的第一次 `上一首` 会跳过刚才正在听的那首（历史是从 `playTrackAt` 才开始记的，现在进入随机时用当前曲目打底）；在线曲目查到的来源写不回队列行（`'assets' in queued` 恰好把"本来什么都没有"的那些行挡在外面）；设置保存失败会弹两条一样的提醒；待写入队列查询失败被显示成"没有等待写入的内容"（现在是另一句话并禁用两个按钮）；迁移成功后刷新目录失败会被说成"迁移失败"；歌单的「移除封面」「从歌单移除」缺 `catch`，失败时界面停在半成品状态；歌单拖动排序并发发送两个请求时可能后发的先落库、把旧顺序留下（现在串成一条链，每次都是整表顺序）；网络导入的"改过了"只比数量，纯拖动改顺序不算改；导入预览的封面字段与破图回退（`<img>` 拿不到文件时改用应用自己的占位图标，而不是浏览器的破图）。
- **两条审阅意见经复核不成立，记在这里免得下次再报**：① `flags` 勾选连点会丢更新——`createSettingsWriter` 是乐观应用（`apply({...read(), ...snapshot})`），第二次点击读到的已经是第一次的结果；② 咪咕分页会在 `totalCount` 缺失时停在第一页——那一行早就带着 `total &&` 的守卫和解释它的注释。另有一条（渲染层的头像查询失败提示）在本轮之前已修，审阅跑的是旧提交。
- **一处测试自己走了真实网络**：「指向已被删除文件的头像记录作废」重建 store 时没带 `fetch`，于是那一步是去问线上平台"周杰伦有没有头像"——通过与否取决于当天平台答什么。补上假端点并断言"确实重新问过"之后，这一条从 539ms 变成 10ms。

### 界面

- **歌单里的歌曲可以拖动排序**（需求 8）：共享的歌曲列表新增 `reorderable`，行可以按住拖到任意位置，插入位置用一条高亮线指示，松手后整表顺序落库。此前 `playlists.reorder` 这条 IPC 与主进程的持久化逻辑都是好的，缺的只是渲染层从来没人调用它——所以这次是"接上"而不是"新写"。
- **歌单页去掉无条件「清空」，改为批量移除**：多选机制（Shift 连选、Ctrl 加选、Ctrl+A、复选框列、选择工具栏）本来就在共享的歌曲列表里，缺的只是让歌单页的动作跟上了选择——「从歌单移除」此前只作用于右键那一条，`清空` 又无论选没选都整表清掉。现在两处都按当前选择生效（多条时菜单项写作「从歌单移除 N 首」并先确认，说明歌曲仍留在曲库、可再加回来），`清空` 按钮连同 `playlist:clear` 通道一起删除：想要清空，Ctrl+A 再移除即可。主进程侧 `removeTrack` 相应改为 `removeTracks`（一次删除只落盘一次，并对渲染层传来的 id 数组做类型过滤）。顺带清掉 `QueueView` 里早已没有对应 DOM 的 `.reorder__*` 样式与歌单页的 `.bulk`。

## 0.1.7 — 2026-09-21

### 功能

- **桌面歌词**：新增独立悬浮窗，在应用之外置顶显示当前歌词行。设置·播放、播放条「词」按钮、下栏 ⋯ 菜单三个入口共用同一份状态；悬浮窗可拖动改位置（位置写入设置，重开后回到原处），「锁定位置」后鼠标点击穿透到下层窗口，字号分小/中/大/特大四档且改动实时生效。未播放时显示「曲名 – 艺术家」占位而不是空白条。悬浮窗只暴露 `onState / dragTo / openMenu` 三个方法，拿不到曲库与音源桥。

### 界面

- **标题栏**：LOGO 与「JJ Music」固定在最左，前进/后退/搜索整体右移并与侧边栏右边界对齐；窗口按钮抽成共享组件，标题栏与播放页复用同一套，最大化后按钮会跟着变成「还原窗口」并换字形（此前播放页那份是复制的，永远显示最大化）。
- **专辑按专辑名合并**：同名专辑只出一张卡（1357 → 1242），封面取该专辑下占多数字，卡上仍标出主唱。代价是同名不同歌手的专辑会合到一张卡里，这是按你选的「按专辑名合并」规则的结果。
- **艺术家**：头像由圆形改方形，且可以点击进入该歌手的作品页（此前只是图片）。
- **播放模式**：改为点击直接循环切换，上方浮出当前模式名提示 1.5 秒，不再呼出菜单；四种模式换上互不相同的字形（顺序/列表循环/单曲循环/随机）。
- **播放页**：下栏换成与主界面同款（无封面的版本，保留艺术家等信息），小歌词行不再显示；歌词来源标签移到曲名信息列末尾；原来那排歌词图标按钮收进 ⋯ 菜单与右键菜单；新增全屏按钮。
- **侧栏**：歌单管理改成与导入歌单同款的卡片式列表；除歌单外的栏目一律不可长按拖动（此前在专辑等栏目上按下拖动会误触发文件导入浮层）；搜索按钮未聚焦时也保持高亮。
- **发现页**：状态卡此前是 `minmax(170px, 1fr)`，四张卡会平分整个视口（2200px 宽时单张拉到 472px），现改为固定四列并给整行设 1076px 上限——始终一排、单张最宽 260px；曲库与音源都就绪时设置引导区不显示、页面只剩四张卡加约 520px 空白，现补一条「最近播放」封面横向轨（最多 12 张，点击即从该处播放，「查看全部」进独立页）。该轨复用「最近播放」页的同一套回查逻辑，历史里已移动或改标签的本地文件不会按旧快照播放。
- **设置·播放的「睡眠定时」行脱离分组**：它渲染在 `.settings-items` 之外，于是不带分组的底板、宽度和分隔线，看着像另一个区块。移进组内后与其它行完全一致。
- **「下载设置」按钮带下划线**：它是 `RouterLink`，即真正的 `<a>`，被 UA 样式加了下划线而 `.btn` 从未复位（侧栏与音源页各自局部补过 `text-decoration: none`）。改为在 `.btn` 上一次性复位。

### 修复

- **播放页顶部的按钮真实点击没有反应**：「收起」以及播放页那组全屏/最小化/最大化/关闭此前只能靠脚本触发，鼠标点下去没有响应。根因是 `-webkit-app-region` 既不继承、也不会因为覆盖层画在上面就让下层让位——标题栏声明了整条顶部拖拽区，播放页是盖在它上面的 `position: fixed` 覆盖层，于是那 48px 内的真实输入全被当成窗口拖动。`no-drag` 只写在容器上不够，必须写在真正接收点击的元素上（标题栏之所以正常，正是它对 `button` 逐个声明过）。已用真实系统级点击验证：改动前点「收起」`nowPlaying` 仍为 true，改动后同一坐标即收起。
  - 这类缺陷自动化看不见：CDP 的 `Input.dispatchMouseEvent` 注入渲染进程、绕过浏览器的拖拽区处理，所以「元素在最上层、可命中、pointer-events 正常」全都成立，而人就是点不动。
- **同一栏目内下钻后「返回」会直接跳出该栏目**：专辑/艺术家详情与「音乐库」打开的文件夹此前是组件内的一个 `ref`，网格原地被替换、历史里不留痕迹；而标题栏的返回是真的 `router.back()`，于是从详情按一次返回会跳过网格、直接落到上一个栏目。现在这些层级写进查询串（`?album=` / `?artist=` / `?folder=`）成为真正的历史条目：详情 → 网格 → 上一栏目。顺带把内容区的 `:key` 从 `route.fullPath` 改为 `route.path`——否则改查询会重挂视图，筛选文字与滚动位置会在每次下钻时丢失；`route.path` 仍含具体参数，`/playlist/a` → `/playlist/b` 照常重挂（实测两个歌单连续切换，标题与内容都跟着换）。内容区不重挂后，`CollectionView`（读 `route.query.q`）与 `SearchView`（`watch` 查询）本就对查询响应，无需改动。
- **开着桌面歌词时，关闭主窗口会把应用变成一条没人能关的歌词条**：`window-all-closed` 要等**所有**窗口都没了才触发，而悬浮窗也是一个 `BrowserWindow`。于是在"关闭窗口时最小化到托盘"关着（默认）时点 ✕：主窗口被销毁，`window-all-closed` 因为悬浮窗还活着而不触发，托盘图标又只在托盘模式下才会创建——结果是进程仍在跑、桌面上只剩一条歌词，既没有窗口也没有任何入口。实测：主窗口 target 消失、只剩 `desktop-lyrics.html`、进程存活。现在主窗口的 `closed` 会顺带销毁悬浮窗，实测同一操作后进程直接退出。
  - 同时补上 `mainWindow` 此前从不置空：销毁后的窗口对象留着，`second-instance` 调 `isMinimized()`、IPC 来源校验读 `webContents` 都会抛「Object has been destroyed」。现在 `closed` 里置空，`second-instance` 与 `activate` 遇到没有可用主窗口的情况会重建窗口（`quitting` 中则忽略）。
  - 托盘模式不受影响：那条路径是隐藏而不是关闭，`closed` 不触发，所以收进托盘时歌词照常在桌面继续走（已实测）。
- **收进托盘后进度条与桌面歌词会冻住（音频还在放）**：播放位置是用 `requestAnimationFrame` 采样，而 Chromium 在窗口隐藏后就不再发帧。实测：可见时 5 秒推进 5.00 秒、歌词行 0→6；关窗到托盘后同样 5 秒推进 **0.00 秒**、歌词行不动，但频谱峰值仍是 174/175、`playing` 仍为 true——即声音正常，只有界面时钟停了。这恰好打在桌面歌词最典型的用法上：把播放器收起来、只留歌词。改为定时器采样（窗口隐藏时会被节流到约 1Hz，对只用来读位置足够）。
  - 实测修复后：隐藏期间 5 秒仍推进 5.00 秒；端到端验证"隐藏后歌词继续走"——索引 0→7，悬浮窗显示的文本与播放器当前行一致。
  - 回归中 `player-regression` 先红了 5 项：测试桩里 `globalThis.window` 是手搓对象，只补过 `requestAnimationFrame`。按同样的做法补上惰性的 `setInterval/clearInterval`（这些测试不断言进度循环，真跑起来反而会在用例结束后继续发 `progress`），并删掉已无人用的两个 rAF 桩。
- **曲库文件夹白名单可以由渲染进程自己扩大（任意文件读取的第二道门，比第一道更宽）**：`libraryFolders` 就是文件访问白名单本身（`jjmedia://`、歌词读取、标签写入都以它为根），但它有两道渲染进程可开的门。一是 `library.addFolder(path)` 直接收路径；二是更宽的 `settings:update`——它会把 patch 里点名的每个文件夹 `existsSync` 一下就 `addFolder()` 并扫描，等于完全绕开选择器还能对任意目录发起全盘扫描。修复前实测：把 `.cache/outside-lib` 写进去，该目录下的文件立刻可读。现在选择器改由主进程自己打开（与 `导入文件` 同一套做法），`addFolder` 桥面不再收路径；`settings:update` 则丢弃 `libraryFolders` 键。
  - 丢弃而不是报错：调用方一次性回写整套设置（恢复默认、测试收尾还原）不该因为顺带带了这个字段就整体失败；保证的是"白名单不可能经此改动"，不是"点名它就要受罚"。返回的 `after` 带的是真实列表，渲染进程的副本同一次调用就被纠正。
  - 移除文件夹仍走专用通道（它只会收窄，且对未知目录是空操作）。
  - 真实流程已用系统级点击走通：点「添加音乐文件夹」→ 原生「选择音乐文件夹」对话框打开 → 选定目录 → 该目录进入曲库与设置；随后用 `removeFolder` 移除，列表回到原样。
  - 回归：e2e 新增两条（经设置通道扩大白名单无效 / 同一次写入的其它设置仍然生效），UI 冒烟 1 项。原先那条 `reactive folder settings save through IPC` 是**恰好**借 `libraryFolders` 当载荷的，改用渲染进程确实拥有的列表字段来证明同一件事，另加一条断言白名单纹丝不动。
- **下载目录同样不再由渲染进程给路径**：`downloadFolder` 此前只过 `isAbsolute`，而它是下载音频的落盘位置、也是标签写入的输出目录，渲染进程却可以经 `settings:update` 直接写。现在选择目录的对话框由主进程打开、由主进程写入（与曲库文件夹同一套做法），`settings:update` 一并丢弃该键。
  - 真实流程用系统级点击走通：设置→下载 点「选择目录」→ 原生「选择下载目录」对话框 → 选定 `.cache\dl-test` → 主进程记录、`settings.json` 落盘、界面该行显示新目录、下载器实际使用的也是它，五项全过。
  - 渲染进程的设置副本经"空补丁回读"同步：走设置写入器而不是直接赋值，这样与仍在途的写不冲突（写入器只在没有写进行中时才从本地重新取基准，直接赋值会与在途批次互相覆盖）。
- **曲风 / 专辑 / 艺术家详情页没有「播放全部」**：这三处进入二级后只有一张曲目列表，想整张播放只能从第一首开始手动连着听——此前唯一的整播入口是卡片上的双击，而那个双击已被证实是死的（第一次点击就把网格换成了详情，第二次点不到卡片）。现在三个详情页表头都有「播放全部」，双击那条死代码一并移除。
- **「当前播放」从歌曲页表头改为右下角悬浮小按钮**：原来它是歌曲页表头里一个带文字的按钮，于是这个"找到正在播的那首"的动作只有歌曲页有，而且位置在已经滚过三千行的列表顶端之外。现在它是固定在右下角的圆形图标按钮，歌曲页、曲风、专辑、艺术家四处都有；后三处只在正在播放的曲目确实属于这一列表时出现（否则无行可滚）。歌曲页保留原有语义：正在播放的被筛选条件挡掉时，点定位会先清掉筛选再滚过去。
  - 定位走的是列表自己的 `reveal()`，会把该行滚到视口中央并标记选中。实测滚到列表末尾取那一行来播、再跳回顶部点定位：`scrollTop 0 → 242000`，该行落在可视区内并被标记选中。
- **歌词通道不再按渲染进程给的路径读盘（任意文件读取）**：`lyric.resolve` / `lyric.searchOnline` / `lyric.candidates` 此前接收整个 `LocalMusicInfo`，而服务会照着里面的 `path` 与 `lyricPath` 去读文件——`readSidecar` 又没有白名单、扩展名与大小限制。修复前实测：传一个 `{id:'x', lyricPath:'曲库外某文件'}` 就能把该文件内容当歌词取回（同一路径走 `lyric.readFile` 则被正确拒绝）。现在三条通道只收曲目 id，由主进程从自己的索引里取回权威记录（`indexedTrack()`），索引里没有就拒绝；渲染进程再递整个对象会被直接拒掉而不是照单执行。附带把 `readSidecar` 改成 `stat` + 4MB 上限（`readFile` 自身没有上限），同步的 `existsSync` 探测一并换成异步。
  - 索引只能由"扫描用户选定的文件夹"或"对话框选中的文件"写入，所以按 id 取回等于把权威交还给主进程。`resolveLocalLyric` 的注释写明了这条前置条件——它仍会读 `track.lyricPath`，该字段全仓库只有扫描器写、且取值恒等于 `sidecarPathFor(path)`。
  - 没有做"索引查不到就退回渲染进程给的路径"：值得放行的路径 `allowedMediaPath` 本来就放行，加这条退回等于把刚关的洞再开一次。
  - 回归：e2e 新增三条（索引外的 id 被拒 / 旧的整对象调用被拒 / 真实索引曲目仍能解析到歌词），离线套件新增"超大侧车被忽略"。实测播放取词、活动行跟随与三条通道的正常路径都不受影响。
- **已索引但所在文件夹已移出配置的歌曲，可以正常写标签与歌词了**：0.1.5 引入路径白名单时，`jjmedia://` 认曲库索引（索引里的路径放行），而标签/歌词/打开位置这几条 IPC 只看配置文件夹与对话框选中的文件，两者对同一个文件判定相反——歌能播，写标签却报「路径不在允许的媒体范围内」。现在这些通道采用与协议相同的权威，渲染进程仍然无法凭一个自造的 `path` 触达索引之外的文件（e2e 增补了这条反向断言）。
- **初始化超时的音源会永久留下一个子进程**：15 秒超时只把 `ready` 拒掉，而失败分支随后是把该音源标记为错误并停用，这条路径**不经过** `stop()`——于是没有任何人回收那个进程，它连同自己的内存上限一起留到应用退出；每个卡死的脚本都是一个泄漏。拆出 `teardown()` 供 `stop()` 与启动失败分支共用，它只处理进程与在途请求，**不删** `this.runtimes` 里的记录（音源管理页正是在启动失败时才去读那份日志）。
- **启动成功后才死掉的音源，界面永远显示「已启用」**：`scriptError` 在主进程没有订阅者，`getCrashReason()` 没有调用方，于是退出原因算出来就被丢掉。现在退出处理会写错误并回退启用开关（与启动失败分支一致），但仅当这次死亡不是 `stop()` 要的——这个意图现在在进入时就记录，而不是事后推断。顺带：`stop()` 此前对已经退出的子进程也要空等满 2 秒 SIGKILL 宽限，重启崩溃音源和退出应用（`before-quit` 等 `stopAll()`）都被拖慢；启动前校验的风险缓存无界，而它的键是整份编码后的脚本（实际样本约 740 KB 一条），改为 32 条并淘汰最旧。
- **取流地址与下载不再绕过 URL 守卫**：`isValidMusicUrl` 只判协议，脚本递回 `127.0.0.1:8080` 或云元数据地址应用照抓；下载器则用裸 `fetch` 抓脚本给的地址，而主进程其它出网路径都过守卫（逐跳重校验）。两处现在都走 `assertPublicHttpUrl` / `safeFetchResponse`。
- **桌面歌词可以拖出屏幕且再也抓不回来**：`place()`（创建与重开时）会钳制位置，`dragTo()` 不会。那条 820px 的歌词条能被拖到只剩几像素可见，本次会话内无法再抓取；因为 `place()` 也钳制，重开应用看着像"自己好了"。现在拖动同样按所在显示器工作区钳制，并且这两个通道除 `sender` 外还校验 `senderFrame`——沙箱 preload 会交给窗口内每一个 frame。
- **静音后音量条变成死控件**：工具栏把音量条渲染成 `muted ? 0 : volume`，所以静音时把手停在 0；往上拖会真的设音量却仍不解静音，于是把手弹回 0——看起来是控件坏了，而不是提示你静音键开着。现在把音量抬离 0 即解除静音（拖到 0 仍是静音，那不是"要声音"的请求）。
- **清空队列挡不住下次启动的整队恢复**：`clearQueue()` 调 `stop()`，而 `stop()` 第一件事就是写会话快照——它记下的正是刚被清掉的那一队。现在快照在停止之后被丢弃。顺带两件事：为这一队设的睡眠定时不再掐掉你之后新开的播放；`lastSession` 的"清空"必须是显式 `null`，因为设置写入器会把每个补丁经 JSON 快照一遍（为了剥掉 Vue 代理与调用方数组），而 `JSON.stringify` 会丢掉 `undefined`——写 `{lastSession: undefined}` 时它与 `stop()` 刚写的快照合进同一批，然后被快照覆盖。这条是测试抓到的，不是读代码读出来的。
- **曲库与歌单列表的旧响应会盖掉新响应**：`refreshSources` 有代际保护，`refreshLibrary` / `refreshPlaylists` 没有。扫描完成事件与用户操作同时索要同一份列表时，先发的后到就会赢，表现为"刚导入的歌不见了，下次刷新又回来"。歌单与收藏现在一次并发读取、一起赋值，中间不会再出现"新列表配旧收藏"。
- **专辑 / 艺术家页的筛选框对查询串失去反应**：把下钻层级写进查询串那次（`?album=` / `?artist=`）没有给这两页补 `route.query.q` 的监听，而 `:key` 又已改成 `route.path`（不再因查询变化重挂组件）。于是标题栏的前进/后退——正是靠查询变化来走层级——会让筛选文字停在旧值上。这是我自己在这一批修复里引入的回归，回环测试中由 `?q` 相关断言抓到。
- **换强调色时桌面歌词不跟着换**：悬浮窗的载荷里那句 `getComputedStyle(...)` 读的是文档上的 CSS 变量，但整个 computed 没有任何依赖会因强调色改变而重算——封面取色又是异步的。表现是改了颜色（或换到取色不同的专辑）后，歌词条停在旧色，最长要到下一句歌词才跟上。现在它依赖主题代码在颜色落地时递增的计数。
- **`dialog` 桥面整组已死**：`dialog.openFolder` 在两个目录选择器移进主进程后就没人调用，`openFiles` / `openLyric` 更早就没有调用方（`library.importFiles`、`lyric.importFile` 各自在主进程开框）。三条通道都留着 `rememberPicked()` 副作用——即渲染进程可以借原生对话框把任意路径送进媒体白名单——一并删掉。另外 `TitleBar` 注册的 `router.afterEach` 从不解绑（全局守卫活得比组件久），`TransportControls` 上那档没人用的 `size="lg"` 与 `hideMode` 也删了。
- **主窗口隐藏后定时器仍被节流**：进度时钟改成定时器之后，`backgroundThrottling` 仍为默认的 `true`，隐藏时会被压到约 1Hz（5 分钟后约 1 分钟一次）。现在显式关掉，位置读数不再依赖窗口是否在前台。

### 测试

- **UI 冒烟里「发现页不得出现 `.recent`」这条断言改为断言真实契约**：它写于最近播放整张列表被挪出发现页那次，判据是"发现页没有任何 `.recent` 标记"。发现页补回紧凑封面轨后它会误红。现改为"发现页不得内嵌历史 TrackList 行，且轨道要交棒给 /recent"——当初要防的是重复那份列表，不是这块数据本身。
- **桌面歌词新增离线与运行时两层覆盖**：`desktop-lyric.test.mjs` 测位置钳制（含多屏负坐标工作区）、静止位置、当前行取值与旧设置文件缺字段的合并；UI 冒烟新增 7 项，实测开关会开出/销毁第二个窗口、悬浮窗拿不到 `jj` 桥、显示的是播放器当前那一行、窗口透明而非灰块、字号与锁定状态会传过去。
- **`npm run test:e2e` 不再使用你正在使用的数据目录**：它此前直接对 `%APPDATA%\jj-music` 运行，并在结束时把开跑时抓到的**整份设置快照**写回去——期间应用自己写入的播放历史（`recentPlayed`、`lastSession`）会被回退，若你同时开着应用，新增的曲库文件夹也会被抹掉（曲库文件夹在 `settings.json` 与 `index.json` 两处各存一份，启动时按并集纠偏，所以两边同时为空时才会留下"歌还在、文件夹没了"的状态）。现在它与 UI 冒烟一样跑在副本 profile 上（`--keep-profile` 可保留），并新增一条断言实测真实目录未被写入；副本用完自动删除。冒烟脚本同样补上了清理——它每次会复制约 950 MB（含封面缓存），此前 7 次运行在 `.cache` 留下 6.6 GB。
- **联网搜索不再因偶发抖动听红**：酷狗会对正常关键词偶发返回空列表（6 次运行中 2 次，且 1.5 秒内的两次重试都空），QQ 端点偶发连接重置。这类上游抖动原先直接判为失败，现按 1.5 秒、6 秒的退避最多试三次，并在输出中标注。
- **`salt-ui-smoke` 依赖真实曲库数据**：其中 2 项检查需要多歌手、多曲目的曲库，profile 被清空成 1 首时必然失败（并在后续选择器上崩溃）。恢复曲库后 35 项全部通过。
- **钉住酷狗字面量解析器不变成 RCE**：那个端点返回的是 JS 对象字面量，解析等于执行来自网络的一段文本，而挡在它与主进程之间的只有沙箱对象的形状——`Object.create(null)` 没有可走的原型链；换成普通 `{}` 时它被 contextify 到宿主 realm 的 `Object.prototype` 上，`(function(){return this})().constructor.constructor` 就是主进程自己的 `Function`，响应体可以直接读 `process`。两种形状都实测过：空原型抛异常，`{}` 返回真实 pid。
  - 这条断言第一版**是空过的**：它写成了 `this.constructor(...)`（把 `Object` 当函数调用），拿到的是 TypeError 而不是宿主对象，于是把沙箱换成 `{}` 它照样绿。改法是把内置产物里的沙箱改弱后重跑，看着它变红（`sandbox leaked process.pid = 17004`）再恢复。
- **静音与清空队列补两条离线断言**：`player-regression` 新增"抬音量即出静音"和"清空队列丢掉续播点与睡眠定时"。睡眠定时那条不只看 `sleepAt` 标签被清空——定时器被惰化记录，断言的是 30 分钟那个回调确实不再挂着，否则只清标签也能过。两条都做了同样的变异验证：从构建产物里删掉解静音与 `cancelSleepTimer()` 调用后转红，恢复后转绿。
- **探针里两处把实现细节当契约的地方**：`verify-round13` 断言的是 `.transport--md` 这个类名，而"两栏控件尺寸一致"真正的意思是渲染出来的盒子相同——改为量取 `.icon-btn` 与播放键的实际尺寸和间距并比较（实测两栏都是 6×32px + 40px 圆盘、间距 9px）。`verify-round14`/两个截图脚本按 `.transport--md` 找播放模式按钮，随那档尺寸一起改掉。
  - 顺带记一次误判：这批探针一度集体报"页面切换后选择器为空"。原因是预览实例被前面某个脚本最小化进了后台——最小化的窗口只拿约 1fps 的帧，Vue 的过渡要等帧才推进，1.5 秒的 `sleep` 就不够了。把窗口还原后同一批全绿；这不是应用缺陷，但探针"睡固定时间"的写法在无人值守时会自己制造红。
- **审阅后确认不是缺陷的三条**：主进程文本解码固定按 UTF-8（实测 QQ 歌词、QQ 搜索、内置端点都回 `charset=utf-8`，而真正会遇 GBK 的音源侧 `lx.request` 早已用 iconv 按头解码）；`scanExtensions` 是写入但从不参与扫描的僵字段（UI 也没暴露它，不构成"设了没反应"的谎言）；`filesDropped` 的路径来自 preload 的 `webUtils.getPathForFile`（真实拖放），不是渲染进程自造的字符串。

### 发布与 CI

- **`SHA256SUMS.txt` 由 `npm run dist` 生成**：此前脚本只*报告*产物，校验文件每次手工拼——而 README 把它作为发行资产链接着，于是它可能悄悄指向已经不存在的文件（0.1.6 那份在仓库里就 `sha256sum -c` 不开）。现在按 `package.json` 的版本挑选 `.exe` / `.zip` 写哈希，**一个产物都没有时直接失败退出**；`release/` 里留着 0.1.2 到 0.1.6 的旧文件（还有两枚空格命名的 0.1.2 遗留），按版本筛选正是为了防止它们混进校验文件。
- **CI 现在会构建用户真正下载的那两个文件**：`packaged-sources` 作业此前只跑 `npm run pack`（产出 `win-unpacked`），而发行页上是 NSIS 安装包与 Portable ZIP——`nsis` / `zip` 配置坏掉、或校验文件指向不存在的文件，CI 都不会红。现在追加 `npm run dist`，再用 `sha256sum -c` 校验，并断言校验文件恰好两行、都属于本次版本。
- **`verify-package.mjs` 补上 `app.asar.unpacked`**：它只解 `app.asar` 找特性标记，而 0.1.1–0.1.5 那次故障恰好在这条视线之外——归档本身看起来完全正常，缺的是归档*旁边*的 `iconv-lite`（fork 出来的宿主以纯 Node 运行，没有 asar 支持）。现在直接断言宿主脚本与两个依赖包在磁盘上存在，另加一条桌面歌词标记。

## 0.1.6 — 2026-09-19

热修复：已发布的 Windows 包在干净电脑上启动不了任何音源。

### 修复

- **打包版音源不再全数启动失败**：音源子进程在加载自己的依赖时就中止（`ERR_MODULE_NOT_FOUND`），界面表现为「音源进程退出（退出码 1）」，任何脚本都跑不起来。根因是宿主 `source-host.ts` 顶层 `import iconv from 'iconv-lite'`，而 `asarUnpack` 只解出了宿主文件本身。子进程以 `ELECTRON_RUN_AS_NODE` 运行，是纯 Node、不认 asar，于是沿宿主真实路径逐级向上找 `node_modules`——而那个依赖还封在 `app.asar` 里面。现已把 `iconv-lite` 与它自己的依赖 `safer-buffer` 一并解包。
- **影响范围：0.1.1 / 0.1.2 / 0.1.3 / 0.1.5 的全部 Windows 包**。这三行的配置与宿主 import 逐版相同，所以从 0.1.1 起就这样。它也解释了此前那份「另一台电脑音源开不起来」的报告：本机一切正常，是因为 `release/win-unpacked` 位于仓库之内，祖先目录里的开发用 `node_modules` 悄悄补上了这个依赖。
- 实测数据：从 GitHub 取回的 0.1.5 免安装包，21 个脚本一个都起不来；只在解包目录旁放上这两个包、其余字节完全不动，`tx, wy, kw, kg, mg` 立即全部上报。修复后的 0.1.6 包在仓库外运行时，20 个采样中 19 个启用成功、上报 7 个平台、并解析到一个真实的在线播放地址（第 20 个是被启动前校验拦下的 `独家音源`，属预期）。

### 测试

- **打包版检查改为在仓库之外运行**：`clean-profile-sources` 现在先把应用复制到临时目录再启动，只有确认祖先目录里没有开发用 `node_modules` 时才就地运行（`--no-isolate` 可关）。此前这条检查——连同 CI 的 `packaged-sources` 作业——永远在仓库内跑，对本次这类缺陷完全免疫；合成音源也测不出来，因为崩溃发生在宿主的 import 阶段，早于任何脚本代码。加固后的检查经反向验证：把有缺陷的 0.1.5 布局放进仓库内跑，仍报 3 项失败并提示依赖解析问题。
- **检查结论更贴近构建本身**：被启动前校验主动拦下的音源、以及每个配置最多 20 个音源的产品上限，不再计为构建失败；子进程报 `ERR_MODULE_NOT_FOUND` 时会打印定位提示。

## 0.1.5 — 2026-09-19

咪咕在线音源、访问边界加固、启动失败可见化、打包版音源门禁，以及一批测试与诊断工具的修复。

### 功能

- **咪咕音乐在线搜索与歌词**：全局搜索新增咪咕平台，在线歌词覆盖扩展到 QQ、网易云、酷我、咪咕四家。适配器使用咪咕网页播放器当前调用的接口（无需签名与鉴权）；此前广为流传的移动端接口已经废弃，只会拿到重定向后的 HTML。咪咕的搜索响应本身就携带歌词地址，因此歌词不需要第二次查询。
- **咪咕平台抽测恢复**：平台验证靠宿主搜索取测试曲目，而咪咕此前没有搜索适配器，只能一直显示「无法取得该平台的测试歌曲」。现在能真实解析地址并读到音频数据。
- **全局搜索的平台标签改由主进程上报**：标签列表原先是一份手写副本，与适配器列表各自漂移；现在直接取主进程报告的适配器，以后新增平台无需改动界面即可出现。

### 移除

- **副歌识别**：歌词重复段落的副歌推断、进度条上的副歌高亮色带与「跳到副歌」入口一并删除。说明开销量级以免误读：检测挂在派生值上，只在歌词变化时算一次（300–1000 行实测 6.6–15.1 ms），并非常驻负担；移除是因为价值不足以抵偿维护成本，而不是因为拖慢了播放器。附带消除了 `LyricLine` 的重复声明及其毫秒/秒单位换算。

### 安全加固

- **本地文件访问统一收口**：歌词读写、标签写入与「打开文件所在位置」此前接受任意绝对路径，现在与 `jjmedia://` 共用同一套曲库白名单——解析符号链接后再判定边界，用户通过对话框主动选中的文件单独放行并设有数量上限。
- **出网地址受限**：封面抓取与平台抽测不再接受任意地址。拒绝非 http(s)、携带凭据、以及回环 / 私网 / 链路本地 / 元数据端点；重定向逐跳重新校验（公开主机的一次跳转即可绕过只看初始地址的检查）；响应体施加大小上限。
- **酷我搜索改用 HTTPS**：其响应内容会被求值解析，明文传输意味着链路中间人可注入可执行代码。实测两种协议返回逐字节一致。

### 修复

- **启动失败不再静默**：初始化抛错过去会留下一个没有窗口、没有托盘、也没有任何提示的进程，与"应用打不开"无从区分。现在写出 `<数据目录>/startup-error.log`（含堆栈）并弹出错误对话框后退出。

### 其他

- **新增 CI**：向 main 推送或提交 PR 时，在 Windows runner 上执行类型检查、生产构建与全部无头套件。此前 README 与 docs 宣称的各项验证结果全部依赖单机手工执行。
- **二进制镜像迁移**：Electron 与 electron-builder 的下载镜像从 `.npmrc` 改到环境变量（`ELECTRON_MIRROR`、`ELECTRON_BUILDER_BINARIES_MIRROR`）。npm 11 已在警告将不再向生命周期脚本透传未知配置键；迁移后仓库默认从官方源获取，需要镜像的机器自行设置。
- **`npm run diagnose` 恢复可用**：该脚本因编码损坏存在语法错误而无法启动；修复后又暴露出它导入音源后从不启用（新版导入默认停用），于是一个脚本都没启动、反倒把空结果报成 5 个引擎故障。
- **UI 冒烟检查恢复完整**：自 0.1.3 播放页底栏重构后，脚本点击的选择器所指的 class 已不存在，导致其后 **13 项** 检查从未执行——含 EQ 生效与持久化、播放列表切歌、最小窗口抽屉、内置收藏无删除按钮、设置突发写入。现已全部执行并通过。
- **端到端断言纠正**：「平台必须全部属于 LX 固定五家」的检查与产品自身的类型定义和测试相互矛盾（音源脚本声明私有平台是有意支持的行为），改为校验 `normaliseSources` 实际保证的性质。
- **无头套件构成变化**：新增 URL 守卫与路径作用域两组离线断言，移除副歌套件，共 17 个套件。

## 0.1.3 — 2026-09-18

下边栏统一、播放体验与在线播放修复。

### 界面

- **下边栏统一**：主界面与播放页的下边栏由同一个组件渲染——收藏（❤）移入控制按钮组最左侧，播放队列按钮移入最右侧，两处按钮序列完全一致，仅尺寸与封面不同。
- **播放页布局**：控制与进度移到底部独立控制栏；左侧只保留大封面；封面进入播放页时有自下而上的飞入过渡。
- 播放页歌名移入底栏（突出显示），艺术家 / 专辑 / 音质作为次要信息随行展示。
- 设置 → 外观 → 渲染 新增「界面文字大小」（85%–150%，与窗口缩放独立，默认不变）。

### 主界面

- 侧边栏移除「文件夹」入口：文件夹浏览并入音乐库页（点击文件夹即可查看其中曲目），`/folders` 旧链接自动跳转。
- 「歌曲」与「播放队列」页新增「当前播放」按钮，可定位并选中正在播放的曲目（曲库中被筛选隐藏时会先清除筛选）。
- **记住上次播放**：关闭应用时的曲目、队列与进度会被记录，下次打开自动恢复到上次位置（不自动出声，按播放继续）。

### 播放页

- **修复歌词无法滚动**：歌词列高度不再被布局压缩，上下留白改用随容器高度解析的占位元素——此前固定 `40vh` 内边距在窗口较矮时会让歌词区完全无法滚动。
- 歌词行距收紧（默认约为原来的 70%），并在 设置 → 外观 → 歌词 新增「行距」调节（1.2×–2.6×）。
- 歌词中重复段落自动识别为副歌，进度条上以高亮带标注，点击直接跳到副歌。

### 修复

- **修复在线播放报「不支持的音频格式或地址不可用」**：部分音乐 CDN 的响应自带跨域许可头，播放器此前会再追加一个，导致头值重复而被浏览器拒绝。现在仅在响应缺失时补充。
- 修复恢复上次播放时进度被归零的竞态（恢复位置现在随音频加载直接应用）。
- 修复初始化完成后崩溃的错误被吞掉、进程变僵尸的问题。

### 性能

- 副歌识别速度提升：500 行歌词从约 143ms 降至约 9ms（长歌词不再阻塞界面）。

### 其他

- 移除从未生效的「桌面歌词」设置数据项（功能本身在后续路线中）。

## 0.1.2 — 2026-09-18

音源安全与稳定性更新：新增启动前校验与隔离，修复音源启动失败、多音源可用性和播放器交互问题。

### 音源安全

- **启动前校验**：启动音源前执行静态检查，按「阻断 / 提示 / 说明」分级。阻断项包括系统关机与重启命令、Win32 关机 API、执行外部程序、加载 `child_process`、终止自身进程、访问原生绑定、电源控制调用。
- **组合特征拦截**：对「服务端授权下发 + 代码加密不可审阅」这一组合特征的脚本直接拒绝启动。该特征来自一次实测事故：某音源在启动时调用了系统关机命令。
- **隔离机制**：校验未通过的音源会被自动停用并持久隔离，重启后不会重新启用；需在界面显式「解除隔离」才能恢复，且解除后仍需单独启用。
- **导入默认停用**：新导入的音源默认不启动，避免导入即运行。
- 支持批量「全部启动 / 全部关闭」，每个音源仍单独执行校验。

### 音源可用性

- 修复部分音源初始化失败的问题：运行环境补齐 `require` / `__filename` / `__dirname` / `module` 等 CommonJS 全局对象。带环境自检的脚本此前会因此静默退出。
- 多个音源支持同一平台时，按启用顺序依次尝试，某个音源失败会自动切换下一个。
- 音源启动失败时，界面开关会正确回退为停用状态。
- 修正 `musicInfo.id` 缺失导致的取流失败（已安装音源中有 11 个会读取该字段）。
- 保留脚本声明的非标准平台标识，不再被静默丢弃。
- 收窄「执行外部程序」校验规则，修复其误拦正常音源的问题。

### 播放器

- 播放页音量改为竖向推子，悬停扬声器图标弹出，支持点击、拖动、滚轮调节；主界面为常显横条加数值。
- 播放页右上角补充最小化 / 最大化 / 关闭按钮。
- 修复重新进入播放页时歌词先跳到 0 秒再滚回播放位置的问题。
- 固定底部播放控件位置，修复歌词长度变化导致控件左右移动、以及控件点击无效误触发进入播放页的问题。
- 在线取流增加超时与卡顿检测，失败时自动尝试其他音源。

### 说明

- 本版本曾实现基于受限令牌的启动隔离（剥离系统关机特权），因实测使用体验不佳已停用，实现保留但默认关闭。当前音源安全依赖启动前校验与隔离机制。
- 已知限制：静态校验无法读取加密脚本中的字符串，因此「校验通过」不等于绝对安全，请只使用可信来源的音源。

## 0.1.1 — 2026-09-17

首次完整发布：本地曲库、在线搜索与播放、音源管理、下载、歌词与公开歌单导入。

### 功能

- 主界面、播放界面、设置页面及多级菜单；发现音乐与最近播放。
- 分类、歌单和队列数量；在线平台自动抽测状态。
- 本地优先全局搜索、播放页面 EQ 与播放列表。
- 在线歌曲按音质下载、批量任务、LRC、MP3 / FLAC 歌词及封面嵌入。
- 网易云、QQ、酷狗、酷我公开歌单预览和导入。
- Windows x64 安装版、完整免安装 ZIP。

### 终版修复

- 修正 MPEG-4 / M4A 与 ADTS 文件格式识别。
- 为不支持硬链接的磁盘提供下载保存回退，同时避免覆盖同名文件。
- 退出时中止下载调度、清理临时文件并等待数据保存。
- 修复同路由切换搜索词不刷新、下载取消提交竞态及重试遗留路径。
- 校正标签写入能力，拒绝损坏 FLAC 或备份失败时修改原文件。
- 为封面和歌单响应增加读取大小限制，保持完整响应超时约束。
- 版本号由构建配置统一提供，打包前移除测试环境开关。

验证：14 个离线测试套件、30 项播放端到端检查、40 项实际打包程序检查通过。在线服务状态可能随平台及网络变化。
